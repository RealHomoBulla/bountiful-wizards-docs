# Игровой чеклист

Отметки от 06.09 приняты и сохранены полностью. Ниже — незакрытые игровые проверки.

---

# 🎮 ПЛАН НА ОДИН ЗАХОД В МИР — собран 2026-09-11

⚖️ **Зачем он появился.** В ночь на 11.09 впервые все семнадцать разделов `systems/` измерены по
одной шкале, и стало видно то, чего по отдельности не было видно: **узкое место пака сместилось с
«мы этого не сделали» на «этого никто не играл»**. Самый сильный раздел (лут, 79 %) держится на
сверке файлов и логах — ни одна из его семи частей не подтверждена наблюдением в игре.

🔑 **Это НЕ новые проверки.** Это те же самые, что уже лежали ниже и по семнадцати страницам, только
**сгруппированные по одному физическому проходу по миру**, а не по разделам: всё, что делается у
одного верстака, в одном биоме или за один рейд, стоит рядом. Собрано из 17 системных страниц, 65
свежих отчётов, 238 строк TODO и 122 неотмеченных строк этого файла; дубли сведены, сделанное
убрано. **46 строк вместо 155.**

⏱️ **Оценка:** **3–4 часа коротким маршрутом, 5–7 полным.** Проходы можно делать по одному в разные дни —
они разделены по зависимостям, а не по времени.

⚠️ **Порядок прохода 0 обязателен**: пока изолированный смоук Factory Siege не пройден,
естественную волну в твоём мире запускать нельзя.

📌 **Старый список сохранён целиком ниже, ни одна твоя отметка не тронута.** Если этот план
окажется удобнее — скажи, и я сверну старый в архив; сам я его не выбрасывал, потому что это твой файл.

Каждая строка — один физический проход по миру, с id из реестра для всего, что надо найти, ожидаемым
ответом и тем, что её блокирует. `[файл]` значит, что статическую часть уже проверили по файлам; сам
результат действия проверяется только в игре. `[считать]` — там, где ожидаемое число ещё не выводится
из локальных файлов.

### Проход 0 — чистый старт, вход и тестовая копия (10–20 мин)

1. **Изолированный смоук Factory Siege.** В **отдельной тестовой копии** клиента и сервера включи пару `factory_siege-0.0.11-test.jar`, подними обе стороны, выполни `/factorysiege debug dependencies` и `/factorysiege debug sculk-invariant`. Ждём: ни одной ошибки зависимостей Forge, миксин применился, обе команды прошли. 🔴 **Блокирует все игровые строки Factory Siege — естественную волну в твоём мире до этого не запускать.** `[файл]` джарник и пины сходятся.
2. **Свежий вход на сервер.** Подними текущий сервер, зайди одним клиентом, походи две минуты, выйди. Ждём: подключение проходит, дисконнекта нет; **строки `[ERROR]` записать отдельно**, не смешивая с результатом. Гейт для всех интерактивных строк ниже.
3. **Загрузка и вход после миграции FTBQ.** Только **после** миграции Construction Wand при закрытой игре: подними и подключись. Ждём: ни одного падения по отсутствующему предмету или иконке квеста. Проверить живые id: `buildinggadgets2:gadget_copy_paste`, `effortlessbuilding:diamond_randomizer_bag`, `buildinggadgets2:gadget_building` — неверный или отсутствующий id это провал загрузки. Блокирует квестовые строки.
4. **Экран анимаций и сохранение биндов.** Открой управление анимациями и переподключись. Ждём: `animlib` грузится, `Tab` открывает Tool Belt, `Ctrl+E` — Create Toolbelt, Combat Roll остаётся на `R`, приземление ParCool на `Y`, и ни один конфликт не переписал сохранённый профиль.

### Проход 1 — генерация нового мира и структуры (45–90 мин, в основном разведка)

5. **Конечные месторождения RNS.** В **свежем мире** после чистого старта найди и вырабатывай `create_rns:deposit_silver`. Ждём: обе стороны показывают `create_rns` 1.3.1, `infiniteDeposits=false`, и месторождение в итоге истощается. Убедись, что на разрешённом маршруте не появляется запрещённых `create_rns:deposit_wolframite`, `deposit_uranium`, `deposit_osmium`. ⚠️ Лестницу генерации не оценивать, пока не использованы свежие чанки.
6. **Маршрут руды Оверворлд/Незер.** В нескольких **новых** чанках Оверворлда запиши биом, Y, координаты и id блоков для `minecraft:gold_ore`, `iron_ore`, `diamond_ore`, `diamond_block`, `emerald_ore` — изумруд отдельно в `minecraft:emerald_peaks` и в обычных биомах. В новых чанках Незера отдельно посчитай `minecraft:ancient_debris`, `nether_gold_ore`, `quartz_ore`. Ждём: нет старой стены в 250–500 блоков до `ancient_debris` и не потерян заменяющий бюджет. ⚠️ **Одна пустая выборка — не доказательство отсутствия.** Блокер: сначала применить `OREGEN-NETHER-CUSTOM-BUDGET-Q5-1`.
7. **Прочие руды и индикаторы.** В новых чанках проверь корундовые друзы Quark (живой представитель `quark:black_corundum_cluster`, остальные цвета выпиши из EMI), `salt:rock_salt_ore` и `salt:deepslate_rock_salt_ore`, `caverns_and_chasms:spinel_ore`, `ae2:certus_quartz_ore`. Ждём: корунд вне океана, но не в океане; каменная соль на пляжном/океанском/капельном маршруте, но не в обычном камне вглубине; сниженная частота шпинели и аметиста; природного `ae2:certus_quartz_ore` нет вовсе. Над подтверждённой жилой примени металлоискатель и **запиши реальный id блока-индикатора** — выдумывать неизвестный id нельзя.
8. **Переход серебра и палладий.** При чистом запуске EMI обязан показывать `galosphere:palladium_lattice` и `galosphere:palladium_bomb` на прямом `immersiveengineering:ingot_silver`; ни один рецепт не должен светить снятые `galosphere:silver_*`. В свежем чанке не должно быть `galosphere:palladium_ore` и его глубинного варианта; в старом чанке запиши, ремапит ли его Block Swap. Затем скрафти девять `caverns_and_chasms:raw_silver` через `immersiveengineering:raw_silver` 1:1 и собери блок. Блокер: решение владельца по серебру и починка мёртвых `silver_*` рецептов.
9. **Структуры и радиус поиска.** После чистого старта сервера: в логе **ноль** строк `Found invalid structure namespace entry` и `Found invalid structure set entry`. В новом мире `/locate structure sculkhorde:sculk_tomb` — запиши результат. Проверь `/locate structure integrated_stronghold:stronghold` против ока Эндера от спавна. Посети `repurposed_structures:mansion_oak` и опознай `illagerinvasion:archivist`, `basher`, `invoker`, `provoker`; запиши, встречается ли рядом ванильный `minecraft:vindicator`. Блокер: схема/дистанции Structurify и деплой датапака иллагеров.
10. **Подземелья и BOMD.** В `dungeonnowloading:temple_of_duality` активируй `dungeonnowloading:fairkeeper_spawner` его родным редстоун-механизмом. Ждём: `fairkeeper_alert=true`, 1–4 заспавненных моба, затем одноразовое исчезновение. Отдельно, после условий выхода BOMD, найди и подерись с `bosses_of_mass_destruction:obsidilith` на его арене.
11. **Ландшафт Integrated Villages.** В свежесгенерированном чанке посети структуру Integrated Villages (например `/locate integrated_villages:airship_village` или `mossy_mounds`). Ждём: нет висящего в воздухе рельефа и небезопасной посадки, старт структуры валиден. Запиши id структуры и координаты.
12. **Маршрут Deeper and Darker.** Дойди до `/locate biome deeperdarker:echoing_forest` (или `deeplands` / `blooming_caverns`), найди природные `deeperdarker:echo_log` и `echo_leaves`, добейся выпадения `deeperdarker:echo_sapling`. В `/locate structure deeperdarker:ancient_temple` возьми Шёлковым касанием природную `deeperdarker:ancient_vase`. Ждём: одна ваза в инвентаре и закрытая задача `3F72C953DE0304C3`. Заодно запиши сенсор и шрайкер мотерлода и `can_summon=true`.
13. **Инструменты дальней разведки.** У свежей жилы примени `immersiveengineering:coresample` и открой геологию в Полевом справочнике. Ждём: непустой результат поиска и 17 записей геологии; в EMI **нет** `kubejs:prospector_drill`, `kubejs:core_sample`, `kubejs:sample_cartridge`. Отдельно проверь OreExcavation и Liteminer: какой инструмент срабатывает и есть ли конфликт.

### Проход 2 — деревня, торговцы, квесты и доставка (30–50 мин)

14. **Поверхность торговцев.** В одной деревне осмотри `horn_merchant`, `visitor` и караван: запиши предлагаемые id, момент появления и любое пересечение с `bountiful:decree`. ⚠️ **Снятый `silkroute_trader` не искать** — его нет по живому перечислению.
15. **Заказ на зачарованную книгу.** Возьми у Гидеона или Векса заказ на `minecraft:enchanted_book`, принеси запрошенное зачарование нужного уровня и сдай. Ждём: заказ закрывается сразу. Запиши владельца доски и запрошенный NBT `StoredEnchantments`. Блокер: свежий синк и рестарт после исправленных файлов wants.
16. **Компас у исследователя AE2.** Осмотри предложение `ae2:fluix_researcher`. Ждём на выходе `ae2:meteorite_compass`, **никогда** `minecraft:compass`. Запиши цену в изумрудах и видимость предложения.
17. **Видимость строк по репутации.** После `/reload` открой доску с репутацией ниже нуля. Ждём: строки Холлиса, Флинта, Кобба и Гаррика видны при `repRequired=-30`, а восемь бывших `-10000` исчезают ниже −30. Запиши по одному примеру в обе стороны с id пула.
18. **Напитки и карты.** Осмотри у Hazel `farmersrespite:coffee`, `long_coffee`, `strong_coffee`, у Tilly `farmersrespite:green_tea`: предметы и количества отображаются, награда приходит. Купи Castle Map и запиши цену в изумрудах и дополнительный предмет. Прокачай картографа Conjurer и купи его карту: ждём цель `conjurer_illager:theatre` и **отсутствие** ошибки `conjurer_illager:on_theater_maps` в логе. Точные id аддонов копировать из EMI.
19. **Физический цикл Wares.** `/wares give`, открой запечатанный договор, запиши требуемые id, количества, партии и выплату. Поставь `wares:delivery_table` и `wares:cardboard_box`, достав вручную; ждём `доставлено == заказано` и Completed Delivery Agreement. Повтори с безработным взрослым жителем до профессии `wares:packager` — ждём автоматическую доставку. ⚠️ Это проверка **физики доставки**, а не нашей экономики.
20. **Награды квестов и командное убийство.** После миграции FTBQ выполни одну исправленную живую цель и убийство `questkilltask` **вдвоём**. Ждём: **ровно одна награда на команду**, а не по награде каждому. Запиши id квестов и UUID получателей.

### Проход 3 — бой, анимации, навыки и магия (35–60 мин)

21. **ParCool и текущая поверхность навыков.** На новом профиле открой актуальный ParCool Guide, проверь Roll/Tap/Breakfall и Cling to Cliff, убедись, что Dodge выключен. Ждём: приземление ParCool на `Y`, Combat Roll на `R`, HUD выносливости и Cling работают, а в старом меню Puffish «Paths» **нет категорий**. В `specialities` ждём 15 навыков с нулевого уровня и видимой полосой опыта; прокачай один боевой и запиши **реальное** изменение урона и задержки атаки, а не потолок из файла.
22. **Гейты магии и редкость.** **До** выдачи `can_magic` проверь `irons_spellbooks:graybeard_staff`, магический аналой и пять станций Unraveling — ждём отказ. Выдай `can_magic`, повтори — ждём приём; затем проверь изученное заклинание не более чем из четырёх школ. На выделенном сервере получи более высокую редкость, впечатай заклинание выше стартовой редкости и проверь путь наковальни D2: ждём приём только в пределах открытой редкости и отказ выше неё.
23. **ISS Ponder и поверхности аддонов.** В EMI открой W на заклинании `irons_spellbooks:guiding_bolt`, дай Ponder-игроку кастануть по манекену, затем сам кастани неизученное. Ждём: у Ponder реальный каст и эффект, у неизученного отказ без эффекта, и **никакого** `gui.iss_ponder.cast_failed`. Осмотри вкладки аддонов Iron's и кастующего из Mobbility: вкладки непустые и без дублей, каст видно.
24. **Магический лут и потолок реликвии.** Открой Цитадель после рестарта. Сначала: в свежем логе **нет** `Unknown type 'minecraft:set_written_book_pages'` и `Couldn't parse element loot_tables:irons_spellbooks:chests/citadel/citadel_tomes`. Затем открой полку `irons_spellbooks:chests/citadel/citadel_tomes` — ждём одну обычную именованную `minecraft:book`. В EMI убедись, что у отключённого набора реликвий нет рецепта, и получи одну разрешённым лутом. Проверь, что `marrelics:sunpiercer` `slayer` останавливается на 12 уровне и +395 урона. Блокер: синк мастера и рестарт.
25. **События и боевые визуальные проверки.** В Оверворлде `!events status`, затем `!events test cache` и `!events test star`. Ждём: помеченный костёр и лагерь плюс сундук Lootr; у звезды — обсидиановый след 3×3, огонь и сундук. Обычной ночью `!events test quiet`: естественные враждебные спавны замирают и возобновляются на следующем обычном рассвете или после рестарта. ⚠️ **`/time set 13000` не использовать.**
26. **Регрессия анимаций и CEM.** В новом виде от третьего лица проверь зацеп и вис ParCool, ванильный и Better Combat лук, арбалет, `takesapillage:archer` и удар мечом. Ждём: руки, поза и текстуры корректны, возврата FA+Player нет, верёвка остаётся обычной.
27. **Регрессии боя и взаимодействия с миром.** Поставь и проверь: поведение Absorption и Health Boost у `vminus`; исключённые предметы по правой кнопке (стойка блока **не** должна включаться); сбор `ftbezcrystals` правой кнопкой (без дюпа); вторая разделочная доска `offcutting` в левой руке (один выход, прочность тратится, пустая доска не даёт ничего); синхронизацию поездов `createthreadedtrains` между клиентом и сервером; `effortlessbuilding` в чужих клеймах (ни обхода, ни дюпа). Каждый id копировать из EMI до записи.

### Проход 4 — один технологический стенд (35–55 мин)

28. **Генри и снежная петля.** В EMI убедись, что рецепт `create_henry:freezing/snow` **не** превращает `minecraft:snow` в блок снега. Прогони 3 `minecraft:snow_block` → 6 `minecraft:snow` → заморозка Генри, и отдельно `minecraft:snowball` → слой. Ждём: удвоения «слой → блок» нет, строки Марлоу и Эшби не изменились.
29. **Direct Chute.** Отправь больше 16 `minecraft:copper_ingot` из верхнего сундука через `direct_chute:direct_chute` в нижний за 20 тиков, затем повтори с такой же ориентацией жёлоба Create. Ждём: больше 16 приходит в ожидаемом направлении в обоих случаях.
30. **Pneumatic Express.** Прогони 16 `minecraft:copper_ingot` по маршруту насос → труба → приёмник и выгрузи/загрузи чанк. Ждём: всё доехало и пережило. На той же линии посмотри в очках давление, вакуум, направление, скорость и число предметов — пустой или ложный оверлей это провал. Блокер: сначала закрыть известный NPE корзины FD.
31. **Энергетическая лестница фиксированного состояния.** Собери описанный котёл 18 уровня с 18 отдельно передаточными `createaddition:alternator` на 256 об/мин. Ждём **ровно 2 610 FE/t** без перегруза. При полном небе замерь `create_new_age:basic_solar_heating_plate` = 52 FE/t и `advanced_solar_heating_plate` = 165 FE/t; при сниженном свете, ночью и в непогоду выход обязан падать. Прогони один заправленный `create_new_age:reactor_rod` и запиши устойчивую топологию и выход ≤217 FE/t. Проверь `immersiveengineering:dynamo` в каждой задуманной среде и запиши `int(3 × принятое вращение)`.
32. **Энергия «всё зелёное» и Кобб.** При текущем множителе 30.0 замерь прогрев пластины и пятиминутное поведение basic + 6 Stirling; проверь один и два стержня с котлом и 18 нагрузочными двигателями и клапанами; проверь коротконагруженную катушку, четыре погодно-вращательных случая IE и слив `create_diesel_generators` на 1 000 мБ дизеля. Ждём: один стержень `nuclear_fuel` ACTIVE ≈**24 минуты**, а не 4 ч 48 мин. У Кобба при репутации 0 запиши, появляется ли заказ `cobb_wants_alternator`. Блокер: `energy_input_contract.py --require-master` должен быть зелёным, иначе числа workbook не считаются актуальными.
33. **Обработка металлов и снятые машины.** Убедись, что в EMI нет рецепта `createaddition:rolling/copper_plate`; что `vintage:rolling/netherite_rod` больше не даёт `vintage:netherite_wire`, а `vintage:coiling/netherite_rod` даёт `vintage:netherite_spring`; что Helve Hammer не апгрейдит незерит без шаблона. Живой рецепт `create_enchantment_industry:mechanical_grindstone` собирать **только после твоего выбора**, потом записать компоненты и стадию. Мёртвый `create_enchantment_industry:disenchanter` живым блоком не считать.
34. **Ороситель, взрывы и инструменты.** Пропусти воду через `sliceanddice:sprinkler`: полив работает без краша, слайсер, доска и комбайн остаются рабочими (капли XP-жидкости отсутствуют намеренно). Подорви один TNT рядом с игроком, затем бочку Create, порох и дробилку. Ждём: частицы есть, но **нет** тряски экрана, размытия, сердцебиения и приглушения звука, а логика машин цела.

### Проход 5 — вертикальный срез Factory Siege (45–90 мин после прохода 0, только в тестовой копии)

35. **Миграция неймспейсов контригры.** После загрузки джарника фабрики EMI обязан показывать пять Java-id: `factory_siege:seismograph`, `dampening_orb`, `noise_relay`, `pollution_duct`, `field_barricade`; старых `kubejs:`-тёзок быть не должно **после применения миграции**, а не только по бумажной спецификации. Скрафти по одному на своей стадии, ткни машину одним `factory_siege:dampening_orb` (**тратится ровно один раз**), собери раскладку реле и коробов и прочитай её `factory_siege:seismograph`. Ждём одобренное ослабление сигнатуры и **никакого удвоения внимания**.
36. **Сохранность контригры и безопасность скруббера.** Построй маленькую фабрику с `factory_siege:wet_scrubber`, `minecraft:chest` и блок-сущностью Create; сломай, подбери, перезапусти, перезайди — вода, заряд фильтра и весь NBT обязаны пережить. **`factory_siege:wet_scrubber` не должен разрушаться нигде вне размеченного коридора.**
37. **Размещение и идентичность.** `[считать]` На тестовой фабрике примени выбранную двухкликовую регистрацию и запиши id якоря и фабрики, позицию портала, поводок и зазор до периметра. Ждём: реальный `GatewayEntity` вставлен **снаружи** периметра, никакой запасной координаты или сущности не создано, а перенос, слом, перепривязка и перезапуск сохраняют внимание и долг. Блокер: твои ответы U060/U063.
38. **Ростер волны и поиск пути.** Запусти утверждённый профиль `factory_siege:wave_swarm` на настоящем движке Gateway. Ждём: `minecraft:zombie` ×4, `husk` ×3, `spider` ×3, `cave_spider` ×2 появляются в заданном темпе, идут по лентам Create в размеченный коридор, и **ни один невыбранный блок или сущность не становится целью**. Запиши позиции спавна, тайминг, стадию, таймаут и число сущностей.
39. **Фальсификатор политики разрушения.** В тестовой копии размечь один `factory_siege:pollution_duct` **внутри** коридора, а снаружи поставь `factory_siege:wet_scrubber`, блок-сущность Create, `minecraft:chest`, `minecraft:furnace` и ещё один короб. Запусти волну диверсии. Ждём: ломается **только** размеченный короб, все блок-сущности и наружные блоки целы, в отладочном логе есть `Factory Siege destruction denied … by OUTSIDE_CORRIDOR`. Повтори, подменив цель сундуком **прямо перед последним ударом** — сундук обязан уцелеть.
40. **Клеймы и параллельность.** Накрой коридор чужим клеймом FTB, поставь по одному атакующему на цель при `MAX_CONCURRENT_ATTACKERS_PER_BLOCK=1` и проверь закрытие защитником против самого быстрого и самого медленного настроенного времени слома. Ждём: клейм запрещает разрушение, один атакующий не стопорит рой, а защитник успевает закрыть, когда это физически возможно.
41. **Провал, ремонт и сохранность бюджета.** `[считать]` Заставь гейтвей провалиться выбранной тобой политикой провала, осмотри запись бреши и почини размеченный участок. Ждём: наказание **ровно одно**, участок остаётся открытым до ремонта, повторное `Failed` и перезапуск **не** списывают второй раз, и следующая волна не обнуляет потраченный бюджет. Точное количество ремонта и награда остаются нерешёнными под `OPEN-WAVE-COST-01`. ⚠️ **Никакая награда этим чеклистом не утверждается.**
42. **Персистентность состояния осады.** В тестовой копии после запуска волны сохрани `factory_siege_failures.dat`, перезапусти сервер и проверь восстановление живого состояния. Ждём: сохранённая осада не превращается в победу из-за рестарта, провал/наказание не списываются повторно, а восстановление Gateway и бреши явно отражается в логе.

43. **Завершение волны, награда и производительность.** Прогони полный цикл «сигнал → оборона → волна → уборка». Ждём: гейтвей завершается, родной дроп и опыт мобов ведут себя как обычно, ни события, ни леджера не остаётся, награда не дублируется. Через Spark запиши MSPT и число сущностей за одну волну и сравни с принятым пределом. ⚠️ **Стоимость из дропа не выводить.**

### Проход 6 — финальный лут и приёмка (20–35 мин)

44. **Граница декретов LootJS.** После разрешённого синка и рестарта прогони несколько раз `/loot give @s loot minecraft:chests/village/village_toolsmith`. Ждём: хотя бы один `bountiful:decree` с `bountiful:decree_data` и валидным id пула (`toolsmith`, `armorer` или `fletcher`). Затем `/loot give @s loot minecraft:entities/zombie` — декрета быть **не должно**. ⚠️ Один промах ничего не доказывает: **запиши число бросков**.
45. **Сундуки Hollowmarch и полка Цитадели.** Открывай **только новые** контейнеры жилых домов и водонапорной башни Hollowmarch и полку Цитадели. Ждём: валидный лут и отсутствие ошибки пропавшей таблицы, на полке `minecraft:book` как в строке 24. ⚠️ **Старый уже открытый сундук доказательством не является.**
46. **Дроп Xtra Arrows.** На сервере убей одну `minecraft:bat` и одного `minecraft:vex`. Ждём: ровно один `xtraarrows:bat_skin` и ровно два `xtraarrows:vex_wing`. Проверь, что обычный сундучный лут не изменился, и запиши любые предупреждения о registry-id.
47. **Еда, offcutting и достижимость наград.** Повтори проверку разделочной доски в левой руке из строки 27, затем осмотри и выполни исправленный кукурузный квест на `culturaldelights:corn_kernels`. Ждём: задача закрывается, зависимый квест на тортилью остаётся доступным, и никакого провала «предмет только блок» на `culturaldelights:corn`.

### Зависимости и время

Физический порядок: **проход 0** старт и вход → **1** свежие чанки → **2** деревня и квесты → **3**
один персонаж → **4** один стенд → **5** изолированная тестовая копия Factory Siege → **6** лут.
Строки 5–13 требуют чистого старта и новых чанков; 21–27 — стабильно подключённого клиента; 35–42 —
изолированной загрузки Factory Siege, размещения, миграции и защитных гейтов; 43–46 — финального
синка и рестарта.

⏱️ **Короткий маршрут — 3–4 часа**, если все предпосылки готовы. Полный, с провалившимися
предпосылками и незакрытыми твоими выборами по фабрике — вечер, **5–7 часов**. `[считать]` — это
оценка планирования, а не замеренное время.

---

# 📜 Прежний список — целиком, как он был

## Новые и уточнённые пункты после просмотра владельца

- ✅ **Первый серверный boot после девяти новых модов — ПРОЙДЕН 09.09, проверять не нужно.** Сервер стартовал 09.09 в 21:02:50 (`Done (3.734s)`), поднял **629 модов**, отработал ~39 минут и выключился чисто. Ни одного FATAL. Старая пометка «после чистого старта» на других пунктах снова измерима — их можно проверять. ⚠️ В логе того запуска **931 строка `/ERROR]`**, их никто не читал: запуск завершился — это не то же самое, что «всё здорово». Отдельная задача, не твоя проверка.

- [ ] **Компас после синка 06.09.** В поиске нет Stronghold, месторождений RNS и добавленных в запрет секретных/боссовых структур, включая восемь боссовых структур Twilight Forest; остальные разрешённые структуры доступны. Рецепт компаса сохранён.
- [ ] **Общий поиск реликвий.** В EMI запрос `#bountiful_wizards:artifacts_and_relics` показывает Artifacts, Relics и оба аддона; варианты чармов, мешочков семян и зелий группируются с доступом к содержимому.

- [ ] **ParCool после синка: приземление и бинды.** После синка проверь, что работают Roll и Tap при приземлении; отдельно проверь, отключен ли Dodge; и что Combat Roll остаётся на **R**, а приземление ParCool — на **Y**. Приземление и боевой перекат должны работать независимо.

- [ ] **Картограф: цена Castle Map.** Тип карты подтверждён на трёх картографах. При следующей покупке запиши только цену в изумрудах и дополнительный предмет.

- [ ] **Четыре напитка у торговцев.** У Hazel снова выдаются кофе, долгий и крепкий кофе, у Tilly — зелёный чай. Открыть предложения и EMI: предметы и количества отображаются, получение награды работает.
- [ ] **Hollowmarch: новые сундуки и бочки.** В ещё не открытых контейнерах жилых домов и водонапорной башни пустынного рынка появляется лут; в логе нет ошибки отсутствующей таблицы для этих контейнеров. Старый уже открытый пустой сундук не проверяет исправление.
- [ ] **Геологический справочник у Digby.** Купить книгу, проверить русские страницы и переходы между ними; бесплатно при первом входе она не выдаётся.
- [ ] **Реликвии без крафта.** После перезапуска EMI не показывает рецепты изготовления реликвий из отключённого набора; получение через разрешённый лут сохраняется.
## Незакрытые игровые проверки
## Оregen / More Gateways — свежий мир (evidence 2026-08-31)

- [ ] **More Gateways: живой output и timer.** Пройти один gateway установленного host-мода до конца и сверить wave/reward с JSON; намеренно превысить `max_wave_time` одной волны. Отдельно подтвердить, что `rats_*` и `alexsmobs_*` pearls не крафтятся при отсутствии этих host-модов, и записать любой Bountiful want на недоступный gateway item.
- [ ] **Corundum.** В новом overworld вне океана, y 0..64, увидеть Quark corundum cluster; в океане его не должно быть. Перед этим сверить live server `quark-common.toml`, не старый mirror.
- [ ] **Rock salt.** В новом мире проверить `salt:rock_salt_ore`/deepslate вариант в beach/ocean/dripstone caves y −5..110 и отсутствие в обычной inland stone; не смешивать с Galosphere pink salt или Vintage Delight patch salt.

- [ ] **После `STRUCTURIFY-SCHEMA-AND-DISTANCE-REDISTRIBUTION`.** После чистого server boot подтвердить в логе ровно ноль `Found invalid structure namespace entry` и `Found invalid structure set entry`; затем только в новых чанках найти/записать распределённые структуры и убедиться, что их дистанции соответствуют утверждённому списку.
- [ ] **После `RAIDSENHANCED-OMEN3-AND-SIEGE-ROUTING`.** Подтвердить загруженный native Required Raid Omen level 3: added raid mobs не появляются в раннем рейде и появляются только на фактическом позднем пороге; отдельно проверить один siege-wave с выбранным 1–2 mob без ложного сообщения об успешном спавне.

## Чеклист анимаций — принятое состояние 2026-08-30

- [ ] **FA+Player удалён после подтверждённой поломки позы — game test.** Все три известные копии
  `FA+Player-v1.1.zip` (`9EE3AE…937F33`) вынесены из master, клиента и client-backup в
  `BountifulArchive/removed_openloader_resources/FA+Player-v1.1/`: это был единственный активный
  player-CEM слой, ломавший руки на `ParCool` hang/ledge и лук (арбалет не затронут). `FreshAnimations`,
  `FreshCompats` и patched `Freshly Modded` остаются включёнными; Fresh Moves и Joyful остаются
  `.zip.disabled`. После перезапуска принять: hang держит край руками, лук имеет vanilla/Better
  Combat позу, арбалет не регрессирует, rope остаётся нормальной. Не возвращать FA+Player или
  Animation Overhaul ради этой проверки.

## LootJS L4 — проверить центральную границу декретов после sync/restart

- [ ] **Декреты в сундуках: runtime-falsifier для `OPEN.md` §1.248 L4.** После разрешённого master sync/restart центральная шестистадийная граница должна оставить четыре возвращённых decree-пула. В мире с запущенным сервером несколько раз выполни `/loot give @s loot minecraft:chests/village/village_toolsmith` (5% на ролл; один промах ничего не доказывает). Подтверждение: хотя бы один `bountiful:decree` с NBT `bountiful:decree_data` и id `toolsmith`, `armorer` или `fletcher`. Затем `/loot give @s loot minecraft:entities/zombie`: декретов не должно быть. Декрет без этого NBT — отдельная утечка стока; отсутствие после достаточной серии — сигнал, что порядок/селектор изменился. Основание: `agents/loot/2026_08_31_SOL_LOOTJS_REGISTRATION_CENTRALIZATION_AND_GUARD.md`.


## Закрытый блиц 2026-08-28 — проверки после применения

- [ ] **Квесты: оставшиеся снятые цели.** Dead King уже принят. Шесть бывших
  Alex's Mobs / Wabi Sabi / Incendium целей должны отсутствовать без автоматической подмены;
  владелец затем вручную наполнит и улучшит эти главы.
- [ ] **Galosphere Silver/Palladium — P0 после `003afb1`.** После чистого запуска в EMI открой
  `galosphere:palladium_lattice` и `galosphere:palladium_bomb`: оба рецепта принимают только прямой
  `immersiveengineering:ingot_silver`; 13 material-рецептов и Vintage palladium routes не видны.
  В новом чанке не должно появиться `galosphere:palladium_ore`/deepslate-варианта (два source
  biome-modifier и AdLods выключены, Block Swap остаётся лишь страховкой старых чанков). Старый чанк
  проверить отдельно и записать находку: Block Swap retro-path не гарантирует очистку уже сохранённой
  руды. Не засчитывать, пока `galosphere_spellbooks` не перестанет логировать dead `silver_*` recipes.
- [ ] **Магия.** До `can_magic` обе учебные станции Unraveling отказывают; после выдачи стадии
  обучение и каст выученного заклинания работают. Проверь потолок четырёх школ.
- [ ] **Create RNS / Wheat Book.** После clean boot обе стороны должны показывать `create_rns` 1.3.1,
  `infiniteDeposits=false` и не генерировать wolframite/uranium/osmium. Проверь, что `wheat_book`
  больше не гарантирован в аванпостах и обычных домах, а новая цепочка крафта работает только с
  утверждёнными ингредиентами.
- [ ] **Create Henry без free-SU bypass.** После полного перезапуска сервера и клиента в JEI и EMI
  нет `create_henry:kinetic_motor`, а его crafting-рецепт не находится. Затем собери и запусти
  сохранённый `create_henry:furnace_engine`: пока соседняя печь реально горит, маховик передаёт
  вращение; после окончания топлива останавливается. Не засчитывать только по hide/рецепту — это
  одновременно проверка, что узкий removal не сломал оставленную механику.
- [ ] **ParCool 3.4.3.3 без наших перков.** После чистого старта меню «Пути» не появляется,
  старое «Чутьё инженера» без гогглов не работает. Проверь обычные действия по текущему ParCool Guide;
  отдельный Combat Roll принимается своим пунктом выше. Старые GameStage-флаги сохранены,
  их наличие не означает, что выключенные мосты перков исполняются.
- [ ] **Дневной Thrasher.** После синка и перезапуска побудь днём в подходящем океане: Thrasher всё
  ещё может появиться, но дневная проверка теперь проходит с шансом 0.5 вместо 0.75. Ночные спавны,
  вес 10 и группа 1–2 не должны измениться.


## Снежный принтер — проверить только после синка master (2026-08-29)

Снятие рецепта уже стоит в master и обеих живых копиях. Проверять после чистого старта;
игровой результат остаётся открытым.

- [ ] После чистого старта: Henry `freezing` не должен превращать **слой снега**
      `minecraft:snow` в снежный блок. Это снятый recipe id `create_henry:freezing/snow`;
      старое «6 снежков → 6 блоков» называло другой вход.
- [ ] Отдельно убедиться, что строки Marlow и Ashby не изменились.

## Серебро C&C — приёмка уже выбранного IE-канона

Решение принято: старые формы C&C конвертируются в IE, рецепты C&C не возвращаем.

- [ ] Возьми 9 `caverns_and_chasms:raw_silver`. Убедись, что прямого крафта в блок **нет**.
- [ ] Проверь, что каждый конвертируется 1→1 в `immersiveengineering:raw_silver`, и что из девяти
      получившихся блок собирается.
- [ ] Запиши удобство конверсии; повторный выбор между B и C не требуется.

## Чистый вход на сервер — smoke текущего набора модов

- [ ] **Зайти на сервер и остаться в игре пару минут.** Обычный старт, вход, походить, выйти;
  отметить отказ соединения или ошибки. Историческое «никто не входил» уже неверно:
  серверный лог содержит вход 29.08 в 23:57. Но с тех пор менялся набор, поэтому новый smoke нужен.
  Не считать один вход автоматическим выходом «26 из 50» модов из карантина: список и остальные
  условия агент сверяет по текущим данным. Отдельно остаётся строка TooltipJS ниже.

- [ ] **IMM = Integrated Mowzie's Mobs, не Immersive Engineering — одна проверка после первого чистого старта сервера (identity исправлен 2026-09-07).**
  В серверном `logs/latest.log` в списке модов есть строка `integrated_mowzies_mobs 1.1.0` (jar `IMM v1.1.0-1.20.1.jar`), а `immersiveengineering 1.20.1-10.2.0-183`
  стоит отдельной строкой, как и раньше; ни одной ошибки datapack/worldgen с `integrated_api` или `mowziesmobs`. Только в **новом** мире: `/locate structure mowziesmobs:wrought_chamber`,
  `/locate structure mowziesmobs:monastery`, `/locate structure mowziesmobs:umvuthana_grove`, `/locate structure mowziesmobs:frostmaw_spawn` должны находить структуры (их тип теперь
  `integrated_api:*`, а не `mowziesmobs:*`). Записать только «загрузился / нашёл / не нашёл»: лут, спавн боссов и цены не оценивать — семь проверок карантина и четыре условия владельца открыты
  (`technicalities/КАРАНТИН.md` §«Новая партия 2026-09-06», `agents/mods/2026_09_06_IMM_QUARANTINE_REVIEW.md` §10).


## Два готовых пакета выложены 27.08 — одна проверка в игре

- [ ] 🔴 **Взять у Гидеона или Векса заказ на зачарованную книгу и сдать его.** До синка 27.08 такой
  заказ нельзя было выполнить **вообще**: пул писал `lvl:3` как IntTag, а настоящая книга хранит
  уровень как ShortTag, и `NbtUtils.compareNbt` отбраковывает их по классу тега ещё до сравнения
  значений. В текущих wants Гидеона и Векса уровни записаны как ShortTag, копии совпадают.
  **Файл доказывает формат, а сдачу заказа доказывает только игра.**
  Доски: `bountiful:decree` Гидеона и Векса. Книга — любая обычная `minecraft:enchanted_book`
  нужного уровня, из тех, что просит доска.
- [ ] **Полка Цитадели платит.** Проверить фактическую выдачу в Citadel.
  Loot-table id — `irons_spellbooks:chests/citadel/citadel_tomes`; подробная проверка именованной
  книги ниже сохраняется отдельно. Старое «остальные десять структур отсутствуют» здесь не требуется.

## Xtra Arrows 3 — после server rollout 2026-08-26

- [ ] На SERVER убить bat: проверить ровно одну `xtraarrows:bat_skin`; затем vex: ровно две
  `xtraarrows:vex_wing`. Ничего не менять по результату: owner уже выбрал сохранить оба GLM.
- [ ] Убедиться, что обычный chest loot не изменился: мод не содержит обычных `loot_tables`, только
  два GLM выше. Отдельно отметить, если registry-ID warnings для новых стрел проявятся в игре.

**Статичный счётчик не используется:** перед игровым заходом текущий объём открытых строк получается из самого файла командой `Select-String -Path claudework/ИГРОВОЙ_ЧЕКЛИСТ.md -Pattern '^\s*- \[ \]' | Measure-Object | Select-Object -ExpandProperty Count`.

Приоритеты: (1) после применения уже выбранной миграции `OPEN.md` §1.283 при остановленной игре проверить исправленные FTB-Quest ссылки Construction Wand и boot/join; (2) ореген проверять после полного перезапуска только в неразведанных чанках; (3) проверить снежную петлю Henry после sync, потому что она удваивала торгуемый ресурс.

## BOMD — единственный незакрытый пункт из маршрута структур

- [ ] **Obsidilith Arena.** Только после server parity, выхода BOMD из карантина и закрытия условий
  `BOMD-KEEP-CONDITIONS`: найти арену, пройти реальный бой и проверить нагрузку.
  Полёт оставлен владельцем как есть (§1.273-B); его наличие не провал этого теста. Двенадцать остальных посещений и решения IDAS приняты и архивированы в
  `agents/orca/G91_CHK_DELIVERY.md`.

## VMinus — одна проверка, которая экономит будущий потерянный день (2026-08-27)

- [ ] 🔴 **Проверить, работают ли `Absorption` и `Health Boost`.** У мода `vminus` дефолтный
  `effects/banned.json` перечисляет ровно эти два эффекта, и живой ли этот запрет — не подтверждено.
  Проверить **золотым яблоком** или срабатыванием Тотема Бессмертия: появляются ли жёлтые сердца.
  Зелье черепахи для Absorption не подходит. **Health Boost проверить отдельно** на тестовом персонаже
  командой `/effect give @s minecraft:health_boost 30 0`: увеличился ли максимум сердец;
  это диагностический тест эффекта, не изменение баланса.
  **Зачем это важно:** если запрет живой, тотем работает не так, как ты думаешь, — а `vminus` при
  этом мод с нулём контента, на который никто никогда не подумает. Полный разбор:
  `claudework/agents/mods/2026_08_27_QUARANTINE_BACKFILL_LIGHT_FIVE.md`, мод 1/5.

## Три проверки, которые ждут своей работы (из реестра решений, 2026-08-28)

Каждая проверка ниже начинается после своей работы в TODO и чистого старта с выложенным результатом.
До этого ожидаемого исправленного поведения ещё нет.

- [ ] **После `EXPERIENCE-A-PRIME-APPLY` (`OPEN.md` §1.279).** В EMI убедиться, что
  `create_enchantment_industry:experience` существует; скрафтить `irons_spellbooks:blank_rune` на
  1 000 mB; и проверить, что маршрут Тотема Бессмертия состоит из **десяти** шагов по 1 000 mB, а не
  из одной невыполнимой операции спаута на 10 000 mB. Плохо: рецепт виден в EMI, но станок вечно
  висит в HOLD без строки в логе — это и есть симптом операции больше бака.
- [ ] **После `CONSTRUCTIONWAND-FTBQ-MIGRATION` и `EB-CONSTRUCTION-MIGRATION` (§1.263 / §1.197).**
  Зайти на сервер и открыть затронутые квесты: ни одного `ftbquests:missing_item`. Затем проверить
  режимы Effortless Building на защищённой земле: нет обхода клейма, нет дюпа, нет мгновенной
  большой постройки.
- [ ] **После деплоя моста Biolith (§1.280).** Смотреть **только новые** чанки: записать id биома,
  координаты и скриншоты. Три пещерных биома Galosphere должны встречаться с заданной разрежённой
  частотой, и старые чанки при этом не меняются задним числом.

## AnimLib — последний мод карантинного бэкфилла (2026-08-28)

- [ ] 🟢 **Открыть экран анимаций и просто зайти на сервер.** `animlib` — библиотека анимаций,
  единственная из последней тройки, оставленная `keep with conditions`. Что сделать: открыть её экран на
  клиенте (убедиться, что он рисуется и не роняет игру) и один раз зайти на сервер после чистого старта.
  Плохо: экран не открывается, или сервер не стартует / клиент не коннектится.
  Разбор: `claudework/agents/mods/2026_08_28_QUARANTINE_BACKFILL_BATCH_J.md`, мод 1/3.

## Осада: `!siege raidnow` должен призвать настоящую орду (после выкладки, 2026-08-28)

- [ ] **Форс-рейд действительно запускается.** На тестовом стенде с правами OP сначала
  `!siege status`; если выключена — `!siege on`, затем `!siege raidnow`.
  Нормально: пришла орда. «Система ВЫКЛ» и отказ не-оператору — штатные ограничения.
  Плохо: при включённой системе чат объявляет рейд, а мобов нет, либо пишет
  «форс-рейд НЕ ЗАПУЩЕН». Если включал только ради теста, верни `!siege off`.

## TooltipJS — просто зайди на сервер (2026-08-28)

- [ ] 🔴 **Проверить, что клиент вообще подключается.** `tooltipjs` стоит у тебя на клиенте, на сервере
  его **нет**, а его метаданные оставляют `MATCH_VERSION` по умолчанию — Forge при таком сочетании
  вправе отказать в коннекте. Что сделать: обычный вход на сервер. Хорошо: вошёл, тултипы рисуются.
  Плохо: отказ при подключении с жалобой на несовпадение модов — тогда это выбор «снять с клиента» или
  «выложить на сервер», и он решается вместе с общим выравниванием версий.
  Разбор: `claudework/agents/mods/2026_08_28_QUARANTINE_BACKFILL_BATCH_I.md`, мод 5/5.

## Quest Kill Task — одна награда на команду, а не на каждого (2026-08-28)

- [ ] 🟠 **Проверить вдвоём, сколько наград даёт kill-задача.** `questkilltask` добавляет квестовую
  задачу «убить N существ». Что сделать: взять один квест с такой задачей, набить требуемое количество
  **вдвоём** и посмотреть, сколько наград выдалось и кому. Плохо: награду получает каждый участник —
  тогда это множитель на кооперативе, и его надо гасить до того, как задача попадёт в квестбук.
  Разбор: `claudework/agents/mods/2026_08_28_QUARANTINE_BACKFILL_BATCH_H.md`, мод 5/5.

## Две проверки из карантинного батча G (2026-08-28)

- [ ] 🟠 **Поезда не разъезжаются между клиентом и сервером.** `createthreadedtrains` считает
  железнодорожную сеть Create в отдельном потоке. Что сделать после обычного старта сервера: пустить
  **несколько** поездов одновременно по пересекающимся маршрутам со стрелками и станциями, покататься
  на одном из них и посмотреть со стороны на другой. Плохо: поезд у тебя и поезд в мире стоят в разных
  местах, состав застревает на стрелке, в логе появляются исключения из потока железной дороги.
  Разбор: `claudework/agents/mods/2026_08_28_QUARANTINE_BACKFILL_BATCH_G.md`, мод 1/5.

- [ ] 🟠 **Разделка второй рукой не дюпит.** `offcutting` разрешает класть ингредиент на разделочную
  доску Farmer's Delight второй рукой — это новый путь к уже существующему рецепту. Что сделать:
  разделать что-нибудь второй рукой и убедиться, что выход ровно один комплект; посмотреть, что
  инструмент израсходовал прочность как обычно; нажать ещё раз по пустой доске и убедиться, что ничего
  не выпало. Плохо: два комплекта выхода, или пустая доска продолжает платить.
  Разбор: тот же отчёт, мод 5/5.

## Unlock Schematics — проверка прав, до выхода мода из карантина (2026-08-28)

- [ ] 🟠 **Команда разблокировки схематик не должна быть доступна кому попало.** Мод
  `unlockschematics` добавляет команду, которая снимает блокировку со схематики. Что сделать: от лица
  оператора выполнить её и убедиться, что она работает; затем от лица обычного игрока (или с
  понижёнными правами) — убедиться, что приходит отказ, а не выполнение; и отдельно проверить, что
  чужую схематику нельзя разблокировать в обход владения.
  Плохо: обычный игрок выполняет команду, или чужая схематика открывается.
  Разбор: `claudework/agents/mods/2026_08_28_QUARANTINE_BACKFILL_BATCH_F.md`, мод 5/5.

## Liteminer — до того, как считать цену добычи (2026-08-28)

- [ ] 🟠 **Liteminer: дроп и расход.** Киркой без Fortune/Silk Touch добудь одинаковый
  вид руды обычным способом и Liteminer; сравни дроп на одинаковое число блоков.
  Запиши число блоков/предметов, время и расход голода, затем повтори другим тиром кирки.
  Лимит сейчас 64 блока, exhaustion включён (0.2). Дюп — выход выше контрольного маршрута,
  а не просто больше одного предмета с блока: многопредметный дроп бывает штатным.

## FTB EZ Crystals — одна проверка до того, как аметист будет считаться в ценах (2026-08-28)

- [ ] 🟠 **Проверить, что сбор кристаллов правым кликом не даёт дюп.** Мод `ftbezcrystals` собирает
  правым кликом 15 типов полных кристаллов и сбрасывает блок до small bud. Это единственный мод из
  карантинного батча D, который трогает добычу **торгуемого** `minecraft:amethyst_shard`.
  Что сделать: найти аметистовую жеоду, взять обычный дроп с одного кластера киркой и запомнить
  количество; на соседнем полном кластере нажать правой кнопкой и сравнить дроп; затем нажать правой
  кнопкой ещё раз по уже сброшенному до small bud блоку и убедиться, что второй клик не даёт ничего.
  Плохо: второй клик снова платит, или правый клик даёт больше, чем кирка.
  Разбор: `claudework/agents/mods/2026_08_28_QUARANTINE_BACKFILL_BATCH_D.md`, мод 4/5.

## Раскладка клавиш — профиль B выбран, применение ещё проверить

В текущем клиентском `options.txt` Keywheel всё ещё V, список игроков и Tool Belt оба Tab,
OreExcavation R. Сначала агент применяет принятый профиль точечно, затем проверка ниже.
`/defaultoptions saveKeys` — только после приёмки Mouse 4; личные бинды целиком не сбрасывать.

- [ ] **Visual Keybinder и конфликты.** Открой его кнопкой из Controls; проверь Tab, R, V, C и Alt.
  Не нажимай `unbind all` и `reset all`.
- [ ] **Принятый профиль B — после применения.** Keywheel Mouse 4, список игроков F9,
  Tool Belt Tab; проверить OreExcavation Ctrl+R и Mech Trowel Shift+R. Записать, где модификатор
  не отличается от голой клавиши. После перезапуска назначения должны сохраниться.
- [ ] **Ping-Wheel вдвоём.** Mouse 5: второй игрок видит метку. Этот бинд уже записан;
  применения профиля B для теста не требуется.
- [ ] **Расширение Keywheel — позднее.** По одной странице, сохраняя прямые кнопки,
  пока каждое действие страницы не заработало. Новый профиль выбирать сегодня не нужно.

## Freshly Modded — Takes a Pillage Archer (после закрытия клиента и точечного CEM-фикса)

- [ ] **Бинды пережили перезапуск.** Tool Belt открывается на `Tab`; Create Toolbelt — `Ctrl+E`.
  Если конкретный новый мод снова добавит конфликт, менять только его ключ через Visual Keybinder,
  не переснимать весь baseline с уже испорченного `options.txt`.

- [ ] **Archer снова натягивает лук.** Владелец увидел, что `takesapillage:archer` машет руками вместо
  позы стрельбы; остальные мобы мода нормальны. Уже отключена лишь его CEM-подмена из Freshly Modded,
  без удаления спавна и без снятия текстуры. После перезапуска заспавнить Archer с луком и дождаться
  атаки: нормально — обе руки держат/натягивают лук; плохо — прежняя мельница руками, T-pose либо
  пропавшая текстура.

- [ ] **Матрица player-анимаций от третьего лица.** Not Enough Animations и EMF-совместимость
  всегда остаются включёнными; FA+Player полностью вынесен в архив и не является вариантом этой
  матрицы (Archer CEM по-прежнему выключен). На текущем наборе проверить F5-лук и удар мечом:
  натяжение, руки и траекторию меча должны быть нормальны без FA+Player; отдельные эксперименты с
  Fresh Moves или Freshly Modded player CEM не возвращают удалённый пакет автоматически.

## До входа на сервер

- [ ] **ParCool: HUD и хват за край на текущей 3.4.3.3.** Открыть ParCool Guide и повторить
  действие Cling to Cliff по его текущей инструкции. Нормально — виден stamina HUD и персонаж
  цепляется за край; плохо — HUD нет или хват не срабатывает. Старый шаг «Shift → skill tree →
  купить Hang On за 1 point» относился к 4.x и в этом наборе не является условием теста.

- [ ] 🟢 **Разбрызгиватель (Sprinkler) снова МОЖНО ставить — запрет снят 2026-08-26. Проверь его один раз.**
  Джарник Slice and Dice пропатчен точечно (вариант D): единственное тело CEI-предиката заменено на
  `iconst_0; ireturn`, поэтому переименованные в CEI 2.5.2 классы больше не читаются никогда.
  Выложено на все четыре копии, все четыре имеют один хеш `82176e3a…`; сервер поднят и проверен
  (`Done (6.332s)`, RCON 0/20, ни одного `NoClassDefFoundError`).
  **Что проверить в игре:** поставь Разбрызгиватель и прогони через него обычную воду — он должен
  увлажнять/поливать как раньше, без вылета. Проверь заодно, что слайсер, доска и харвестер целы.
  ⚠️ **Что мы сознательно потеряли:** «залить в Разбрызгиватель жидкий опыт → падают XP-орбы».
  Это единственная функция, которую снял патч; ни один рецепт и ни один квест пака её не использовал.
  При обновлении Slice and Dice сначала попроси агента сверить патч: одинаковое имя jar
  не доказывает одинаковые байты. Текущий клиентский SHA-256 проверен в отчёте G91.


- [ ] ⚠️ **После миграции FTB-Quest проверь вход клиента на сервер.** Construction Wand уже не
  активен ни на одной стороне; на обеих стоят `effortlessbuilding-1.20.1-3.11.jar` и
  `buildinggadgets2-1.0.8.jar`. До остановки игры остаются три stale квестовые ссылки и одна
  карточка/описание; решение и порядок — `OPEN.md` §1.263.

- [ ] **Construction Wand: квестовый хвост.** Jar уже снят с активного сервера и сохранён в
  `_removed_constructionwand_2026-08-26`; на обеих сторонах остались только Effortless Building и
  Building Gadgets. После остановки игры заменить 3 id-ссылки и карточку/описание в FTB Quests,
  затем проверить boot и подключение. Не редактировать SNBT при живом сервере.

- [ ] **Обычный опыт и пустая руна — только после `EXPERIENCE-A-PRIME-APPLY`.**
  После выкладки открыть в EMI жидкость `create_enchantment_industry:experience`, реально наполнить
  бассейн и изготовить `irons_spellbooks:blank_rune`: расход должен стать **1000 mB**, не 100.
  Отдельно сверить новые цены Alistair/Corvin с принятой после пересборки таблицей.
  Сейчас в master ещё стоят обращения к удалённому `hyper_experience`; этот проход их не меняет.
  Проверка десяти шагов тотема остаётся отдельным пунктом выше; прежняя развилка A/A′/D — в OPEN,
  повторного решения из игрового чеклиста не требуется.

## Новый мир / дальняя разведка — ореген

**Стек.** Активен `AdLods-1.20.1-8.1.12.0-build.2294`; `geolosys`, `creategeoresonance` и
`createoreexcavation` отсутствуют на обеих сторонах. При плохом результате запиши координаты,
измерение, высоту и приложи скрин.

- [ ] **Золото и Twilight.** В новом Overworld на `y≈−20` найди золото. Затем в Twilight Forest
  найди обычную руду: `OPEN.md` §1.240 открыт потому, что AdLods снимает 29 ванильных фич, семь из
  них Twilight, а генерирует только в Overworld. Отсутствие золота — дефект; пустой Twilight —
  результат для решения, не «нормально».
- [ ] **Железо и крупные жилы.** В новых чанках записать высоту, объём и координаты находок.
  Текущий AdLods-конфиг россыпи железа задаёт 29 попыток/чанк и диапазон −64..384;
  это параметры генератора, не обещание 29 найденных жил. Редкость крупных месторождений:
  железо 40, медь 8, уголь 30 чанков. Один пустой маршрут не доказывает поломку.
- [ ] **Алмазная малая жила — применена после синка и рестарта; смотреть только новые чанки.** Lode теперь
  Наблюдение 06.09: алмазы у открытой пещеры; ещё нужны Y самих блоков, block id и возраст чанка.
  Конфиги master/клиента совпадают: старт Y −64…16, выход к воздуху разрешён, строгая проверка
  высоты каждого блока выключена. Видимое небо само по себе не доказывает ошибку высоты. Lode:
  8–11 блоков: `70 % diamond_ore`, `30 % diamond_block`, раз в ~6 чанков. Типично это ≈27–37
  алмазов без Fortune; абсолютная случайная граница — 8–99. Нормально: несколько storage-блоков
  среди малой жилы; плохо: прежняя «стена»
  из сотен ore, отсутствие storage-блоков или жила крупнее 11 природных блоков. Запиши биом (`F3`),
  координаты и скрин. Старые чанки не меняются.
- [ ] **Изумруд вне гор — только новые Overworld-чанки.** В нескольких обычных и горных биомах
  запиши biome id, координаты/регион и фактическое число `minecraft:emerald_ore` по одинаковому
  объёму. Конфиг AdLods сейчас `generation=custom`, `Veins=132`, пустые biome white/black lists, а
  байткод пропускает при пустом фильтре любой биом измерения; один найденный блок подтверждает
  глобальность, но одна неудачная прогулка её не опровергает.
- [ ] **Nether-бюджет Q5-1 — только после выполнения `OREGEN-NETHER-CUSTOM-BUDGET-Q5-1`, синка и
  рестарта.** В новых Nether-чанках раздельно посчитать `ancient_debris`, `nether_gold_ore` и
  `nether_quartz_ore` по биому/высоте; приложить координаты и лог загрузки. Плохо: старая стена
  AdLods в 250–500 ancient debris, исчезновение ванильной россыпи без замены или config correction
  в логе; старые чанки не являются приёмкой.
- [ ] **Индикаторы и финдер.** Над жилой посмотри реальные наземные индикаторы, затем подойди с
  `metal finder`: красные лампы должны отличать жилу от россыпи. В §1.244 уже 133 из 216 мёртвых
  ссылок индикаторов, поэтому назови, что именно выросло/не выросло.
- [ ] **Редкости и AE2.** Аметистовые жеоды — кандидат раз в 72 (было раз в 18), спиннел примерно
  вчетверо реже; природного `ae2:certus_quartz_ore` быть не должно, цертус только из метеорита.
- [ ] **Кирки.** Проверить массовую добычу OreExcavation: работает ли ожидаемое действие.
  Liteminer тоже установлен и имеет отдельный тест выше; прежнее «единственный инструмент» неверно.
  Отметить конфликты или срабатывание не того механизма.
- [ ] **Dungeons Now Loading: Temple of Duality.** `dungeonnowloading:fairkeeper_spawner` не является
  обычным моб-спавнером: пройди штатный redstone-триггер камеры (кнопка/плита/головоломка) и подожди
  2–6 секунд. Нормально: блок загорается (`fairkeeper_alert=true`) и выпускает 1–4 мобов; после волны
  он одноразово исчезает. Не ломай и не переставляй его. Если после триггера мобов нет, запиши
  координаты и измерение, выполни `/data get block X Y Z` и приложи скрин проводки/состояния.

## Один творческий стенд — производство и транспорт

- [ ] **Снежная петля Create: Henry.** Проверить прежний маршрут 3 снежных блока → 6 слоёв →
  Henry freezing → блоки и отдельно оставшийся путь снежок → слой. Хорошо: удваивающего рецепта
  слоя в блок нет; плохо: петля жива. Цены брать из текущих Marlow/Ashby, старые числа не являются приёмкой.
- [ ] **Direct Chute.** Точный тест (G82 H16): сундук сверху → chute → сундук снизу, больше 16 предметов; за 20 тиков должно прийти больше 16. Потом повторить точную ориентацию воронки Create и подтвердить приём ниже по цепи. Доказательство: `claudework/agents/knowledge/technology/INTEGRATION_MAP.md`, раздел Direct Chute — в игре ещё не проверено. `direct_chute:direct_chute` стоит на обеих сторонах. Над сундуком и отдельно
  над воронкой Create подай одинаковую стопку. Хорошо: перенос в ожидаемом направлении; плохо:
  воспроизводится «иногда не работает».
- [ ] **Effortless Building.** `effortlessbuilding-1.20.1-3.11.jar` активен на обеих сторонах.
  В survival проверь ранние режимы стройки на чужой/защищённой территории: хорошо — честная
  строительная помощь без обхода claims, дюпа или мгновенной большой постройки; плохо — любой из
  этих abuse. Это runtime-гейт `OPEN.md` §1.236.
- [ ] **Pneumatic Express: предметы.** Активен `create_pneumatic_express-1.0.1-forge-1.20.1`.
  Насос → Pipe → приёмник должен провести 16 `minecraft:copper_ingot` и пережить выгрузку чанка.
  Жидкость не считается: предметы не идут — дефект capability-слоя.
- [ ] **Pneumatic Express: гогглы.** На той же трубе нужны режим (давление/вакуум), направление,
  скорость и число предметов. Пустой или ложный overlay — дефект.
- [ ] **ISS Ponder.** `iss_ponder-forge1.20.1-1.0.3-all` есть на обеих сторонах. В EMI открой W у свитка
  Guiding Bolt (spell id `irons_spellbooks:guiding_bolt`, не отдельный item id): нужен cast по манекену, не `gui.iss_ponder.cast_failed`/базовая
  сцена Create. Неизученный spell обычным кастом обязан отказать без эффекта.
- [ ] **После уточнения стенда агентом: остальные исправления машин и дюпов.** Старая строка
  «два дюпа / crate / пять экскаваторов» не задаёт точных объектов и рецептов. Сегодня её не
  засчитывать; агент должен назвать каждый recipe/item id и ожидаемый вход/выход.
  Впечатанный посох и блокировка мечом уже проверяются отдельными пунктами.
- [ ] **Прокатка меди.** После чистого старта проверь recipe id
  `createaddition:rolling/copper_plate`: скрипт его явно снимает.
  Отсутствие именно этой карточки — ожидаемо. Если из медной пластины всё же получается
  провод, запиши полный recipe id, вход и выход; такой маршрут требует разбора.
- [ ] **Взрывы — спокойный экран.** Устрой один TNT-взрыв рядом с персонажем: сам взрыв и частицы
  остаются, но экран не трясётся, не темнеет/не вспыхивает и не получает blur, heartbeat, sway,
  ringing или muffled sound. Затем проверь Create-механизм, бочку с порохом и крашер: Shrapnel
  остаётся источником осколков/обломков, но машинная логика не должна ломаться.

> Regulator не в активном jar: это прототип до решения о цене/фазе (`OPEN.md` §1.229). Не искать его
> в EMI и не считать отсутствие поломкой.

## Магия, перки и прогрессия — один персонаж

- [ ] **Полка томов Citadel — только после синка master KubeJS и restart/reload сервера.**
  Master override заменяет неподдерживаемый `minecraft:set_written_book_pages` на один именованный
  `minecraft:book`; до похода сначала в свежем `latest.log` должны отсутствовать и
  `Couldn't parse element loot_tables:irons_spellbooks:chests/citadel/citadel_tomes`, и
  `Unknown type 'minecraft:set_written_book_pages'`. Затем открой полку Citadel: обычные пулы
  остаются, а первый пул без условия делает один ролл именованной обычной книги, не written tome.
  Иначе приложи скрин и
  строки лога. Основание: `TODO.md` `LOOT-CITADEL-TOMES-FUNCTION`.

- [ ] **Активные аддоны Iron's.** На обеих сторонах есть `irons_spellbooks_tweaks-1.5.0`,
  `twilight_spellbooks-1.2.0`, `farmers_spell`, `CasterCuriosBonus`, `mobbility` и аддон. Вкладки
  не должны быть пустыми/дублированными; на враге должно быть видно, что Mobbility колдует.
- [ ] **Unraveling Spells.** Невладеющий персонаж пробует `irons_spellbooks:graybeard_staff`:
  хороший исход — отказ, плохой — обход `irons_restrictions`. Затем magic lectern крафтится,
  принимает свиток и скрывает неизученное; отметь свитки из 29 его лут-таблиц как возможный обход.
- [ ] **Три заклинания на одинаковой цели.** Для spell ids `irons_spellbooks:fang_strike`,
  `irons_spellbooks:ice_spikes`, `irons_spellbooks:arrow_volley` запиши редкость, уровень,
  снаряжение, урон за полный каст и реальный cooldown. Старые DPS не использовать как
  порог брака без привязки к уровню и боевым атрибутам.
- [ ] **Редкость на выделенном сервере.** Заработай ступень выше старта и попробуй впечатать spell
  этой ступени. Отказ из-за чтения только `startingRarity` — runtime-дефект `OPEN.md` §1.181.
- [ ] **B1 — тайник.** В Overworld: `!events status`, затем `!events test cache`.
  После успешного слуха подойти к метке на ≤96 блоков и подождать две секунды:
  должны появиться костёр и сундук Lootr с лутом. Три строки слуха плюс строка метки штатны.
  Отказ из-за двух живых точек или неудачного безопасного размещения не считать багом.
- [ ] **B2 — звезда.** `!events test star`, затем так же дойти до метки:
  ограниченный след 3×3, обсидиан, костёр и сундук с лутом. Пустое место после прибытия — находка.
- [ ] **A3 — тихая ночь.** Дождись вечера или ночи, затем `!events test quiet`: проверить отсутствие
  новых естественных hostile-спавнов и их возврат после следующего обычного рассвета.
  Не делай `/time set 13000`: команда сбрасывает счётчик дней и искажает проверку очистки фазы.
  Уже живые мобы, спавнеры и принудительная орда — отдельные источники. После следующего
  обычного рестарта фаза тоже должна очиститься. События включены, конфиг менять не надо.
- [ ] **Разведка IE.** `immersiveengineering:coresample` сообщает место/находку; в `fieldguide` →
  геология 17 записей. Плохо: пустая категория или в EMI остались `kubejs:prospector_drill`,
  `core_sample`, `sample_cartridge`.

- [ ] **Otherside: алмазная motherlode и предупреждение.** В новых чанках биомов
  `deeperdarker:echoing_forest`, `deeperdarker:deeplands` или `deeperdarker:blooming_caverns`
  найти сгруппированную алмазную руду в sculk stone/gloomslate. Записать координаты, объём
  находки и соседние sensor/shrieker; ожидается shrieker с `can_summon=true`.
  Одна неудачная прогулка не доказывает отсутствие редкой жилы. Это отдельный тест от
  маленьких алмазных жил Overworld и исчерпаемых месторождений RNS.

- [ ] **Sunpiercer — потолок P=12 (ruling владельца 2026-09-06, строка манифеста 2026-09-07).**
  Взять `marrelics:sunpiercer` (creative) и качать способность `slayer`: в UI реликвии максимум
  способности должен быть 12, не 20; уровень самой реликвии тоже останавливается на 12
  (`levelingData.maxLevel`); на 12-м уровне бонус урона +395 (35 + 30×12), дальше не растёт.
  Если максимум 20 или урон растёт после 12 — `config/marrelics/relics/sunpiercer.yaml` на этой
  стороне не подхвачен: сравни хэш с master и перезапусти. Основание: commit `22e2db4`,
  `agents/magic/G97_SONNET_RELICS_DELIVERY.md` §4.

## Жители и новый контент

- [ ] **Торговцы.** В деревне осмотри horn merchant, visitor и караваны:
  что продают, когда доступны, не заменяют ли Bountiful. Плохо: ранний бесплатный маршрут/дубль доски.
  Silkroute trader из снятого Arkane Domains искать не нужно.
- [ ] **Заказы на зачарованные книги — после синка копий.** Возьми у **Гидеона** или **Векса**
  любой заказ на зачарованную книгу и сдай её. Раньше это было **невозможно**: уровень в заказе
  записан как `lvl:3` (IntTag), а настоящая книга носит `lvl:3s` (ShortTag), и ванильное сравнение
  Исправленные файлы Гидеона/Векса уже одинаковы в master и обеих живых копиях;
  отдельная проверка сдачи после чистого старта всё ещё нужна.
  Плохо: доска принимает книгу, а заказ не закрывается. Хорошо: закрывается сразу.
  ⚠️ **Выдача Брамвелла и раньше работала правильно** — чтение уровня типово-терпимо, так что если
  его книги были нормальными, это не признак того, что заказы тоже чинились.

- [ ] **AE2-исследователь.** Компас за 16 изумрудов должен быть `ae2:meteorite_compass`, не
  `minecraft:compass`.
- [ ] **51 предмет без маршрута.** В EMI продолжи `technicalities/69_ТОРГУЕМЫХ_БЕЗ_МАРШРУТА.md`: назови реальный
  крафт/добычу, отдельно ведро/spouting/bottling/right-click, и проверь
  `apotheosis:blazing_hellshelf` на обычном верстаке.
- [ ] **Conjurer-карта театра.** Подними картографа до 2 уровня, найди его карту Conjurer и купи её
  с компасом: карта должна искать `conjurer_illager:theatre`, а не молча исчезать. Плохо: торговля
  не появляется, карта возвращает пустой результат при доступной цели, либо после `/reload` в логе
  есть ошибка legacy-тега `conjurer_illager:on_theater_maps`; тогда пришли скрин GUI и строку лога.

## Wares — отдельный технический smoke delivery-loop

Это **не** проверка будущей экономики: нынешний example agreement штатно платит stock-предметами,
а будущие authored контракты будут 128 и 256 `create:shaft` за Spurs/Bevels и придут от отдельного
logistics NPC/доски. Здесь проверяется только физика Wares, чтобы не смешать её с authoring.

- [ ] С правами OP выполни `/wares give`, держа освободившийся инвентарь. Должен появиться
  **sealed example agreement**. ПКМ по нему раскрывает договор; запиши или пришли скрин его
  требуемых предметов, количества, числа партий и оплаты. Плохо: команда не существует, предмет
  не раскрывается или договор уже expired.
- [ ] Выдай/поставь `/give @s wares:delivery_table` и `/give @s wares:cardboard_box 64`. В стол
  положи раскрытый договор, ровно требуемые предметы и коробку. Нажми ручную доставку: она
  разрешена в текущем конфиге и должна идти примерно в 3 раза дольше Packager-пути. Плохо:
  `NO_INPUT`, `NO_PACKAGES`, `INVALID_AGREEMENT` либо завершение без списания нужных предметов.
- [ ] Доведи все партии до конца. Хорошо: счётчик `delivered` доходит до `ordered`, награда попадает
  в output, а договор становится **Completed Delivery Agreement**. Не суди по награде о цене — это
  только stock demo Wares.
- [ ] Отдельно проверь Packager: поставь Delivery Table рядом с доступным взрослым безработным
  жителем, дождись профессии `wares:packager`, затем запусти новый example agreement. Хорошо:
  житель работает у стола и delivery идёт без ручной кнопки. Плохо: он получил профессию, но стол
  навсегда остаётся `NO_WORKER`; тогда приложи скрин GUI и время суток.
- [ ] Отметь, где именно был взят тестовый договор: команда, склад новой деревни или Wandering
  Trader. Для smoke команда допустима; будущая система сохраняет vanilla Wares delivery-loop и оба
  источника agreements, но Trader появляется только по ресурсоёмкому `MERCHANT-CALL`, а agreement и
  payout будут переписаны с emerald на Spurs/Bevels. Не считать stock agreement готовой экономикой.

> Village n Pillage Remake теперь установлен и на клиенте, и на сервере (modId проверен в jars).
> Старое предупреждение «только клиент» снято; установка сама по себе не принимает его контент.

## Две проверки из реестра OPEN — они блокируют архивацию строк

Заведены 2026-08-26 из `agents/registers/2026_08_26_OPEN_LEDGER_PROOF_BACKFILL.md`. Обе строки в
`OPEN.md` уже доказаны коммитом; не хватает **только** твоего подтверждения из игры, и без него
реестр честно держит их как `game=pending` и не архивирует.

- [ ] 🟡 **§5 — блокировка мечом не должна включаться на исключённых предметах.** Возьми любой
  предмет из списка исключений правого клика, используй его как обычно и убедись, что обычное
  действие работает, а стойка блокирования **не** включается. Повторить после любого перемещения
  джарников: тег пересобирается, и вместе с ним может вернуться старое поведение.
  ⚠️ Это единственная половина, которую агент закрыть не может: тег он проверяет по файлу, а
  срабатывание — только ты в игре.

- [ ] 🟡 **§1.186 — пройти твой Desktop-набор из 51 предмета и записать каждое исключение по
  маршруту получения.** Не «работает / не работает», а именно список тех, где путь получения
  отличается от ожидаемого: они и есть результат.


## Энергетическая лестница — только после чистого старта

До сравнения с числом агент должен назвать конкретную схему, нагрузку и метод замера.
Если они не заданы, записывай фактический результат, но не считай расхождение доказанным багом.
Это относится и к числовым пунктам Energy all-green ниже; формула сама по себе не принимает стенд.

- [ ] **C&A boiler.** Level-18 boiler с 18 отдельно geared `createaddition:alternator` @256 RPM держит ровно **2 610 FE/t** и не overload; 2 286 FE/t — отдельный 224-RPM case, не диапазон.
- [ ] **CNA solar.** При полном unobstructed skylight `create_new_age:basic_solar_heating_plate` / `advanced_solar_heating_plate` дают ceiling **52 / 165 FE/t**; при меньшем skylight, ночи или погоде output снижается.
- [ ] **CNA reactor.** Одна fueled `create_new_age:reactor_rod` устойчиво держит заявленную topology, не превышая arithmetic ceiling **217 FE/t**; «видно тепло» не является приёмкой.
- [ ] **IE dynamo.** В каждом intended wind/water environment `immersiveengineering:dynamo` даёт observed output `int(3×accepted rotation)`; записать environment и rotation, а не только итог FE/t.

Основание: `bountiful_wizards_REMAKE/ENERGY_STATS.md`; результат маршрутизируется в `ENERGY-GAMEPLAY-LADDER-20260828` и `systems/ЭНЕРГИЯ.md`.

## Energy all-green — подтверждение только после чистого старта (2026-09-02)

Источник и точная методика: `agents/knowledge/energy/2026_09_02_ENERGY_ALL_GREEN_CHANGE_LIST.md` §7.
Не считать арифметику игровой приёмкой: для каждого пункта записать фактическое значение и среду.

- [ ] **G-1/G-2 Solar heat — текущий множитель 30.0.** Изолированные basic/advanced plate
  при полном дневном свете: измерить время до перегрева, затем basic + 6 Stirling за пять минут.
  Записать свет, погоду, температуру и потерю блока. Старые 17/6 секунд относились к другому
  множителю и не являются ожидаемым временем текущего конфига.
- [ ] **G-3/G-4/G-9 Boiler/reactor.** На одном и двух rods записать boiler level, stress 18 engines
  и нужное число vent/stirling, удерживающее two-rod core ниже heat limit.
- [ ] **G-5/G-6/G-7.** Проверить short-stress coil, IE dynamo в 4 погодных/rotational случаях и
  diesel-generator drain (1 000 mB diesel, ожидаемо около 303 ticks при consumer ≥800 FE/t).
- [ ] **G-8 Reactor critical.** Один `nuclear_fuel` в one rod: `ACTIVE=true` должен длиться **24 min**,
  не 4 h 48 min; иной результат отменяет расчёт R-01.
- [ ] **G-10 Cobb alternator.** После чистого старта при rep 0 у Cobb может появиться **заказ
  сдать alternator** (`cobb_wants_alternator`, `repRequired=-30` уже во всех трёх копиях).
  Это не покупка alternator с полки; проверяется JSON repair, не новая цена.

## Достижимость наград Deeper and Darker

- [ ] **Ancient Vase.** В `deeperdarker:otherside` найди `/locate structure deeperdarker:ancient_temple`, в естественно сгенерированном храме найди вазу, сломай её Silk Touch и проверь: один `deeperdarker:ancient_vase` попадает в инвентарь и закрывает item-task `3F72C953DE0304C3`. Locate failure, отсутствие ваз в template или отсутствие drop с Silk Touch — фальсификатор static route.
- [ ] **Echo Sapling.** В Otherside найди `/locate biome deeperdarker:echoing_forest`, проверь естественные echo-log/echo-leaf trees и ломай естественные leaves (Fortune III желательно), пока не выпадет `deeperdarker:echo_sapling`. Один неудачный конечный sample не доказательство: loot случайный (5% base, до 10% Fortune); фальсификатор — отсутствующий biome/trees или воспроизводимый loot failure.

Основание: независимый Sol review `agents/quests/2026_08_29_SOL_GLM53_QUEST_REWARD_ACCEPTANCE.md`. Успех переводит обе награды из static-supported в VALID; до него не считать их мёртвыми.


## Gateways — что остаётся ручным триггером после снятия рецептов (2026-08-31)

Проверки ниже различают загруженные рецепты и осадный путь. Право штатной команды уже
установлено из байткода: `open_gateway` требует permission level 2; отдельный поиск этого ответа
в игре снят. Снятие рецептов не считать исполненным только из заголовка.

- [ ] **L3-датапак не подменяет рецепты.** На целевом мире проверить загруженные датапаки
  и рецептную книгу. Перлы для установленных host-модов должны соответствовать фактически
  загруженному набору, зависимые от отсутствующих hosts не должны крафтиться.
  Старые количества 19/11 — снимок, а не достаточное доказательство текущего состава.
- [ ] **Gateway без перла — после подтверждения маршрута агентом.** Сегодня не ждать
  неопределённое «полное окно»: Factory Siege вызывает The Hordes; путь создания Gateway
  в проверенных pack-скриптах не установлен. Сначала нужен точный триггер, условия и предел
  ожидания; затем записать Gateway и источник вызова.

Основание: `agents/mods/2026_08_31_TERRA_MOREGATEWAYS_MANUAL_TRIGGER_INVENTORY.md`. Снятие рецептов
остаётся «на бумаге» до слова владельца.

## Навыки и перки — шесть проверок с ожидаемым ответом

Основание: `agents/knowledge/skills_perks/MOD_SURFACE.md` и её отчёт. Каждая строка сформулирована так,
что провал — это **опровержение записанного факта**, а не просто «не понравилось». Если результат не
совпал, страница неверна и её надо править, а не подгонять.

- **SP-1 · снят как статически решённый.** Установленный мод читает `skill-proficiencies.json`;
  legacy-файл `specialities.json` переименовывается в него, а не наоборот. Оба живых конфига уже
  под правильным именем. Проверка экрана и баланса остаётся в SP-3/SP-6; scope мастера — §1.401.
- **SP-2 · ParCool.** Прежний запрет Roll отменён владельцем; актуальная проверка приземления и боевого уклонения стоит в начале страницы.
- **SP-3 · экран specialities.** Открой экран навыков: ожидается **15 навыков**, все нулевого уровня на
  новом профиле, полоса опыта видна.
- **SP-4 · экран Puffish.** Открой экран Puffish: ожидается, что категории **нет вообще** — не пустая
  категория, а ни одной. Это и есть доказательство, что дерево мёртвое (`OPEN.md` §1.400).
- **SP-5 · лог AttributeFix.** После нового старта передать агенту строки про ParCool и неизвестные
  `puffish_attributes` из лога. Старое ожидание изменений атрибутов 4.x нельзя переносить на 3.4.3.3;
  предупреждение о неизвестном `puffish_attributes:stamina` уже есть в серверном debug.log от 31.08.
  Новый запуск нужен только для проверки текущего набора, число сообщений заранее не фиксировать.
- **SP-6 · баланс specialities.** Прокачай боевой навык и замер урона/скорости атаки рядом с
  Apotheosis-аффиксами: дефолты дают до **+50 % урона и −30 % задержки атаки**. Твой вердикт — в
  `OPEN.md` §1.402; из файлов это не выводится.

## Materlod — одна загрузка закрывает три вопроса сразу (2026-09-02)

Основание: `agents/knowledge/materlod/MOD_SURFACE.md` и её отчёт §6.

- **ML-1 · чем платит серебро.** Открой в EMI категорию Mining для `create_rns:deposit_silver` и запиши,
  **какой именно** самородок он выдаёт — `caverns_and_chasms:silver_nugget` или
  `immersiveengineering:nugget_silver`. Это и есть ответ на `OPEN.md` §1.397, и без него месторождение
  серебра нельзя оценить.
- **ML-2 · сколько месторождений видит вьювер.** В той же категории ожидается ровно **11** месторождений.
- **ML-3 · найти и исчерпать.** Найди месторождение в свежем чанке, выработай его до конца и скажи, что
  произошло на последней операции. Это висящая приёмка домена.

## Снято из чеклиста

| Снято/переформулировано G92 | Почему / что остаётся |
|---|---|
| Повторный выбор профиля и серебра | §1.326 и §1.332 решены; оставлена приёмка применения. |
| Крафт блока Warp Plate как тест обработки | Теперь attuned_shard на плите; это разные типы рецептов. |
| Отсутствие creative flight как условие BOMD | §1.273-B сохраняет поведение; бой и нагрузка остаются. |
| Solar 17/6 секунд при «текущем 1.0» | Сейчас 30.0; время измеряется заново. |
| Ожидание Gateway от Factory Siege без предела | Проверенные скрипты вызывают Hordes; нужен точный Gateway-триггер. |
| Неименованные «два дюпа / crate / пять экскаваторов» | Ожидает объектов и рецептов от агента; не игровой провал. |
| Respec Paths из G91 L2 | Не добавлен: categories=[] и оба perk bridge=false. Старые снятия G91 сохранены. |

| Исходная строка / что | Класс | Где ответ или работа |
|---|---|---|
| L179: Native tree ParCool 4.x после разделения | REVERSED | ParCool META-INF/MANIFEST.MF: Implementation-Version 3.4.3.3; §4 отчёта → актуализация PARCOOL-PATHS-SEPARATION |
| L184: Удаление AO | DONE | E01: активный клиент, modId ao/animation_overhaul отсутствуют; уже отмечено выполненным |
| L193: Fresh Moves/Joyful сохранены выключенными | DONE | E06: обе копии *.zip.disabled и совпадающие SHA-256 |
| L236: /give CEI disenchanter | DEAD | CEICommon.warnRemovedMappings, E02; TODO FTBQ-DEAD-DISENCHANTER-TASK, master chapter_2_steel_and_bronze_age.snbt:1894 |
| L238: /give CEI hyper experience bottle | DEAD | CEICommon.warnRemovedMappings, E02; reward_tables/mythical.snbt:15 → исправление мёртвой награды |
| L239: /give Galosphere silver_bomb для двух квестов | REVERSED | GItems/MappingEvents: palladium_bomb; galosphere.snbt:192,843 уже заменены; GUI-приёмка сохранена |
| L240: /give Galosphere silver_upgrade_smithing_template | DEAD | GItems полный список E04; galosphere.snbt:76 → TODO исправления мёртвой цели |
| L646: Дерево «Пути»: купить 16 узлов/20 очков | REVERSED | kubejs/data/arcane_engineer/puffish_skills/config.json:3–4 — categories=[]; WORKLOG.md:76–82 |
| L649: Покупка старых Roll/Attuned Sight | REVERSED | 33_Perk_Projection.js:3 и client_scripts/98_Perk_Attuned_Sight.js:3 — PACK_PUFFISH_PERKS_ENABLED=false |
| L654: Новый персонаж и начальный unlock breakfall в 4.x | REVERSED | ParCool 3.4.3.3; E04/E08 — текущий гид и GUI, прежнее дерево 4.x не является текущей поверхностью |
| L792: Повторный поиск CEI hyper experience bottle | DEAD | CEICommon.warnRemovedMappings, E02; mythical.snbt:15 |
| L793: Повторный поиск обоих Galosphere silver_* id | DEAD | GItems E04: текущие item ids не silver_*; MappingEvents E08 и galosphere.snbt:76,192,843 |
| L803: Доступ не-опа к open_gateway | DONE | GatewayCommand.lambda$register$1: iconst_2 → CommandSourceStack.m_6761_(I)Z; E02, permission level 2 |

| Дополнительно перенесено | Где осталось |
|---|---|
| Проверка чтения server_mirror, уже снятая 05.09 | Ответ на диске; E10 в отчёте G91, paths.py --server. |
| Устаревший запрет осматривать Village n Pillage на сервере | E01: modId установлен на обеих сторонах. |
| Старые численности карантина/кнопок и хронология выкладки | Текущая статическая проверка агента; игровой smoke и проверки конфликтов оставлены отдельно. |


Ранее снятые пункты сохранены как история, не как новая игровая очередь:

| что | почему / где остался ответ |
|---|---|
| Geo Resonance vs Rock & Stone; Geolosys; Create Ore Excavation | Моды вырезаны 25.08; финальный стек — `OPEN.md` §1.227. |
| RNS и Otherside motherlode | §1.289-B решён и выложен: обе стороны на Create RNS 1.3.1, `infiniteDeposits=false`, wolframite/uranium/osmium запрещены. Действующая fixed-state приёмка — строка «Create RNS / Wheat Book» выше; старые pre-ruling пункты не возвращать. |
| Pneu Ponder/deploy и противоречивые версии jar | Закрыто владельцем и `PNEU-UPSTREAM-ONLY-DEPLOY`; остались transport, гогглы, ISS. |
| Dynamic Waters, PrettyPipes, отключённые/отсутствующие магические моды | Соответствующих jar в живых `mods/` нет. |
| Хронология, состояние сервера, кэши, JVM-память | Не игровой чек: история в `WORKLOG.md`, работа в `TODO.md`. |
| Одноразовый маршрут структур | 12 посещений и два IDAS-вердикта приняты; первичка перенесена в `agents/archive/checklists/2026_08_29_ИГРОВОЙ_ЧЕКЛИСТ_СТРУКТУРЫ_PROCESSED.md`. Незакрытый BOMD живёт выше в этом чеклисте. |

## Disenchanter — придумать рецепт и затем пересчитать цену (игровое решение)

- [ ] В игре придумать и зафиксировать новый рецепт для живого блока `create_enchantment_industry:mechanical_grindstone` (старый ID `create_enchantment_industry:disenchanter` мёртв).
- [ ] Записать компоненты, количества и нужный этап Create так, чтобы рецепт отражал реальную сложность получения.
- [ ] После решения реализовать рецепт в master и проверить его в игре после синхронизации.
- [ ] После реализации заново вывести цену предложения Bramwell и связанные цены из фактической craft-себестоимости; старые 88646, 1581 и 330 считать только историческими ориентирами до нового расчёта.

## Ореген — сверка ссылок страницы ОРЕГЕН.md (бухгалтерия 07.09, без игры)

- [ ] **Ссылки «Проверить в игре» на странице орегена ведут в правильный раздел.** Открой `claudework/systems/ОРЕГЕН.md`:
  строки 30 и 51 должны ссылаться на `ИГРОВОЙ_ЧЕКЛИСТ.md:322-367` (раньше — `:439-463`, это Otherside motherlode и торговцы).
  Открой этот чеклист на строке 322 — там заголовок «Новый мир / дальняя разведка — ореген», на 367 —
  последняя строка пункта Temple of Duality. Строка 52 таблицы зачёркнута 🪦: строки про серебро
  (Crystal Canyons / soul silver / 9:1 / BlockSwap) в чеклисте нет и не было (`:99-105` — smoke входа,
  `:142-148` — VMinus); добавлять её нельзя, пока открыт выход серебра в Crystal Canyons (F1 аудита Opus 06.09).
  Строки 24, 25 и 60 больше не ссылаются на `agents/systems/` (папки нет: `ls claudework/agents/systems` → «No such file»);
  они ведут на `agents/knowledge/oregen/MOD_SURFACE.md` и `agents/knowledge/oregen/RISKS_AND_ACCEPTANCE.md:10`, а первичка 31.08 —
  во внешнем архиве по `agents/registers/REPORTS.md:2809`. Изумрудный пункт (:345-349) и его x3/x12 не трогались — ждут `OPEN.md` §1.494.

## Config parity — D1/D4 бухгалтерия 07.09 (без игры, ночная смена Fable)

- [ ] **D1 — `defaultconfigs/create_integrated_farming-server.toml` принят в master без единого изменённого значения.** Открой `bountiful_wizards_REMAKE/defaultconfigs/create_integrated_farming-server.toml`: файл есть, 2146 байт, 58 строк, три секции `[farming]`/`[fishing]`/`[ranching]`, `roostingInventorySlotCount = 9`, `fishingNetCooldownMultiplier = 8`. `sha256sum` по нему и по трём живым путям из `tools/paths.py --client/--server/--mirror` даёт одно и то же `860cb5a71aacb98f…`. В `reference/tuned_config_manifest.json` есть строка с этим `path` (сразу после `defaultconfigs/parcool-server.toml`), reason начинается с «Adopted UNCHANGED at the mod's shipped defaults». Это учёт, не тюнинг: числа ждут разбора карантина (КАРАНТИН.md строка 750). ⚠️ `audit_config_drift.py` этот файл раньше НЕ видел по построению (идёт от master наружу) — зелёный аудит до принятия ничего не доказывал; отчёт `agents/reviews/2026_09_07_CONFIG_PARITY_D1_D4_D8_FABLE.md` §2.
- [ ] **D4 — `cinematic_respawn` получил строку доказательства в контракте.** Открой `reference/active_modset_contract.json`, найди `policyEvidence.directClientClassMixinOrResourceSurfaceAndDedicatedBootWithoutJar`: в списке есть `cinematic_respawn` между `betterthirdperson` и `colorwheel` (список из 31 id, отсортирован). Основание — `agents/mods/2026_09_06_CINEMATIC_RESPAWN_CLIENT_ENUMERATION.md`, перепроверено по живому jar `cinematic_respawn-1.20.1-forge-1.3.0.jar` в клиенте: 13 классов, все 6 миксинов только в массиве `"client"` файла `cinematic_respawn.mixins.json`, в jar нет `data/`, нет сетевых пакетов, конфиг — локальный `.properties`; на сервере jar отсутствует. `python -c "import json;json.load(open('bountiful_wizards_REMAKE/reference/active_modset_contract.json'))"` → без ошибки. Единственный оставшийся clientOnly-id без доказательства — `mr_potions_description` (старый, не из этой волны).

## Репутационный порог заданий — после коммита `e8c76dc` (проверка в игре)

- [ ] **Задания торговцев больше не пропадают при отрицательной репутации доски.** У 126 записей в 20 пулах `*_wants` порог `repRequired` приведён к `-30` (101 значение заменено: `0`→`-30` 73 раза, `10`→`-30` 16, `25`→`-30` 3, `50`→`-30` 1, `-10000`→`-30` 8; ещё 25 записей ключа не имели и получили `-30`). После `/reload` возьми доску с репутацией ниже нуля и убедись, что задания Hollis (27 записей), Flint (12), Cobb (20) и Garrick (16) на ней появляются, а не исчезают. ⚠️ Отдельно посмотри восемь записей, у которых порог был `-10000` («без гейта» по конвенции `tools/archive/build_endgame_traders_2026_07_22.py`): `grubbs_wants_sawblade`, `hobbs_wants_create_controls`, `hollis_wants_strip_curtain`, `hollis_wants_sliceanddice_sprinkler`, `hollis_wants_luminous_glass_pane`, `hollis_wants_optical_sensor`, `hollis_wants_toolupgrade_powerpack_induction`, `wendell_wants_quantum_bridge_card` — для них `-30` это ужесточение, и на доске ниже `-30` они теперь пропадут. Разбор и вопрос A/B/C — `agents/reviews/2026_09_07_REP_REQUIRED_COMMIT_REVIEW.md` §6.

## Illager Invasion в мансионах Repurposed Structures — подтвердить руками (датапак принят 07.09)

- [ ] 🔴 **Зайди в любой мансион Repurposed Structures и посмотри, кто стоит в комнатах мобов.**
  Найти: `/locate structure repurposed_structures:mansion_oak` (или `mansion_birch`, `mansion_desert`,
  `mansion_jungle`, `mansion_mangrove`, `mansion_savanna`, `mansion_snowy`, `mansion_taiga` — датапак
  переписывает все восемь). Быстрее — `/place structure repurposed_structures:mansion_oak`.
  **Что должно быть:** в комнатах мобов стоят `illagerinvasion:archivist`, `illagerinvasion:basher`,
  `illagerinvasion:invoker`, `illagerinvasion:provoker` — не ванильные `minecraft:vindicator` и
  `minecraft:evoker`. Наведись на моба с F3 или проверь `/execute as @e[type=illagerinvasion:basher,distance=..64] run say ok`.
  **Отдельно запиши, есть ли РЯДОМ ванильные виндикаторы RS:** датапак даёт по одному элементу с
  `weight: 1` и `fallback` на старый пул, и по файлам нельзя понять, вытеснил он старые комнаты
  полностью или соседствует с ними. Это единственное, что нельзя проверить без игры.
  ⚠️ **Ванильный `minecraft:mansion` проверять не надо** — датапак его не трогает, там всё по-старому.
  **Зачем это важно:** мир уже частично сгенерирован с этими комнатами, датапак живёт на сервере с
  20.08, и до 07.09 его вообще не было в репозитории. Пока эта проверка не закрыта, сложность
  мансионов и их лут нельзя считать принятыми (`technicalities/КАРАНТИН.md`, строка про
  `Repurposed_Structures-Illager_Invasion_v2.zip`). Разбор:
  `claudework/agents/reviews/2026_09_07_ILLAGER_DATAPACK_ADOPT.md`.

### После сборки и выкладки нового `factory_siege` — четыре проверки штрафа и коридора

Добавлено 2026-09-11 по `claudework/agents/factory/2026_09_11_H8_BREACH_STORAGE.md`. Это **игровые**
проверки: ни одна из них не делается без запущенного мира, и ни одна не проверена сейчас.

1. **Волна по-прежнему не ломает ничего** — и это ожидаемый результат, а не провал: коридоров в
   реестре ноль. Если что-то сломалось — это находка и повод остановиться.
2. **Проиграть осаду** и найти в логе строку вида
   `Factory Siege gateway <uuid> failed; settlement outcome PUNISH`.
3. **Перезайти в мир и проиграть ту же осаду ещё раз** — во второй раз в логе должно быть
   `ALREADY_SETTLED`, а не второй `PUNISH`. Это проверка на то, что штраф оседает ровно один раз.
4. **Найти файл `factory_siege_failures.dat`** в папке `data/` мира и убедиться, что он переживает
   перезапуск сервера.
