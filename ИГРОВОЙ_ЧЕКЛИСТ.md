# Игровой чеклист

## Xtra Arrows 3 — после server rollout 2026-08-26

- [ ] На SERVER убить bat: проверить ровно одну `xtraarrows:bat_skin`; затем vex: ровно две
  `xtraarrows:vex_wing`. Ничего не менять по результату: owner уже выбрал сохранить оба GLM.
- [ ] Убедиться, что обычный chest loot не изменился: мод не содержит обычных `loot_tables`, только
  два GLM выше. Отдельно отметить, если registry-ID warnings для новых стрел проявятся в игре.

**33 открытых проверок.** Сначала: (1) после остановки игры заменить три оставшиеся FTB-Quest
ссылки и текст `Construction Wand` на живые EB/BG, затем проверить вход; (2) проверить новый чанковый ореген — это закрывает `OPEN.md` §1.240; (3) проверить
снежную петлю Henry, потому что она удваивает торгуемый ресурс. Оregen проверять после полного
перезапуска, только в неразведанных чанках.

Структуры — отдельный маршрут: [ИГРОВОЙ_ЧЕКЛИСТ_СТРУКТУРЫ.md](technicalities/ИГРОВОЙ_ЧЕКЛИСТ_СТРУКТУРЫ.md).

## VMinus — одна проверка, которая экономит будущий потерянный день (2026-08-27)

- [ ] 🔴 **Проверить, работают ли `Absorption` и `Health Boost`.** У мода `vminus` дефолтный
  `effects/banned.json` перечисляет ровно эти два эффекта, и живой ли этот запрет — не подтверждено.
  Проверить проще всего так: выпей **зелье черепахи** (даёт Absorption) и посмотри, появились ли
  жёлтые сердца; отдельно — сработает ли **Тотем Бессмертия**, он даёт Absorption при срабатывании.
  Заодно посмотри на маяк.
  **Зачем это важно:** если запрет живой, тотем работает не так, как ты думаешь, — а `vminus` при
  этом мод с нулём контента, на который никто никогда не подумает. Полный разбор:
  `claudework/agents/mods/2026_08_27_QUARANTINE_BACKFILL_LIGHT_FIVE.md`, мод 1/5.

## До входа на сервер

- [ ] 🟢 **Разбрызгиватель (Sprinkler) снова МОЖНО ставить — запрет снят 2026-08-26. Проверь его один раз.**
  Джарник Slice and Dice пропатчен точечно (вариант D): единственное тело CEI-предиката заменено на
  `iconst_0; ireturn`, поэтому переименованные в CEI 2.5.2 классы больше не читаются никогда.
  Выложено на все четыре копии, все четыре имеют один хеш `82176e3a…`; сервер поднят и проверен
  (`Done (6.332s)`, RCON 0/20, ни одного `NoClassDefFoundError`).
  **Что проверить в игре:** поставь Разбрызгиватель и прогони через него обычную воду — он должен
  увлажнять/поливать как раньше, без вылета. Проверь заодно, что слайсер, доска и харвестер целы.
  ⚠️ **Что мы сознательно потеряли:** «залить в Разбрызгиватель жидкий опыт → падают XP-орбы».
  Это единственная функция, которую снял патч; ни один рецепт и ни один квест пака её не использовал.
  🔴 **Не нажимай кнопку обновления в CurseForge, не проверив после неё.** Она вернёт оригинальный
  джарник поверх патча, и отложенный краш придёт назад молча — имя файла и версия при этом те же.
  Это измерено, а не предположено: `packageFingerprint` записи CF (`1655686120`) уже не сходится с
  тем, что лежит на диске (`2088042773`).
  Проверка одной командой, читает и ничего не двигает:
  `python bountiful_wizards_REMAKE/tools/cf_manifest_diff.py --verify-bytes` — строка
  `modId=sliceanddice … DIFFERS BY: bytes` означает, что **патч на месте**. Если этой строки нет —
  вернулся оригинал, скажи агенту, патч накатывается заново за минуту.
  ⚠️ Та же команда покажет заодно, какие **7** положенных руками джарников кнопка выбросит и какие
  **6** удалённых модов скачает обратно.

- [ ] ⚠️ **После миграции FTB-Quest проверь вход клиента на сервер.** Construction Wand уже не
  активен ни на одной стороне; на обеих стоят `effortlessbuilding-1.20.1-3.11.jar` и
  `buildinggadgets2-1.0.8.jar`. До остановки игры остаются три stale квестовые ссылки и одна
  карточка/описание; решение и порядок — `OPEN.md` §1.263.

- [x] 🟢 **`!siege` закрыт — починено и в мастере, и на сервере (сверено 2026-08-26).** Любой игрок, написав в чат
  `!siege raidnow`, запускает орду: обработчик чата в `90_Siege_Core.js:274` не проверяет ни права,
  ни выключена ли система. Там же `!siege on/off/reset` и установка счётчика эскалации. The Hordes на
  сервере стоит, команда `/hordes start` живая, скрипт загружен. Тебе одному это не мешает —
  опасность только в чужих руках. **Починка уже в мастере** (шесть гейтов, мутирующие команды
  за правами оператора, `!siege status` остался всем) **и уже приехала на сервер**: `90_Siege_Core.js` там байт-в-байт равен мастеру (`903ce221…`, сверено 2026-08-26). Строка закрыта — постороннего можно пускать.

- [ ] **Construction Wand: квестовый хвост.** Jar уже снят с активного сервера и сохранён в
  `_removed_constructionwand_2026-08-26`; на обеих сторонах остались только Effortless Building и
  Building Gadgets. После остановки игры заменить 3 id-ссылки и карточку/описание в FTB Quests,
  затем проверить boot и подключение. Не редактировать SNBT при живом сервере.

- [ ] 🔴 **Гиперопыт и новый рецепт пустой руны — проверь в игре ПОСЛЕ того, как правка ляжет.**
  Ты ответил **A** 2026-08-26 (`ответ А если это технически возможно`), поэтому вот что тебя ждёт и
  что именно смотреть.
  **Что произошло.** Апгрейд CEI 2.5.2 перестал регистрировать жидкость
  `create_enchantment_industry:hyper_experience` — в джарнике **ноль** её вхождений. Наши скрипты
  зовут её в четырёх файлах, KubeJS материализует её как пустой стек, и **15 наших рецептов
  отвергаются по одному**, файл при этом живёт. В логе загрузки это не выглядит как поломка.
  **Новый рецепт (вариант A).** `create_enchantment_industry:experience` в количестве **×10**
  (1000 mB вместо 100 mB), чтобы сохранить задуманную концентрацию 1:10. Крафт дорожает, поэтому
  полка и выкуп пустой руны поедут **вверх** — сейчас там полка **200** и выкуп **120**, и оба числа
  выведены из сборки, которой в игре нет.
  ✅ **Твой прямой вопрос ОТВЕЧЕН 2026-08-26, по джарнику, двумя независимыми маршрутами.** Убрали
  точно — и мод говорит это словами, в `CEICommon`: *«removed in 2.5 and has no semantics-preserving
  replacement; it will not be remapped»*. Не переименовали. Но **заменили концепцию**: вместо
  «гипер-жидкости в трубе» появилась линия **Super Experience** (`Cake o' Enchanting`, `Block of
  Super Experience`), и её второй бак у Blaze Enchanter **нельзя подать трубой** — вход предметный.
  Поэтому в `OPEN.md` §1.274 появился новый вариант **D**: пойти за модом вместо своей жидкости.
  Это уже не только про цену, и я переспрошу тебя отдельно.
  ✅ **СНИМАЮ своё же предупреждение про Bramwell — я ошибся, проверено по байткоду.** Я написал
  «не бери заказ у Bramwell, он невыполним». Это неверно: Bountiful **проверяет** записи пула
  (`BountyTypeItem.isValid` резолвит id и сравнивает обратно), мёртвая запись молча выбрасывается с
  WARN и **никогда тебе не предлагается**. Ловушки нет, ничего делать не надо.
  🔴 **А вот настоящая невыполнимая цель есть, и она в квестах.** Глава «Steel and Bronze Age»,
  квест `325BEDD59362FD79`: задача просит `create_enchantment_industry:disenchanter` — предмет,
  которого в моде больше нет (CEI убрал его в 2.5). У FTB Quests фильтра валидности нет, поэтому
  задача висит и **закрыться не может никогда**. **Если увидишь её в квестбуке — не трать на неё
  время, чиню.** Работа: `TODO.md` `FTBQ-DEAD-DISENCHANTER-TASK`.
  ⚖️ **Ты ответил A′ 2026-08-26. Что это значит для ТОТЕМА — посчитано, и это заметно в игре.**
  Тотем сейчас просит 1000 mB гипера, а гипер был концентратом 10:1 — то есть по замыслу это
  **10 000 mB обычного опыта**. Оставить «1000 mB experience» = удешевить Тотем Бессмертия ровно
  **в десять раз**. Просто вписать 10 000 нельзя: спаут держит **1000 mB**, бассейн **2000 mB**
  (замерено по байткоду Create, конфига нет).
  **Предлагаю `sequenced_assembly` с десятью петлями** по 1000 mB — суммарно те же 10 000, каждая
  операция в пределах лимита, автоматизация цела. **Что изменится для тебя в игре: тотем перестанет
  делаться одним спаутом, нужна будет сборочная линия.** Это единственное заметное последствие A′, и
  я не хочу подсовывать его молча. Решение и альтернативы — `OPEN.md` §1.279.
  🔴 **И про сам ×10 в исходном виде: он не собирается.** Спаут Create физически не запускает операцию больше
  **1000 mB** (бак зашит в байткод, конфига нет), а ×10 сделал бы активацию тотема 10 000 mB — спаут
  просто **завис бы навсегда, без строки в логе**. Поэтому вариант A в исходном виде снят; выбор
  теперь A′ (×10 везде, кроме тотема) или D (уйти на Super Experience). Спрошу отдельно.
  **Что проверить в игре, когда правка ляжет** (EMI-иды указаны):
  1. `create_enchantment_industry:experience` — жидкость существует и наливается в бассейн;
  2. сборка пустой руны `irons_spellbooks:blank_rune` идёт и берёт **1000 mB**, а не 100;
  3. торговцы: полка Alistair и выкуп Corvin по пустой руне показывают **новые** числа, а не 200/120.
  ⛔ **Сейчас применить нельзя и это не забывчивость:** правка рецепта требует пересборки четырёх
  кэшей по порядку, а первый шаг — синк на живой сервер, который ждёт закрытой игры
  (`TODO.md` `RECIPE-INDEX-STALE`). Решение: `OPEN.md` §1.274.

## Новый мир / дальняя разведка — ореген

**Стек.** Активен `AdLods-1.20.1-8.1.12.0-build.2294`; `geolosys`, `creategeoresonance` и
`createoreexcavation` отсутствуют на обеих сторонах. При плохом результате запиши координаты,
измерение, высоту и приложи скрин.

- [ ] **Золото и Twilight.** В новом Overworld на `y≈−20` найди золото. Затем в Twilight Forest
  найди обычную руду: `OPEN.md` §1.240 открыт потому, что AdLods снимает 29 ванильных фич, семь из
  них Twilight, а генерирует только в Overworld. Отсутствие золота — дефект; пустой Twilight —
  результат для решения, не «нормально».
- [ ] **Железо и крупные жилы.** В `−59..20` россыпей стало 110→70 жил/чанк, но жила с
  `raw_iron_block` должна быть раз в ~40 чанков; медь и уголь — в ~30. Плохо: железо исчезло или
  жила не встретилась на длинном маршруте.
- [ ] **Алмазная малая жила — применена после синка и рестарта; смотреть только новые чанки.** Lode теперь
  8–11 блоков: `70 % diamond_ore`, `30 % diamond_block`, раз в ~6 чанков. Нормально: несколько
  storage-блоков среди малой жилы, суммарно не более 99 алмазов до Fortune; плохо: прежняя «стена»
  из сотен ore, отсутствие storage-блоков или жила крупнее 11 природных блоков. Запиши биом (`F3`),
  координаты и скрин. Старые чанки не меняются.
- [ ] **Индикаторы и финдер.** Над жилой посмотри реальные наземные индикаторы, затем подойди с
  `metal finder`: красные лампы должны отличать жилу от россыпи. В §1.244 уже 133 из 216 мёртвых
  ссылок индикаторов, поэтому назови, что именно выросло/не выросло.
- [ ] **Редкости и AE2.** Аметистовые жеоды — кандидат раз в 72 (было раз в 18), спиннел примерно
  вчетверо реже; природного `ae2:certus_quartz_ore` быть не должно, цертус только из метеорита.
- [ ] **Кирки.** `oreexcavation-1.13.174` — единственный оставшийся такой инструмент. Проверь
  массовую добычу: хорошо — работает как прежде; плохо — нет, не работает или цепляется за вырезанный
  `createoreexcavation`.
- [ ] **Dungeons Now Loading: Temple of Duality.** `dungeonnowloading:fairkeeper_spawner` не является
  обычным моб-спавнером: пройди штатный redstone-триггер камеры (кнопка/плита/головоломка) и подожди
  2–6 секунд. Нормально: блок загорается (`fairkeeper_alert=true`) и выпускает 1–4 мобов; после волны
  он одноразово исчезает. Не ломай и не переставляй его. Если после триггера мобов нет, запиши
  координаты и измерение, выполни `/data get block X Y Z` и приложи скрин проводки/состояния.

> `create_rns` ещё лежит на обеих сторонах, но снимается сегодняшним решением владельца. Mine Head,
> буры, Deposit Scanner и Otherside motherlode не тестировать: это уходящая ветка.

## Один творческий стенд — производство и транспорт

- [ ] **Снежная петля Create: Henry.** Проверь 3 блока снега → 6 слоёв ванильным крафтом →
  `freezing` → 6 блоков, и снежок → `freezing` → слой. Хорошо: рецепта блока снега уже нет; плохо:
  петля жива. Это срочно: Marlow продаёт `snow_block` по 20, Ashby покупает по 12 партиями 64–256.
- [ ] **Direct Chute.** `direct_chute:direct_chute` стоит на обеих сторонах. Над сундуком и отдельно
  над воронкой Create подай одинаковую стопку. Хорошо: перенос в ожидаемом направлении; плохо:
  воспроизводится «иногда не работает».
- [ ] **Effortless Building.** `effortlessbuilding-1.20.1-3.11.jar` активен на обеих сторонах.
  В survival проверь ранние режимы стройки на чужой/защищённой территории: хорошо — честная
  строительная помощь без обхода claims, дюпа или мгновенной большой постройки; плохо — любой из
  этих abuse. Это runtime-гейт `OPEN.md` §1.236.
- [ ] **Pneumatic Express: предметы.** Активен `create_pneumatic_express-1.0.1-forge-1.20.1`.
  Насос → Pipe → приёмник должен провести 16 `minecraft:copper_ingot` и пережить выгрузку чанка.
  Жидкость не считается: предметы не идут — дефект capability-слоя.
- [ ] **Pneumatic Express: гогглы.** На той же трубе нужны режим (давление/вакуум), направление,
  скорость и число предметов. Пустой или ложный overlay — дефект.
- [ ] **ISS Ponder.** `iss_ponder-forge1.20.1-1.0.3-all` есть на обеих сторонах. В EMI открой W у
  `irons_spellbooks:guiding_bolt`: нужен cast по манекену, не `gui.iss_ponder.cast_failed`/базовая
  сцена Create. Неизученный spell обычным кастом обязан отказать без эффекта.
- [ ] **Пять применённых исправлений.** В EMI: впечатанный посох без знания/редкости не кастует; меч
  теряет прочность при блоке; два дюпа не имеют рецепта; `crate` не даёт близнеца. У пяти машин-
  экскаваторов выход канонический и рецепт не исчез. Любое исключение — название и скрин.
- [ ] **Прокатка.** У `createaddition:rolling` медной пластины ожидается
  `createaddition:copper_wire`, не `immersiveengineering:wire_copper`: это известный обходной
  маршрут. Плохо — пропавший рецепт или третий выход.
- [ ] **Взрывы.** `Explosion Overhaul` и `Shrapnel Explosions` стоят на обеих сторонах. Проверь
  Create-механизм, бочку с порохом, крашер: нужен заметный урон без поломки машинной логики.

> Regulator не в активном jar: это прототип до решения о цене/фазе (`OPEN.md` §1.229). Не искать его
> в EMI и не считать отсутствие поломкой.

## Магия, перки и прогрессия — один персонаж

- [ ] **Полка томов Citadel — только после синка master KubeJS и restart/reload сервера.**
  Master override заменяет неподдерживаемый `minecraft:set_written_book_pages` на один именованный
  `minecraft:book`; до похода сначала в свежем `latest.log` должны отсутствовать и
  `Couldn't parse element loot_tables:irons_spellbooks:chests/citadel/citadel_tomes`, и
  `Unknown type 'minecraft:set_written_book_pages'`. Затем открой полку Citadel: обычные пулы
  остаются, а её 50%-пул даёт именованную обычную книгу, не written tome. Иначе приложи скрин и
  строки лога. Основание: `TODO.md` `LOOT-CITADEL-TOMES-FUNCTION`.

- [ ] **Активные аддоны Iron's.** На обеих сторонах есть `irons_spellbooks_tweaks-1.5.0`,
  `twilight_spellbooks-1.2.0`, `farmers_spell`, `CasterCuriosBonus`, `mobbility` и аддон. Вкладки
  не должны быть пустыми/дублированными; на враге должно быть видно, что Mobbility колдует.
- [ ] **Unraveling Spells.** Невладеющий персонаж пробует `irons_spellbooks:graybeard_staff`:
  хороший исход — отказ, плохой — обход `irons_restrictions`. Затем magic lectern крафтится,
  принимает свиток и скрывает неизученное; отметь свитки из 29 его лут-таблиц как возможный обход.
- [ ] **Три опасных заклинания.** Проверь COMMON `fang_strike` (51.0 DPS), `ice_spikes` (30.8 DPS)
  и UNCOMMON `arrow_volley` (15 s cooldown). Плохо: COMMON стирает обычного врага без подготовки.
- [ ] **Дерево «Пути».** `/skills category open @s arcane_engineer:paths` → 16 узлов, 20 очков.
  Купи `perk_test_scoreboard_ready` *до* `perk_test_scoreboard_increment`; проверь атрибуты, night
  vision, scoreboard, bonus point и `/tag @s list`. Плохо: пустое окно или молча неработающий тип.
- [ ] **Кувырок и чутьё инженера.** До `perk_shared_roll` кувырка после падения с 6 блоков нет,
  после — он отменяет урон, а респек отнимает перк. После `perk_engineer_attuned_sight` сними гогглы
  у механизма Create: overlay остаётся. Любая способность до покупки/после респека — дефект.
  ⚠️ **Сейчас этот пункт заведомо провалится, и это известно:** половина ParCool у перка мертва
  против установленного `4.0.0.x` — `OPEN.md` §1.305. Проверять его имеет смысл только после починки.
- [ ] **Своё дерево ParCool: стартует ли новый персонаж уже с `breakfall`.** Создай **нового**
  персонажа, открой гид `parcool:parcool_guide` и посмотри, открыт ли `parcool:breakfall` до
  единой траты опыта. Из файлов это не читается: `ActionCapabilities` получает `initial` в
  конструкторе `Parkourability`, а булев из constant pool — не факт о живой игре. 🔑 Ответ решает,
  делает ли вариант A в `OPEN.md` §1.305 настоящие ворота или косметику, и он же нужен до выбора
  A/B в `OPEN.md` §1.306.
- [ ] **Редкость на выделенном сервере.** Заработай ступень выше старта и попробуй впечатать spell
  этой ступени. Отказ из-за чтения только `startingRarity` — runtime-дефект `OPEN.md` §1.181.
- [ ] **Мировые события.** `!events test cache` даёт три строки слуха и `lootr:lootr_chest`; `star`
  оставляет шрам 3×3 с обсидианом/костром/сундуком; `quiet`, затем `/time set 13000`, гасит hostile
  spawn до рассвета. Пропавший объект, спавн или неочистившаяся фаза — плохо.
- [ ] **Разведка IE.** `immersiveengineering:coresample` сообщает место/находку; в `fieldguide` →
  геология 17 записей. Плохо: пустая категория или в EMI остались `kubejs:prospector_drill`,
  `core_sample`, `sample_cartridge`.

## Жители и новый контент

- [ ] **Торговцы.** В одной деревне осмотри `horn_merchant`, `visitor`, караваны (🪦 четвёртое имя, `silkroute_trader`, снято 2026-08-27: он принадлежит моду `arkane_domains`, которого нет **ни на клиенте, ни на сервере** — `ls <side>/mods | grep -i arkane` пуст с обеих сторон, и `OPEN.md` §1.218 велит убрать мод целиком; осматривать там было нечего):
  что продают, когда доступны, не заменяют ли Bountiful. Плохо: ранний бесплатный маршрут/дубль доски.
- [ ] **Заказы на зачарованные книги — после синка копий.** Возьми у **Гидеона** или **Векса**
  любой заказ на зачарованную книгу и сдай её. Раньше это было **невозможно**: уровень в заказе
  записан как `lvl:3` (IntTag), а настоящая книга носит `lvl:3s` (ShortTag), и ванильное сравнение
  отбрасывает теги разных классов ещё до сравнения значений. Исправлено 62 записи, но **в мастере** —
  пока копии не синканы, в игре будет старое поведение.
  Плохо: доска принимает книгу, а заказ не закрывается. Хорошо: закрывается сразу.
  ⚠️ **Выдача Брамвелла и раньше работала правильно** — чтение уровня типово-терпимо, так что если
  его книги были нормальными, это не признак того, что заказы тоже чинились.

- [ ] **AE2-исследователь.** Компас за 16 изумрудов должен быть `ae2:meteorite_compass`, не
  `minecraft:compass`.
- [ ] **51 предмет без маршрута.** В EMI продолжи `69_ТОРГУЕМЫХ_БЕЗ_МАРШРУТА.md`: назови реальный
  крафт/добычу, отдельно ведро/spouting/bottling/right-click, и проверь
  `apotheosis:blazing_hellshelf` на обычном верстаке.

> `village_n_pillage_remake-1.1.1-forge-1.20.1` есть **только на клиенте**, на сервере его нет.
> Его 12 мобов, 40 структур в 20 наборах и руда `windswept_andesite_iron_ore` (10×13 блоков/чанк,
> `y 90..159`, `windswept_peaks`) не принимать в мультиплеере до связной серверной выкладки: «пусто»
> сейчас было бы ложным результатом.

## Две проверки из реестра OPEN — они блокируют архивацию строк

Заведены 2026-08-26 из `agents/registers/2026_08_26_OPEN_LEDGER_PROOF_BACKFILL.md`. Обе строки в
`OPEN.md` уже доказаны коммитом; не хватает **только** твоего подтверждения из игры, и без него
реестр честно держит их как `game=pending` и не архивирует.

- [ ] 🟡 **§5 — блокировка мечом не должна включаться на исключённых предметах.** Возьми любой
  предмет из списка исключений правого клика, используй его как обычно и убедись, что обычное
  действие работает, а стойка блокирования **не** включается. Повторить после любого перемещения
  джарников: тег пересобирается, и вместе с ним может вернуться старое поведение.
  ⚠️ Это единственная половина, которую агент закрыть не может: тег он проверяет по файлу, а
  срабатывание — только ты в игре.

- [ ] 🟡 **§1.186 — пройти твой Desktop-набор из 51 предмета и записать каждое исключение по
  маршруту получения.** Не «работает / не работает», а именно список тех, где путь получения
  отличается от ожидаемого: они и есть результат.

## Снято из чеклиста

| что | почему / где остался ответ |
|---|---|
| Geo Resonance vs Rock & Stone; Geolosys; Create Ore Excavation | Моды вырезаны 25.08; финальный стек — `OPEN.md` §1.227. |
| RNS и Otherside motherlode | `create_rns` снимается сегодняшним решением. |
| Pneu Ponder/deploy и противоречивые версии jar | Закрыто владельцем и `PNEU-UPSTREAM-ONLY-DEPLOY`; остались transport, гогглы, ISS. |
| Dynamic Waters, PrettyPipes, отключённые/отсутствующие магические моды | Соответствующих jar в живых `mods/` нет. |
| Хронология, состояние сервера, кэши, JVM-память | Не игровой чек: история в `WORKLOG.md`, работа в `TODO.md`. |
| Дубликаты структуры | Маршрут и координаты — только в `technicalities/ИГРОВОЙ_ЧЕКЛИСТ_СТРУКТУРЫ.md`. |
