# Дайджест

Что агенты закончили и что это значит. Скажи **«прочитал дайджест»** —
страница уедет в архив за пределами репозитория
(`BountifulArchive/claudework_archive/digests/`), а не удалится.

⚠️ Пока не прочитан — только дополняется, никогда не переписывается.

---

### Добавлено 2026-09-04 22:01

## Вердикты по 13 продуктам волны 63–64 (продюсер G65, 2026-09-04 22:0x)

Источник: отчёт Воркера H2 `knowledge/doc_method/2026_09_04_DIGEST_VERDICTS_24.md` (13 из 31).
Три его вердикта я перепроверил и **исправил** — они устарели за час, см. ⚠️ ниже.
Остальные 18 продуктов пишет H3 прямо сейчас (`..._DIGEST_VERDICTS_PART2.md`).

1. **Зеркало доков** (`DOCS_MIRROR_DRIFT_INVENTORY.md`). «17 коммитов отставания» пугает не тем:
   по содержанию расходится **1 страница из 28**, остальные 26 побайтово идентичны. Настоящая
   находка обратная — реальный синк прямо сейчас **удалит** `ДАЙДЖЕСТ.md` из зеркала, потому что
   страницы нет на диске между `clear` и следующим `append`. → синк только после того, как эта
   страница вернётся на диск (она вернулась этой записью).
2. **Гейт дуп-циклов** (бриф H4 + `DUPCYCLE_DETECTOR_SPLIT.md`). Fix B развёл падение детектора и
   отсутствие базиса; тест 6/6, код в коммите `140efbb`. Решения владельца нет.
3. ⚠️ **Исправлено.** H2 написал «Воркер L3 умер, продукта нет». Это было верно в 20:38 —
   `OPEN_LEDGER_EVIDENCE_GAP.md` лёг в 21:09 (28 КБ) и уже разведён в `REPORTS.md`.
   Живой остаётся сама проблема: **101 закрытое решение без доказательства**; L1 этой волны
   разбирает строки 31…101.
4. **Бриф L2 про зеркало** — та же новость, что и №1, не читайте как две.
5. ⚠️ **Исправлено.** «L1 умер» — `state/2026_09_04_SELFCHECK_61_EVIDENCE.md` лёг в 21:10 (39 КБ),
   разведён, 26 обещаний из 61 готовы к закрытию.
6. ⚠️ **Исправлено.** «H3 умер» — `UNROUTED_REPORTS_PACKAGE_B.md` лёг в 21:10 (78 КБ), разведён.
   Но его структурный вывод держится и стал хуже: **неразведённых отчётов 72**, волна производит
   их быстрее, чем их разводят. → решение владельцу: A) воркер-разводчик в каждой волне
   (−1 воркер на полезную работу); B) пакетами раз в несколько волн (очередь растёт, отчёт
   протухает); C) как есть (72 отчёта — уже оплаченная и неприменённая работа). Рекомендую A.
7. **Дайджест не догоняется разовым прогоном.** Бриф говорил 24, инструмент 31, сейчас 46.
   → решение владельцу: A) дописывать по ходу каждой волны (продюсер тратит такты);
   B) отдельный воркер-дайджестер (−1 воркер); C) пусть копится (вы платите за то, что не увидели).
   Рекомендую A — этой записью я его и начал.
8. **Страница Gateways/корундум/соль** — правка однострочная, чинили битую ссылку. Содержание не
   менялось: «UPDATED» в дайджесте ≠ «стало другим».
9. **Коммит-ревью `b97a4c8` (GLM):** 3 находки, все живы, все одного класса — **число в сообщении
   коммита не воспроизводится из его же диффа**. Кода не касается. → это повторяющийся дефект,
   а не три случая; правило пошло в `POLISHING_TODO.md`.
10. ⚠️ **Исправлено.** H2 нашёл `CR-20260904-1a413d9.md` пустым бланком без вердикта — верно на
    19:32. Вердикт GLM записан в 21:23: **findings (2)**, обе низкие и обе уже исправлены
    (ROUTING.md показывал снятую модель Gemini; OPEN.md называл коэффициент выкупа рыбы 0.897
    универсальным, а у сельди 0.902 — записано как исключение).
11. **`TODO_ROWS_DONE_BUT_OPEN.md`:** ревизия опровергла собственный источник — 2 из 3 «открытых»
    строк были закрыты в тот же день. 🔴 Опасная строка: `VINTAGE_URANIUM_TAG_GATE_NEVER_APPLIED`
    открыта, но её буквальная инструкция **ломает загрузку** — переформулировать до выдачи.
12. **`DOC_ROUTES_KNOWLEDGE_80.md`:** из «80 битых ссылок» настоящая ровно **одна** (починена),
    70 — долг по пометке архива, 9 — ложные срабатывания чекера. Расширять предикат чекера было бы
    ошибкой. Осталось: прогнать `repair_archived_citations.py` (53 из 70 одной командой).
13. **`BACKLOG_ROUTING_PACKAGE.md`:** сверх двух пропущенных по заданию воркер нашёл **ещё семь**
    уже полностью разведённых вопросов. → правило перед разводкой: сперва грепнуть OPEN/TODO по
    предмету, потом предлагать строку; дубль-строка названа самой дорогой ошибкой разводки.

---

### Добавлено 2026-09-04 22:02

### Точные пути к тем же 13 продуктам (для инструмента — читать не обязательно)

1. `claudework/agents/knowledge/doc_method/2026_09_04_DOCS_MIRROR_DRIFT_INVENTORY.md`
2. `claudework/agents/orca/briefs/2026_09_04_H4_DUPCYCLE_DETECTOR_SPLIT.md`
3. `claudework/agents/orca/briefs/2026_09_04_L3_OPEN_LEDGER_EVIDENCE.md`
4. `claudework/agents/orca/briefs/2026_09_04_L2_DOCS_MIRROR_DRIFT.md`
5. `claudework/agents/orca/briefs/2026_09_04_L1_SELFCHECK_EVIDENCE.md`
6. `claudework/agents/orca/briefs/2026_09_04_H3_UNROUTED_PACKAGE_B.md`
7. `claudework/agents/orca/briefs/2026_09_04_H2_DIGEST_VERDICTS.md`
8. `claudework/agents/knowledge/oregen/GATEWAYS_CORUNDUM_SALT.md`
9. `claudework/agents/reviews/CR-20260904-b97a4c8_GLM_REVIEW.md`
10. `claudework/agents/reviews/CR-20260904-1a413d9.md`
11. `claudework/agents/knowledge/doc_method/2026_09_04_TODO_ROWS_DONE_BUT_OPEN.md`
12. `claudework/agents/knowledge/doc_method/2026_09_04_DOC_ROUTES_KNOWLEDGE_80.md`
13. `claudework/agents/knowledge/doc_method/2026_09_04_BACKLOG_ROUTING_PACKAGE.md`

Плюс сам отчёт, из которого взяты вердикты:
`claudework/agents/knowledge/doc_method/2026_09_04_DIGEST_VERDICTS_24.md`

---

### Добавлено 2026-09-04 23:48

## 2026-09-04, поздний вечер — что нашли шесть последних воркеров

**Терминалы копились не потому, что кто-то забывал их закрыть.** Ворота, которые держат вкладку
воркера открытой, требовали строку в `REPORTS.md` для файла, которому эта строка **запрещена**:
если задача велела воркеру обновить постоянную страницу знаний, ворота требовали её зарегистрировать,
а `REPORTS.md` с 2026-09-03 такие страницы намеренно не регистрирует (строка отвечала бы на вопрос
«куда ушло содержимое» её же собственным путём). Строку написать нельзя → вкладка не закрывается
никогда. Починено (`b5ebb7b`), 13 вкладок закрыты, осталось 7 и все по делу.

- **ASTRA (GPT-6 high), провайдерские папки — 57/57 файлов.** План свёртки с хешами источников,
  владельцами, доказательствами входящих ссылок и пофайловой инструкцией. 50 файлов ложатся в 27
  существующих страниц, семи нужно по два адресата. **19 явных блокеров + 7 остатков кода/данных** —
  то есть папку нельзя просто «схлопнуть», и он это доказал, а не заявил.
- **ASTRA #2, папка `economy/` — честно частично: 32 из 158.** 19 условных «архивировать после
  свёртки», 13 явных блоков, **ноль немедленных удалений**. Ключевое: живой код и пулы **противоречат**
  нескольким старым «pending»-утверждениям, а детальные JSON’ы, staged-тесты и снапшоты не дают
  выкинуть источник по одной сводке. Он сам назвал границу и дал точку продолжения, вместо того чтобы
  сделать вид, что закончил 158 файлов. Для теста новой модели это ровно то поведение, которое нужно.
- **GLM 5.3 Flash, `doc_method/` — 172 файла в 9 предметных групп.** И он **опроверг собственное
  задание**: в брифе стояло «190 датированных», в папке физически 172 (168 датированных + 4 без даты).
  Пересчитал сам, а не принял цифру на веру.
- **Хвосты волны 2026-09-03, лежавшие без разводки сутки.** Развели, и оттуда выпали три работы:
  U-8 (доказательство отсутствия тегов четырёх металлов) **никуда не приземлился** — сдан только
  текстом; страница `oregen/CURRENT_STATE.md` в настоящем времени велит не доверять индексу рецептов,
  **который уже в порядке** с 21:31 (падает другой кэш); оба остаточных worktree **не несут ни одного
  невлитого байта** — ни merge, ни cherry-pick не нужны.
- **Три доказательных пакета вечера** (`UNROUTED_REPORTS_PACKAGE_B`, `SELFCHECK_61_EVIDENCE`,
  `OPEN_LEDGER_EVIDENCE_GAP`) уже стоят исполняемыми строками в `TODO.md`: 12 предложений разводки,
  26 из 61 обещаний готовы к закрытию, 30 доказательных маркеров для закрытых решений.

Решений от тебя ни один из этих пунктов не требует.

---

### Добавлено 2026-09-04 23:49

### Источники к разделу выше — по одному пути на вердикт

- ASTRA, провайдерские папки 57/57 → `claudework/agents/knowledge/doc_method/2026_09_04_PROVIDER_FOLD_PLAN_ASTRA.md`
- ASTRA #2, `economy/` 32 из 158 → `claudework/agents/knowledge/doc_method/2026_09_04_ECONOMY_DIR_FOLD_PLAN_ASTRA.md`
- GLM, `doc_method/` 172 файла в 9 групп → `claudework/agents/knowledge/doc_method/2026_09_04_DOCMETHOD_FOLD_PLAN_GLM.md`
- 12 предложений разводки отчётов → `claudework/agents/knowledge/doc_method/2026_09_04_UNROUTED_REPORTS_PACKAGE_B.md`
- 26 из 61 обещаний готовы к закрытию → `claudework/agents/state/2026_09_04_SELFCHECK_61_EVIDENCE.md`
- 30 доказательных маркеров закрытых решений → `claudework/agents/knowledge/doc_method/2026_09_04_OPEN_LEDGER_EVIDENCE_GAP.md`

---

### Добавлено 2026-09-04 23:57

### Ещё четыре продукта — вердикты

- **Датчик «279 modId вне карт» меряет не то.** Он считает не «не задокументировано», а «не
  процитировано в бэктиках на одном из 24 `MOD_SURFACE.md`». Все 279 **уже упомянуты** где-то в
  knowledge (обход 535 файлов: 279 из 279), а 13 из 37 доменов не могут привязать ничего — у них
  вообще нет `MOD_SURFACE.md`. И число **не может** опуститься ниже 184. Это твоё решение: **§1.438**
  в OPEN, три буквы с ценой, рекомендация C. → `claudework/agents/knowledge/doc_method/2026_09_04_MODSET_279_UNMAPPED.md`
- **Вердикты по дайджест-продуктам 14–45** записаны. Попутно поймана ловушка счёта: новые продукты
  дописываются **сверху**, поэтому нумеровать надо по файлам, а не по позициям — иначе одни разбираются
  дважды, другие теряются. → `claudework/agents/knowledge/doc_method/2026_09_04_DIGEST_VERDICTS_PART2.md`
- **Package C, ещё десять неразведённых отчётов.** Главное не строки, а находка: у части отчётов
  содержимое **уже полностью применено** в живых реестрах. Значит отставание «76 не разведены» —
  частично пробел в бухгалтерии, а не потерянная работа. → `claudework/agents/knowledge/doc_method/2026_09_04_UNROUTED_REPORTS_PACKAGE_C.md`
- **Кросс-семейное ревью 10 коммитов (GLM).** Одна живая находка, и она **уже исправлена**: коммит
  `2d15954` удалил строку реестра для живого файла `ideas/IDEAS_MINING.md`, пообещав в собственном
  сообщении обратное. → `claudework/agents/reviews/CR-20260904-1531f26_GLM_REVIEW.md`

---

### Добавлено 2026-09-05 00:19

### Зеркало доков больше не стирает страницу, которой законно нет на диске

Реальный синк подметал `clone.rglob("*.md")` безусловно, а копировал назад только страницы,
присутствующие **в эту секунду**. То есть страница из `missing` не «оставалась как была» — её
удаляли, коммитили и пушили, а исчезновение лишь сноской упоминали в README. Живое окно, не теория:
`ДАЙДЖЕСТ.md` уезжает в архив каждый раз, когда ты говоришь «прочитал», и возвращается следующим
`append` — синк между этими двумя моментами уносил у тебя ровно ту страницу, которую ты читаешь.

Теперь это `SKIP` с печатью строки, а не молчаливое удаление. **Ретайермент при этом не сломан:**
страница снимается снятием ORDER-строки — тогда имя не публикуется и не числится `missing`, и тот же
проход её удаляет. Синк, который никогда не умеет удалять, был бы так же неправ, как тот, который
удаляет всегда; два из пяти новых тестов существуют ровно чтобы поймать такую перекоррекцию.

Проверено мной независимо, не со слов воркера: 5/5 зелёные с фиксом, **3 из 5 красные против версии
инструмента из `git show HEAD:`** (доказано снятием), 22 прежних `--dry-run` теста не сломались,
`--dry-run` exit 0 и «remote not contacted». Реальный синк не запускался.
→ `claudework/agents/knowledge/doc_method/2026_09_05_DOCS_MIRROR_DELETE_FIX.md`

---

### Добавлено 2026-09-05 00:27

### Ночная вахта: три отчёта, и один из них меняет то, как мы тратим воркеров

- **Семь решений по свёртке `agents/` готовы к твоей букве — §1.439–§1.445 в OPEN.** H2 свёл три
  плана (ASTRA ×2, GLM) в один пакет и нашёл три вещи, которых не было ни в одном из них:
  под свёртываемыми папками лежат **172 не-Markdown файла** (115 из них в `doc_method/`, их не
  посчитал никто) — пока они там, ни одна папка не исчезнет, сколько отчётов ни сворачивай;
  **`retire_to_archive.py` возвращает 0, даже когда отказал по всем строкам** (перепроверил сам по
  исходнику: единственный `return 0` на 243-й, отказы только печатаются) — то есть «сухой прогон
  прошёл» и «ничего не переехало» по коду возврата неразличимы; и **~90 строк `REPORTS.md`
  указывают на файлы, которые свёртка уводит из дерева**.
  ⚠️ И он **отказался** написать одну строку работы: план C подозревал, что таблица топлива U-6 не
  легла в `energy/MODEL_PIPELINE.md` — она легла, строка 159. Не изготавливать работу из
  непроверенного сомнения — ровно то поведение, которое нам нужно.
  → `claudework/agents/knowledge/doc_method/2026_09_05_FOLD_PLANS_DECISION_PACKAGE.md`

- 🔴 **Датчик «отчёты: N не разведены» меряет не то, и это уже третье независимое подтверждение.**
  Он считает, есть ли у файла строка в `REPORTS.md`, а **не дошло ли его содержимое до решения**.
  Из 22 проверенных строк **8 оказались бухгалтерией**, которую датчик вообще не должен был считать.
  Значит от трети до половины «неразведённого» — это учёт, а не потерянная работа. Практический
  вывод: запускать воркера по сырому выводу `reports_gap.py`, не перечитывая сами отчёты против
  живого дерева, — это воспроизводить те же ~35 % впустую каждый раз.
  → `claudework/agents/knowledge/doc_method/2026_09_05_PACKAGES_BC_APPLY_READY.md`

- **Self-check: готовые команды закрытия на каждую строку**, с уже написанной причиной и ссылкой на
  доказательство; мутирующие подкоманды не запускались ни разу.
  → `claudework/agents/knowledge/doc_method/2026_09_05_SELFCHECK_APPLY_TEXT.md`

---

### Добавлено 2026-09-05 00:44

### Все 101 строки реестра без доказательства теперь разобраны — и один гейт красный не по нашей вине

- **Ledger: разбор закончен.** Второй проход добил оставшуюся 71 строку, вместе с первыми 30 это
  все 101. Итого ≈69 готовы к закрытию с уже написанным текстом маркера, ≈13 закрыты, но ничего не
  применялось (или решение живёт только в строке индекса), **9 противоречат живому дереву** — этих
  первый проход не нашёл вовсе, здесь копали глубже, — и 7 требуют игры или живой копии.
  Воркер честно сказал, что две легенды не по одному шаблону и сводить их — моё решение, а не его.
  → `claudework/agents/knowledge/doc_method/2026_09_05_OPEN_LEDGER_EVIDENCE_REST.md`

- **Кросс-семейное ревью 10 коммитов (DeepSeek): approved, ноль находок** — и это не штамп. Каждую
  цифру, id и путь он перевыводил из самих файлов и инструментов, а не из сообщений коммитов; счёт
  рецепт-верификатора воспроизвёл побайтово живым прогоном. Заодно подтвердил, что батч сам
  правильно отчитался о двух своих же ошибках в счёте.
  🔑 **И одно наблюдение он сознательно НЕ записал как находку батча** — правильно сделал:
  `reference/_kubejs_recipes.json` лежит изменённым и незакоммиченным, из-за чего **три V2.1-гейта
  падают** (`v21_gate`, `leaf_value_model --self-test`, `v21_certificates`). Воспроизвёл сам:
  exit 1, `FreshnessError`. Но ни один коммит батча не трогает `kubejs/server_scripts` — это
  штатный fail-closed, а вешать на батч чужой дефект значит обесценить сам гейт.
  **Чинится одной командой** (`python tools/kubejs_recipes.py`), и это безопасно: скрипты чистые,
  `git status` по ним даёт ноль строк. Не запускаю сейчас только потому, что это общий артефакт, а
  в волне живы шесть воркеров — сделаю, когда волна опустеет.
  → `claudework/agents/reviews/CR-20260904-93d06a5_REVIEW.md`

---

### Добавлено 2026-09-05 00:46

> Бриф к тому же ревью — `claudework/agents/reviews/CR-20260904-93d06a5.md` — это моя инструкция ревьюеру, а не продукт: отдельного вердикта в нём нет, вся ценность в ответном файле выше. Отмечаю его только чтобы ворота отпустили вкладку.

---

### Добавлено 2026-09-05 00:57

### Дыра в реестре отчётов: 60 → 16, и цифра предсказана заранее

- **GLM разобрал все 60 файлов, у которых не было строки в реестре, и дал 44 готовых строки
  побайтово.** Вставил, гейт упал с **60 до 16** — ровно та цифра, которую отчёт предсказал до
  вставки как собственный тест приёмки. Проверка назначений осталась 0 провалов по всем четырём
  классам.
  🔑 **Он классифицировал 0 файлов как «исключение»** — то есть отказался придумать себе правило,
  по которому механические `WORKER_DONE` сводки можно не регистрировать, и отправил их в архив.
  Это был лёгкий выход, и он его не взял.
  Оставшиеся 16 — его рекомендация в архив, ничего не вынесено.
  → `claudework/agents/knowledge/doc_method/2026_09_05_SIXTY_UNREGISTERED_REPORTS.md`

- **ASTRA (GPT-6) продолжила свёртку экономики: 40 файлов, суммарно 72 из 158.** 17 условных
  «архив-после-свёртки» и 23 явных блока в этом проходе (суммарно 36 и 36), **ноль немедленных
  снятий**, оставшиеся 86 перечислены по точным путям с указанием следующего.
  Она снова честно назвала границу вместо имитации завершённости — за это и получила продолжение.
  Две её структурные цифры перепроверил сам: в `agents/economy/` ровно **158 Markdown и 50
  не-Markdown** файлов.
  → `claudework/agents/knowledge/doc_method/2026_09_05_ECONOMY_DIR_FOLD_PLAN_ASTRA_PART2.md`

---

### Добавлено 2026-09-05 00:59

### Готовность systems/: карта построена, и три страницы занижены не по делу

Это цель по закону 13 — процент готовности каждой страницы. Ни одна ночная задача до этого их не
трогала, теперь есть карта.

- **Страниц с невоспроизводимым процентом — ноль.** Все семнадцать несут написанную формулу, и
  каждый процент пересчитан заново и сошёлся. Арифметике верить можно.
- 🔴 **Но таблицы ссылаются на уже закрытые решения как на открытые — то есть занижают готовность.**
  Перепроверил сам по индексу: §1.248, §1.270 и §1.283 все закрыты 2026-09-01. Из-за них две из семи
  частичных строк ЭКОНОМИКИ и одна owner-строка ПРОГРЕССИИ держатся за мёртвые ссылки. Пересчёт
  таблиц поставил в очередь, сам не делал: взвешенная таблица owner-facing, её двигают целиком.
- **Куда ночь воркеров реально покупает готовность** (работа, которую можно делать без тебя и без
  игры): ЭКОНОМИКА — 3 позиции, крупнейший рычаг; ТОРГОВЛЯ — 12 неоценённых торговых таблиц, чистое
  авторство; ЛУТ и МЕТАЛЛУРГИЯ — по 2.
- 🔑 **И ответ, который стоит знать до того, как ругать цифру: НАЁМНИКИ 8 % — это не запущенность, а
  ожидание.** Четыре из шести недостающих строк стоят прямо за четырьмя твоими открытыми решениями;
  честной позиции «сделать сегодня» у страницы нет вообще. МАТЕРЛОД 59 % — то же: конфиг завершён,
  осталась одна игровая приёмка.
  → `claudework/agents/knowledge/doc_method/2026_09_05_SYSTEMS_READINESS_BLOCKERS.md`

---

### Добавлено 2026-09-05 01:03

### Отчёт годичной давности говорил, что удалил множитель — не удалил ни одной строки

Множитель 1.15 (`LABOR`) всё ещё объявлен и всё ещё умножается в `parse_kubejs.py` (:18, :276, :308)
и `cost_jar_recipes.py` (:33, :232) — все пять строк, которые отчёт 2026-08-14 описывает как
удалённые. Правки делались в worktree `ds/wave0-ports`, которого больше нет: неслитая ветка, не
выдумка.

🔑 **Но воркер сделал главное — спросил, кто эти инструменты вообще использует. Никто.** Оба печатают
в stdout и не импортируются ничем (перепроверил сам: все оставшиеся упоминания — проза, docstring или
комментарий). Сам 1.15 принадлежит **удержанному** режиму L1/BB: `tools/v21.py:9` прямо называет эту
константу retired, а в `economy/LAWS_AND_MODEL.md` числа 1.15 нет вообще.

**Значит это дефект документации, а не неверная цена** — и различие тут стоит всего, потому что цена
у этих двух вещей совершенно разная.

Что сделал сразу: повесил на отчёт баннер «этот порт не приземлился» с построчной сверкой. Отчёт,
который описывает несделанное изменение, отмывает себя в правду в тот момент, когда кто-нибудь
свернёт его в системный отчёт — это ровно закон 15.
Что решаешь ты: **§1.446**, три буквы с ценой, рекомендую **B** (вынести оба инструмента в архив,
тем же коммитом удалив пять строк).
→ `claudework/agents/knowledge/doc_method/2026_09_05_LABOUR_MULTIPLIER_LIVE_AUDIT.md`

---

### Добавлено 2026-09-05 01:12

### Девять строк реестра противоречили живому дереву — и одна из них живой десинк

Разбор девяти строк, где реестр говорит одно, а дерево другое. Две строки исправил прямо в `OPEN.md`,
четыре оказались дефектами самого пака, две воркер честно оставил как «не могу сказать», назвав обе
конфликтующие записи вместо того, чтобы выбрать за тебя.

- 🔴 **§1.280: джарник Biolith, который по твоему решению должен быть ТОЛЬКО клиентским, стоит на
  сервере.** Это не расхождение текста — это ровно тот десинк, который закон 12 называет «не
  осторожность, а баг, который вылезет у игрока». Снятие требует твоего слова.
- **§1.309 ссылалась на доказательство, которого никогда не существовало** —
  `agents/archive/blitz/2026_08_30_BLITZ_CLOSED.md` отсутствует во всей истории git. Переписал ссылку
  на реальный §1.342.
- **§1.283 была помечена «подтверждено», а применена не была.** Живые файлы до сих пор несут мёртвые
  id `constructionwand:iron_wand`/`diamond_wand` — в **трёх** местах, не в двух, как нашёл воркер.
  Задача `CONSTRUCTIONWAND-FTBQ-MIGRATION` (P0) ждала именно этой строки.
- **§1.110: два твоих правила про `toomanyrecipeviewers` противоречат друг другу**, и ни одно не
  отменяет другое по §-номеру. Нужен твой номер, который скажет, какое старше.

⚠️ И один разбор между моими же воркерами: L6 посчитал §1.283 закрытым и вывел, что готовность
ПРОГРЕССИИ занижена; L7 показал, что подтверждение ложное. Верны оба наполовину — решение **вынесено,
но не исполнено**, поэтому вывод L6 именно по этой строке я снял.
→ `claudework/agents/knowledge/doc_method/2026_09_05_LEDGER_NINE_CONTRADICTIONS.md`

---

### Добавлено 2026-09-05 01:16

### Я завёл задачу на работу, которая была сделана сутки назад — воркер это поймал

Ночью я записал строку «U-8 не приземлился, перенести его текст в `metallurgy/MOD_SURFACE.md`». Он
приземлился **2026-09-04, коммит `d06b078`** — проверил по `git show --stat` и по самой странице.
Строку я завёл из двух отчётов, **ни один из которых не был сверен с git**: ровно тот класс ошибки,
который эта же ночь ловила у других. Снял как устаревшую, с причиной.

🔑 **И приземлившийся вердикт оказался СИЛЬНЕЕ исходного текста U-8:** netherite — **KEEP, а не
STRIP**, потому что тег `forge:ingots/netherite` поставляется универсальным джарником Forge (слой
`libraries/`), которого перечисление по одной папке `mods/` не видит в принципе. То есть вставить
исходный текст сейчас было бы **регрессией**. Независимая перепроверка сошлась и с оригиналом, и с
приземлённой страницей.

- **Маркеры-остатки в `doc_method/`: 121 файл, 86 к удалению / 27 оставить / 8 неясно.** Воркер
  держал правило «имя файла само по себе не даёт права удалять» — гейтом была проверка, что
  породивший диспатч действительно завершён.
  🔴 **Но обратимость он не проверял, а я проверил: в git отслеживаются 10 из 99 маркеров.** Значит
  удаление большинства **необратимо** — ни reflog, ни истории. Это не уборка, а односторонняя
  операция; переписал задачу под перенос в архив вместо `rm`, и только при нуле живых воркеров.
  ⚠️ Класс к тому же саморастущий: за время одного диспатча счёт прошёл 115 → 119 → 121.
  → `claudework/agents/knowledge/doc_method/2026_09_05_U8_LANDING_AND_MARKER_RESIDUE.md`

---

### Добавлено 2026-09-05 01:27

### Реестр решений: 101 строка без доказательства → 32, и попутно найден баг парсера

Два прошлых отчёта нашли доказательства для всех 101 строки и **ни один не написал маркер в его
литеральном виде**, поэтому вставить было нечего и датчик не двигался. Этот воркер написал их: 68
маркеров вставлены, **101 → 32 без доказательства, 0 сломанных**, «готовы к архиву» 19 → 78.
Каждый `applied=<sha>` я проверил на существование в git до вставки — 68 из 68 разрешились.

🔴 **Но настоящая находка — не маркеры, а баг в самом инструменте.** `open_ledger.py` требовал цифру
сразу после `## `, поэтому **114 секций из 373**, написанных как `## §1.384 — …`, он не видел вообще.
И хуже, чем «не видел»: текст под таким заголовком приклеивался к телу **предыдущей** секции — то
есть маркер, поставленный туда, был бы приписан **чужому решению**, и `verify` подтвердил бы
утверждение не про ту строку. Это ровно тот отказ, ради невозможности которого реестр и заведён.

Починил инструмент (коммит `935b361`), а **не** 26 заголовков, как предлагал второй сценарий отчёта:
править owner-facing заголовки под парсер — это наоборот, формат реестра истина, а инструмент её
читает. Из-за этого выбора итог 32, а не 59 из первого сценария.

⚠️ И 32 строки воркер сознательно оставил без маркера, назвав по каждой, чего не хватает, вместо
того чтобы угадать значение в реестр, весь смысл которого — что `verify` может его проверить.
→ `claudework/agents/knowledge/doc_method/2026_09_05_LEDGER_MARKER_LINES.md`

---

### Добавлено 2026-09-05 01:31

### Поправка к тому, что я говорил тебе дважды: готовность НЕ занижена

Я дважды сказал, что цифры готовности ЭКОНОМИКИ и ПРОГРЕССИИ занижены из-за ссылок на закрытые
решения. **Пересчёт это не подтвердил: ни один процент не двигается** — ЭКОНОМИКА 50 %,
ПРОГРЕССИЯ 36 %.

Почему я был неправ: §1.270 и §1.283 **не цитируются ни одной строкой таблицы готовности** — они
живут в таблице решений и в прозе. А единственная строка на §1.248 обязана остаться 0,5, потому что
её второй блокер — runtime-фальсификатор, ждущий игровой приёмки. Вывод предыдущего воркера я принял
на веру и передал тебе, не проверив, к какой именно таблице относятся цитаты.

🔴 **Но дефект нашёлся настоящий, просто другой и дороже:** три мёртвые цитаты **просили тебя
ответить второй раз** на то, что ты решил 2026-09-01 — про якорь медианы лута и про вариант Rowan.
Ровно тот класс, который шаблон страницы называет «он ответит второй раз и разозлится». Убрал обе
строки, буллет «Rowan не проходит гейт» заменил (перепроверил сам: `sanity_check.py` даёт **0
BLOCKING**), счётчик вопросов 9 → 8.

Строку про Construction Wand переформулировал вместо удаления: замены **выбраны**, но **не применены**
— нужно только твоё «да» на правку при закрытой игре.

И в обе страницы добавил стоячий шаг: строка считается закрытой, только если решение **и вынесено, и
исполнено на живой стороне**, проверено открытием самого файла.
→ `claudework/agents/knowledge/doc_method/2026_09_05_READINESS_RECOMPUTE_ECONOMY_PROGRESSION.md`

---

### Добавлено 2026-09-05 01:48

### Гейт реестра отчётов: 60 → 0 за ночь

Последние 16 файлов вынесены в архив вне репозитория, и `verify_reports_register.py --gate` теперь
даёт **OK, ноль расхождений** — ровно тот ноль, который отчёт предсказал заранее.

🔑 **Чистые 16 из 16 «в архив» — это ровно тот ответ, от которого бриф предостерегал**, поэтому я его
проверял, а не принял. Воркер открыл все шестнадцать целиком (941 Б – 6,7 КБ, ничего не выборочно),
прогнал две независимые проверки — каждое целое число против соседнего `ARCHIVE_REVIEW.md`, затем
каждую содержательную формулировку, — и числа, которых он не нашёл у соседа, оказались счётчиками
охвата самих обзоров, а не измерениями. Он же разобрал три противоречащих прежних вердикта вместо
того, чтобы их обойти, и проверил, что во внешнем архиве копий ещё нет.

**Что сделало перенос безопасным, а не односторонним, добавил я: все 16 отслеживаются в git**, значит
история хранит каждую версию — это и есть собственная гарантия закона 14. Каждую копию сверил по MD5
до удаления оригинала; ни один внешний файл не перезаписан.
→ `claudework/agents/knowledge/doc_method/2026_09_05_SIXTEEN_ARCHIVE_CANDIDATES.md`

---

### Добавлено 2026-09-05 02:07

### Разводка пакетов B и C применена: неразведённых отчётов 54 → 37

Шесть новых решений (§1.447–§1.452), одиннадцать строк работы и пятнадцать строк реестра — всё
вставлено. Проверка назначений: **ноль провалов по всем четырём классам**, гейт снова OK.

🔑 **Перенумерация и была смыслом задачи.** Шесть §-номеров, которые предлагал исходный отчёт
(§1.436–§1.442), **все оказались заняты посторонним содержимым** за то время, пока он писался — ровно
то столкновение, о котором он сам предупреждал. Воркер перевывел каждый номер по живому файлу.
И он не выдал вставляемых строк для того, что уже применено или устарело: записать их второй раз —
это и есть отказ, ради предотвращения которого сводка делалась.

⚠️ Одна вещь для протокола: я вставил `REPORTS.md` первым, и гейт на минуту показал 17 провалов
назначений — потому что эти строки ссылаются на секции `OPEN.md` и id `TODO.md`, которых ещё не было.
**Адресаты должны ложиться раньше строк, которые на них ссылаются.**
→ `claudework/agents/knowledge/doc_method/2026_09_05_PACKAGES_BC_PASTE_SET.md`
