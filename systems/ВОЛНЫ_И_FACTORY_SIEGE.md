# Волны и Factory Siege

> **Главный мод:** Factory Siege + KubeJS поверх Gateways to Eternity / GatewaysJS; The Hordes остаётся только legacy/test-контуром · **Готовность: 34 %**
> **Больше всего мешает:** live jar отключён; свежий исходник ABI-проверен и pinned к Sculk Horde 0.12.7, но ещё не прошёл изолированный pack-smoke.
> **Ждёт твоего слова:** только проектные решения в `factory_siege/OPEN.md` (§1.302 и §1.303 закрыты) · **Проверить в игре:** 3 контура + `SIEGE-JAVA-RUNTIME-SMOKE`.

## Куда идём — каким это будет на 100 %

Фабрика создаёт не абстрактный «уровень угрозы», а понятный цикл: производственная активность и шум
накапливают внимание, игрок видит сигнал опасности, готовит оборону и переживает ограниченную волну,
после чего возвращается к производству. **Factory Siege** — единственный кандидат на этот контур:
его исходник уже содержит каналы air/noise/heat/electromagnetic/arcane, учёт волн и собственные
таблицы противников. Защита должна быть промышленным sink'ом — боеприпасы, жидкости, питание,
стены и логистика — а не вторым оружейным модпаком.

Это отдельная система, а не подраздел «Технологий»: технологии отвечают за то, *как* строится
фабрика, а здесь игрок отвечает на вопрос, *что происходит, когда фабрика стала заметной миру*.
Связанные, но не смешиваемые границы: прогрессия открывает доступ к оборонным средствам, торговля
может награждать за контракты. По `raidsenhanced` вопрос закрыт: **`OPEN.md` §1.303 решён блицем
2026-08-30 (Q5R-A)** — оставить родной гейт Required Raid Omen уровня 3, подстроить численность
рейдов и переиспользовать одного-двух подходящих мобов в наших фабричных волнах; работа —
`RAIDSENHANCED-OMEN3-AND-SIEGE-ROUTING`. Прежнее направление «снять его с ванильных рейдов
датапаком» этим рулингом заменено.

Не создаём второй счётчик угрозы в InControl, KubeJS или другом моде. Не включаем естественные
волны до измерения полного цикла, не возвращаем разрушение блоков через дефолтные настройки и не
выдаём награду обычным лутом/XP без отдельной проверки цены и идемпотентности.

## Где мы сейчас

| подсистема | вес | доля | состояние | чем доказано |
|---|---:|---:|---|---|
| Исходный мод и таблицы профилей волн | 3 | 0,5 | 🔧 частично | `factory_siege/src/main/`: `HordeIntegration`, `FactorySiegeCommands`, `FactorySiegeTestSuite` и `data/factory_siege/horde_data/tables/` существуют; это доказательство исходника, не игрового запуска. |
| Каналы фабричного внимания | 3 | 0,5 | 🔧 частично | Две разные реализации. Живая: `kubejs/server_scripts/90_Siege_Core.js:10-33` весит 13 механических источников шума и 5 горящих эмиттеров и даёт два маршрута глушения (орб ×0.35 на машину; короб→шумовой узел BFS ×0.35) — скрипт загружен на работающем сервере (`logs/latest.log:157-158`). Неотгруженная: `PollutionChannel.java:4-8` перечисляет пять каналов air/noise/heat/electromagnetic/arcane, рядом `CreatePollutionSources` и `Ae2SignatureSavedData` — это доказательство исходника, не игрового запуска. |
| Загрузка Factory Siege в действующей сборке | 3 | 0 | ❌ нет | Проверка 25.08: оба игровых пути содержат `factory_siege-0.0.11-test.jar.disabled`, а активен `sculkhorde-1.20.1-0.12.7.jar`. Архивный jar требовал ровно 0.12.6; исходник сейчас exact-pin'нут к ABI-проверенной 0.12.7 и успешно собран, но ещё не загружался в pack. |
| Автоматическое «внимание → волна» и видимый сигнал рейда | 3 | 0,5 | 🔧 текущий Hordes-спайк; Gateway-переход не начат | `90_Siege_Core.js` сейчас делает накопление внимания, три телеграфа и Hordes-запуск; он загружен, но инертен до `!siege on`. **Решение владельца 28.08:** это становится KubeJS-директором Gateway-owned волн: Gateways to Eternity + GatewaysJS — основной production spawn, а Hordes не должен запускаться или маскироваться порталом. Точный AI-мост — Java listener base `GateEvent.WaveEntitySpawned`: он создаёт Gateway-instance и даёт живому raider цель из реестра, KubeJS не дублирует этот ledger. До проверки моста Hordes не удаляется. Enhanced Hordes остаётся независимым ambient AI для подходящих типов Gateway-мобов, но без multiplying/general breaking. **Gateway-стек уже физически стоит на обеих сторонах** (перечисление 2026-08-31: Gateways 4.2.6, GatewaysJS 1.6, три аддона, Sculk Horde 0.12.7 — `knowledge/waves_factory_siege/INDEX.md`), но в KubeJS-дереве нет ни одного вызова создания Gateway (`Gateway.registerJson`/`GatewaysEvents.*` — grep 31.08 пуст), и runtime не принят — это не игровая готовность. |
| Безопасный состав, лут и производительность волны | 2 | 0,5 | 🔧 конфиг и рецепты выложены, лута нет | Безопасность волны уже задана конфигом и совпадает во всех трёх копиях: `hordesCommandOnly = true`, `enableHordeBlockBreaking = false`, `enableHordeMentality = false` (закрытые решения `OPEN-HORDES-01` и `OPEN-BREAK-01`). Лута нет вообще: `raiders_t1/t2` — таблицы спавна The Hordes, а не loot table. §1.302 решён 28.08: KubeJS-рецепты для баррикады, короба и реле идут после латуни/Create; сейсмограф и одноразовый орб — после магии. Гибридная граница: Java владеет физическими предметами и локальными эффектами, KubeJS — директором внимания/волн и рецептами; одно и то же действие не живёт дважды. **Выложено:** `90_Siege_Core.js`, `91_Siege_Counterplay_Recipes.js` (8 регистраций рецептов) и `90_Siege_Blocks.js` хеш-идентичны в мастере/клиенте/сервере (сверка 2026-09-03) — прежняя оговорка «только в мастере» устарела. Остальное — `factory_siege/OPEN.md`: roster, loot, claims, geometry, perf и milestones решаются по отдельности. ⚠️ Отдельная экспозиция: 19 живых jar-рецептов `gateways:gate_pearl` (инвентаризация 31.08) — снятие пока на бумаге |
| Связка с Raids Enhanced | 2 | 0 | ❌ решено, не реализовано | `OPEN.md` §1.303 ✅ (блиц 2026-08-30, Q5R-A): оставить родной Omen-3 гейт, подстроить численность, переиспользовать 1–2 мобов в наших волнах. Работа `RAIDSENHANCED-OMEN3-AND-SIEGE-ROUTING` не начата; загруженный ключ и фактические спавны не проверены |

**Готовность раздела: 34 %** — по единой шкале `agents/docs_audit/2026_08_29_SYSTEMS_READINESS_SINGLE_SCALE.md` (вес 3/2/1 · доля 1,0 / 0,5 / 0 · `Готовность = Σ(вес × доля) / Σ вес`): **Σ(вес × доля) = 5,5 при Σ вес = 16 → 34 %.** Три несущие строки из шести — половинные: исходник и таблицы профилей лежат в мастере, KubeJS-директор загружен на работающем сервере (`logs/latest.log:157-158`) и инертен до `!siege on`. Нулём стоит то, без чего это не система: джарник `factory_siege` не загружен ни одной стороной (перепроверено 2026-09-01: в живых `mods/` обеих сторон его нет), и связка с Raids Enhanced решена (§1.303 ✅), но не реализована. Замер строк — таблица выше и `agents/factory/2026_08_28_GW_AI_03_CARRIER_GUARD.md`.

## Что нужно от тебя

### Решения

| что решить | где |
|---|---|
| Бэкенд, классы рейдов, лут, anchor/claim/geometry, производительность и вехи | [`factory_siege/OPEN.md`](../../factory_siege/OPEN.md) — проектный реестр; эта страница не создаёт вторую копию. Оба общерегистровых вопроса закрыты: §1.302 ✅ 28.08 (рецепты — KubeJS, гибридная граница), §1.303 ✅ 30.08 (Q5R-A, см. «Куда идём»). |
| Когда возвращать Factory Siege из отложенной фазы в реализацию | `TODO.md`: Factory Siege и волны находятся после базовой фазы; авторская страница не меняет этот порядок сама. |

### Проверить в игре

| что проверить | где подробно |
|---|---|
| Совместимый test-jar загружается на обеих сторонах без Forge dependency error. | `TODO.md` `SIEGE-JAVA-RUNTIME-SMOKE` (открыт); до совместимого jar запускать волну нельзя. |
| Один вручную вызванный профиль волн не трогает неразрешённые блоки/блок-сущности и корректно завершается после выхода носителя. | `factory_siege/INGAME_CHECKLIST_2026_08_09.md` и проектный `OPEN.md`. ⚠️ Путь исправлен 2026-08-27: `INGAME_CHECKLIST.md` без даты не существует (`ls` → exit 2). |
| Полный цикл «сигнал → оборона → волна → награда/очистка» измерен по MSPT и числу сущностей. | `factory_siege/OPEN.md` `OPEN-PERF-01` и `OPEN-LOOT-01`. |

## 🔴 Что сломано

| что сломано | чем доказано | что это значит для игрока |
|---|---|---|
| Factory Siege выключен из обеих игровых копий, а старый jar имеет pin-конфликт Sculk Horde. | Листинг 25.08 по путям из `tools/paths.py`: archived jar в `disabled_mods_2026_08_23` требует ровно 0.12.6, тогда как активен 0.12.7. Новый source jar pinned к 0.12.7 после ABI-проверки, но pack-smoke отсутствует. | Система не присутствует в игре. Возвращать старый jar нельзя; возвращать новый в живой мир тоже нельзя до изолированной проверки загрузки. |

Других runtime-дефектов здесь не заявлено: исходник и старые отчёты не заменяют свежий запуск
совместимой пары jar.

## В работе

Сейчас контур находится в отложенной фазе вместе с волнами противников. Текущая ночная волна не
включает jar и не меняет его исходник: ближайшая техническая работа — сначала восстановить
совместимый dependency-набор в отдельной тестовой копии, затем провести один контролируемый
ручной профиль, а не включать натуральные волны в живом мире.

## На чём это стоит

- Factory Siege — собственный проект с исходником, проектным `OPEN.md`, `TODO.md` и игровым
  чеклистом; системная страница показывает его место в паке, а не подменяет эти файлы.
- `META-INF/mods.toml` задаёт mandatory dependency ranges: часть из них точные; архивный jar
  требовал Sculk Horde 0.12.6, а текущий source точно требует 0.12.7 — единственную ABI-проверенную
  версию. Совместимость artefact всё равно проверяется загрузкой отдельной копии, а не метаданными.
- **§1.303 ✅ (блиц 2026-08-30, Q5R-A):** Raids Enhanced остаётся в ванильных рейдах за родным гейтом
  Required Raid Omen 3; числа рейдов подстраиваются, один-два подходящих моба переиспользуются в
  наших волнах. Работа — `RAIDSENHANCED-OMEN3-AND-SIEGE-ROUTING`; проверка — загруженный ключ и
  фактические спавны. 🪦 Прежняя формулировка `RAIDS-FACTORY` («убрать из ванильных рейдов
  датапаком») заменена этим рулингом.
- `claudework/agents/config/2026_08_19_SIEGE_STACK_CONFIG_DOSSIER.md` *(archived 2026-09-03)* объясняет, почему Tweaks и
  защита от block breaking нельзя снимать/переключать вслепую: ошибка может снова вооружить
  разрушение блоков базы при загрузке.

## Чем проверить

```powershell
# Сначала получить реальные пути четырёх копий, затем увидеть активный и disabled наборы.
python bountiful_wizards_REMAKE/tools/paths.py

# В исходнике Factory Siege проверить точные обязательные диапазоны до любой смены jar.
rg -n -C 2 'modId="(hordes|enhanced_hordes|enhanced_hordes_tweaks|sculkhorde)"|versionRange' `
  factory_siege/src/main/resources/META-INF/mods.toml

# Исходник должен собираться до создания test-jar; это не доказательство совместимости с живой сборкой.
factory_siege/gradlew.bat build

# После отдельного test-deploy — только игровая приёмка из project INGAME_CHECKLIST,
# а не включение натуральных волн в существующем мире.
```

Норма: точные dependency ranges совместимы с test-copy, Forge загружается, ручная волна ограничена
профилем и завершается без повреждения запрещённых целей. До этого страница описывает направление и
статус, а не разрешает включение Factory Siege.
