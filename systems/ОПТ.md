# Оптовые поставки

> **Главный мод:** Wares · **Готовность: 37 %**
> **Больше всего мешает:** stock-договоры всё ещё выдают изумруды, а награда и цена первого authored-пилота не выведены из нашей экономики.
> **Ждёт твоего слова:** 0 вопросов · **Проверить в игре:** 5 пунктов

## Куда идём — каким это будет на 100 %

**Wares** — не второй магазин и не альтернативная касса. Это отдельный маршрут для крупных тематических поставок: игрок получает договор, производит заметную партию нужного материала, упаковывает и сдаёт её через delivery table/Packager либо вручную, а затем получает заранее определённую награду, согласованную с прогрессией. Первый контур — два пробных производственных заказа на `create:shaft`: один на одну большую партию, второй — на следующую по объёму ступень. Их точные объём, награда и источник договора берутся из одной ценовой базы, а не из stock-таблицы мода.

Договоры authorятся datapack-таблицами `kubejs/data/wares/loot_tables/agreement/...`; при необходимости награда за завершение привязывается только к подтверждённому событию Wares. Игрок видит один понятный выбор: взять большой заказ, сделать именно тематическую продукцию и закрыть поставку. Повторяемая продажа сырья за изумруды, отдельная валюта Wares и награды, обходящие доски Bountiful, не выпускаются.

Из оригинала вырезается stock cash-out: сейчас Wares способен просить обычные фермерские товары и платить vanilla-изумрудами без доски, таймера или репутационного контура. Остаются сами доставка, коробки, Packager и склады — это полезная физическая форма опта, а не новый рынок.

## Где мы сейчас

| подсистема | вес | доля | состояние | чем доказано |
|---|---:|---:|---|---|
| Мод и физический delivery-loop на обеих сторонах | 2 | 1,0 | ✅ готово | `agents/economy/2026_08_25_WARES_SOURCE_INVENTORY.md`: одинаковый runtime-jar, включены delivery table, Packager, ручная сдача, склады |
| Полная datapack-поверхность договоров | 2 | 0,5 | 🔧 описана, но не выложена | `agents/economy/2026_08_26_WARES_AUTHORING_SPEC.md`: 20 village tables, wandering-trader tables и правило сохранения `random_sequence` |
| Граница системы и защита от второго рынка | 1 | 1,0 | ✅ определена | `agents/WARES_AND_SDMSHOP_AUDIT_2026_08_16.md`; этот документ |
| Экономическая модель награды и цен первого пилота | 3 | 0,5 | 🔧 частично | есть правило «не emerald и не самодельная валюта»; нет выведенного входа/выхода для shaft-заказов |
| Два authored shaft-пилота | 2 | 0 | ❌ нет | в `kubejs/data/wares/` нет договорных override; строка `WARES-WHOLESALE-SYSTEM-PAGE-AND-PILOT` в `agents/registers/TODO.md` |
| Полное вытеснение stock emerald-договоров | 3 | 0 | ❌ нет | нет всех 20 replacement tables; `WARES_AUTHORING_SPEC.md` запрещает частичный rollout |
| Технические smoke-gates и игровая приёмка | 2 | 0 | ❌ нет | ещё не проверены precedence datapack и реальный completion event в отдельной test-copy |

**Готовность раздела: 37 %** — по единой шкале `agents/docs_audit/2026_08_29_SYSTEMS_READINESS_SINGLE_SCALE.md` (вес 3/2/1 · доля 1,0 / 0,5 / 0 · `Готовность = Σ(вес × доля) / Σ вес`): **Σ(вес × доля) = 5,5 при Σ вес = 15 → 37 %.** Единицы стоят у физического delivery-loop на обеих сторонах и у границы системы (защита от второго рынка). Половина — у спецификации договоров: она описана, но в `kubejs/data/wares/` нет ни одного override, и у экономической модели первого пилота. Нулём — два authored-пилота, вытеснение всех 20 stock-таблиц и игровая приёмка. Замер: `agents/economy/2026_08_25_WARES_SOURCE_INVENTORY.md`, `agents/economy/2026_08_26_WARES_AUTHORING_SPEC.md`.

## Что нужно от тебя

### Решения

Сейчас ничего: назначение Wares как оптового маршрута уже задано. Два пробных shaft-заказа не получают числа, валюту или награду из этой страницы: сначала нужен подтверждённый экономический вход текущей ревизии торговых поверхностей.

### Проверить в игре

| что проверить | где |
|---|---|
| Отделить физику штатной доставки Wares от будущей экономики: example agreement, ручная сдача, Packager, завершение и источник договора. Это пять действий одного smoke-loop; stock-награда не принимается как цена. | `ИГРОВОЙ_ЧЕКЛИСТ.md`, «Wares — отдельный технический smoke delivery-loop» |

## 🔴 Что сломано

| дефект | чем доказано | что ломается у игрока |
|---|---|---|
| Stock Wares остаётся параллельным emerald cash-out | `agents/economy/2026_08_25_WARES_SOURCE_INVENTORY.md`: plains contract просит 6–14 wheat и платит 3–8 emerald; override отсутствует | можно продавать обычное сырьё вне нашей ценовой дисциплины и без тематического производственного заказа |

## В работе

Живого Wares-изменения нет. Phase-1 ревизии в
`agents/trade/2026_08_28_NPC_TRADE_CENSUS_AND_CANON.md` подтвердила stock emerald legs и
возможность конверсии, но не установила цену первого shaft-договора. Эта страница фиксирует
границу пилота; она не является скрытым config-изменением и не создаёт второй backlog.

## На чём это стоит

- `agents/WARES_AND_SDMSHOP_AUDIT_2026_08_16.md`: Wares рекомендован как datapack-authorable mass-delivery spike; запрет — второй магазин, квестовая валюта и coin mint.
- `agents/economy/2026_08_25_WARES_SOURCE_INVENTORY.md`: Wares реально загружен на dedicated server и client; pack-side override отсутствует, а безопасный рычаг — datapack выше jar.
- `agents/economy/2026_08_26_WARES_AUTHORING_SPEC.md`: сначала отдельно доказать datapack precedence и completion event; до этого не менять startup-config и не выкатывать неполный набор таблиц.
- `agents/registers/TODO.md`, `WARES-WHOLESALE-SYSTEM-PAGE-AND-PILOT`: два shaft-пилота следуют после доказанной цены/валюты, а не заменяют этот шаг.

### Безопасный малый пилот (не выполнен)

Это **технический**, а не экономический пилот: в отдельной test-copy один marker-override с тем же
table id доказывает приоритет datapack над jar, затем side-effect-free listener даёт ровно один
log-marker на completion одного example agreement. Он не меняет `wares-common.toml`, не содержит
цену, валюту, материальную награду или KubeJS-награду и не попадает в существующий мир. Любая ошибка
останавливает пилот. Только два зелёных gate разрешают отдельно подготовить полный набор из 20
сохранённых table replacements и лишь затем обсуждать два shaft-договора; один или пять частичных
override не разрешены.

## Чем проверить

```powershell
# Runtime Wares на обеих игровых сторонах: нормой являются одинаковые имя и SHA-256.
$packPaths = python bountiful_wizards_REMAKE/tools/paths.py
$client = (($packPaths | Select-String '^CLIENT\s').Line -replace '^CLIENT\s+','')
$server = (($packPaths | Select-String '^SERVER\s').Line -replace '^SERVER\s+','')
Get-ChildItem "$client\mods", "$server\mods" -File | Where-Object Name -match '(?i)wares' |
  Get-FileHash -Algorithm SHA256

# Authored override появится только полным набором: до пилота команда печатает пустой результат.
Get-ChildItem bountiful_wizards_REMAKE/kubejs/data/wares/loot_tables/agreement -Recurse -File -ErrorAction SilentlyContinue

# До технического пилота в MASTER нет ни listener, ни authored agreement. Норма — пустой вывод.
rg -n 'WaresEvents|agreementCompleted|delivery_agreement|sealed_delivery' bountiful_wizards_REMAKE/kubejs

# Перед выпуском JSON обязан быть валидным, а полный набор не должен оставить stock emerald output.
Get-ChildItem bountiful_wizards_REMAKE/kubejs/data/wares/loot_tables/agreement/village -File |
  ForEach-Object { Get-Content $_ -Raw | ConvertFrom-Json | Out-Null }
```

Нормой выпуска станет: оба технических smoke-gate пройдены в отдельной test-copy, authored-таблицы закрывают весь stock cash-out, два shaft-договора имеют доказанные вход/награду, и один полный delivery-loop подтверждён в игре.
