# Торговля

> **Главный мод:** Bountiful · **Готовность: не измерена**
> **Больше всего мешает:** ассортимент и выкуп большинства торговых источников ещё не сведены к одному проверяемому правилу.
> **Ждёт твоего слова:** 5 вопросов · **Проверить в игре:** 3 пункта

## Куда идём — каким это будет на 100 %

**Bountiful** остаётся компактным ядром торговли: игрок берёт тематический декрет на доске, добывает или производит требуемое, сдаёт его и получает осмысленную награду. Доски несут сложные, сюжетные и многоступенчатые обмены; несколько выбранных живых NPC могут давать только простые базовые сделки за нашу валюту, первым кандидатом остаётся Beekeeper. Это решение владельца, а не дизайн по умолчанию мода — `OPEN.md` §1.217.

Поверхность ограничивается вручную authored-пулами Bountiful, KubeJS-заменами datapack-таблиц и точечными post-registration правками сделок. Игрок может найти торговца, увидеть понятный ассортимент и совершить обмен без скрытого второго рынка; каждый живой источник либо входит в ту же проверяемую ценовую дисциплину, либо выключен/не выпускается. Цены и их вывод принадлежат [ЭКОНОМИКЕ.md](ЭКОНОМИКА.md): здесь фиксируются только торговцы, их пул и полнота ассортимента.

Вырезаны дефолтный рынок Bountiful и массовое заселение жителями: они создают параллельную витрину, которую нельзя считать тематической прогрессией. Самостоятельный магазин Superior Shop владелец уже отклонил, а его jar снят 25.08; Bountiful остаётся единственной досочной витриной. **Wares** уже работает на обеих игровых сторонах как отдельный маршрут поставки, но его stock emerald-оплаты пока не приняты: будущие договоры не становятся второй валютой или второй витриной.

## Где мы сейчас

| подсистема | состояние | чем доказано |
|---|---|---|
| Bountiful: тематические декреты вместо дефолтного рынка | ✅ готово | `OPEN.md` §1.217; живые JSON-пулы в `config/bountiful/bounty_pools/` |
| Инвентаризация торговых поверхностей | ✅ готово | статический census jar и классов; `agents/codex/2026_08_18_MODDED_TRADER_ENUMERATION_REPORT.md` дополнен проверкой текущих client/server jars |
| Bountiful-набор, уже переписанный по ценам | ✅ есть набор | все пары декретов перечислены ниже; размер набора всегда печатает команда из раздела 7, а не эта страница |
| Datapack-переписывание внешних торговцев | 🔧 частично — **3 из 29** | вся data-driven поверхность пака = 29 таблиц в двух джарниках (`goblintraders` 9, `supplementaries` 20); наши три замены байт-в-байт совпадают в MASTER/CLIENT/SERVER. Достижимых естественным спавном и всё ещё без override — **12** |
| Живые NPC и хардкодные сделки | ❌ нет нашего ассортимента — **и при этом чужой рынок уже живой** | `OPEN.md` §1.217 разрешает узкий путь, но Beekeeper ассортимента не получил (`OPEN.md` §1.222a G-1…G-5 открыты). Одновременно `visiting-villagers-common.toml` (одинаков на CLIENT/SERVER/mirror) держит `visitorSpawnPercentage = 100`, `defaultVisitorTradeChance = 40`, `maxWanderingVisitors = 2`, и ни одно правило InControl их не гасит |
| Полнота и PRT-проход по всем торговцам | ❌ нет | нарушение у Red Merchant воспроизводится точно (вход 4×115=460, выход 4×203=812), но оно **латентное**: `supplementaries:red_merchant` запрещён к спавну в `config/incontrol/spawn.json` с 06.08. Полного прохода по data-driven и Java-таблицам нет |
| Заказы на зачарованные книги (NBT `lvl`) | 🔧 исправлено в мастере, **не выложено** | 62 записи с `lvl:N` (IntTag) вместо `lvl:Ns` (ShortTag) — Гидеон 5, Векс 26, Брамвелл 31 — всё ещё живут в CLIENT и SERVER; в MASTER их ноль. `NbtUtils.compareNbt` сравнивает классы тегов раньше значений, поэтому заказы Гидеона и Векса на книгу в живом мире закрыть нельзя. Синк ждёт закрытой игры |
| Отдельная магазинная витрина Superior Shop | 🪦 снята | `OPEN.md` §1.250; client jar/runtime config перенесены в обратимый backup 25.08 |
| Оптовые поставки / Wares | 🔧 active, не authored | одинаковый `wares-1.20.1-1.2.10.jar` загружен на dedicated server и client; stock agreements, Wandering Trader и warehouse generation включены, но pack-side override/listener отсутствует |
| Проверка игроком фактических офферов и миграции старых merchant NBT | ❌ нет — и одного запуска мира тут мало | `OPEN.md` §1.144 — правило M2 ✅ **вынесено** 19.08, но в той же таблице исполнения оно стоит «spec only · реализация не начата». Пока миграция не написана, проверять в игре нечего; запуск мира закрывает только первую половину строки |

**Готовность раздела: не измерена честно — счётом по таблице: 3 готовы · 2 частично (одна из них — 3 из 29) · 1 исправлено в мастере и не выложено · 1 снята · 3 нет, и одна из этих трёх — не «не начали», а живой неоценённый рынок.** Прежние 25 % не воспроизводились: девять строк давали восемнадцать неизвестных при одном уравнении, и ни одно равномерное прочтение таблицы не даёт 0.25 (ближайшие — 33 % и 37,5 %). Хуже того, названный тут же базис «по объёму выпускаемой торговой поверхности» **измерим** — и даёт другое: Bountiful это 1 312 пар из 1 341 счётной записи, то есть ~97 % поверхности, и она готова; счёт «по числу источников» даёт 30–40 %. Два законных взвешивания одного базиса расходятся на шестьдесят пунктов, поэтому одного числа тут быть не может. Замер: `agents/economy/2026_08_27_SYSTEMS_TRADE_READINESS_MEASURE.md`.

### Кто уже переписан, а кто нет

**Bountiful — переписанный набор декретов.** Каждая пара `*_wants`/`*_gives` в живой папке — один торговец: Alden, Alistair, Ambrose, Ashby, Barlow, Baxter, Beryl, Boone, Bramwell, Briggs, Chester, Cobb, Conway, Corvin, Digby, Dusty, Elowen, Erasmus, Felix, Fern, Flint, Franklin, Garrick, Gideon, Grubbs, Harlan, Hazel, Hobbs, Hollis, Mara, Marlow, Maud, Merrick, Mordecai, Morgan, Nell, Orville, Pip, Rob, Rowan, Rusty, Silas, Sterling, Tamsin, Tilly, Vex и Wendell. Это именно набор уже переписанных декретов; его живой размер намеренно не зафиксирован текстом.

| источник | что делает | где лежат офферы | можно ли переопределить цены | статус |
|---|---|---|---|---|
| VillagerTradingPlus | **сделок не добавляет ни одной** — это загрузчик. В джарнике только `available_trade_examples/` (4 файла), а читает он `default_villager_trades`, `villager_trades` и `wandering_trader_trades`, и ни один джарник пака их не несёт | наши datapack-папки, которых пока нет | да, data-driven — это готовый инструмент для авторинга, а не утечка | **нечего переопределять.** Но его конфиг несёт два глобальных множителя на цену любой сделки жителя — `trade_cost_scale` и `trade_price_multiplier_scale`, оба **1.0** (нейтральны), одинаковы на CLIENT/SERVER; ⚠️ файла нет в MASTER — он лежит в CLIENT, SERVER и `server_mirror/`, то есть переживает синк, но не переживёт чистую установку из мастера. Это один из **шести** торговых конфигов с такой же дырой (см. «Что нужно от тебя») |
| Visiting Villagers | добавляет приезжих NPC, включая Beekeeper | Java-классы `*TradeOffers` | только кодовым/событийным вмешательством, не готовым JSON-пулом | **нет override** |
| Wares | договоры доставки и сделки Wandering Trader; Packager — работник, не торговец | loot-table JSON договоров | да, KubeJS datapack выше jar | **нет override** |
| Goblin Traders | Goblin Trader и Vein Goblin Trader | datapack-таблицы мода | да | **частично:** заменена только `vein_goblin_trader/uncommon.json` |
| Superior Shop | самостоятельная экранная витрина/обмен | KubeJS-события либо world JSON-каталог | да, но это новый рынок, не замена bounty-loop | **🪦 отклонён и снят 25.08** |
| NoCube’s Villagers Sell Animals | добавляет офферы ванильным профессиям | Java `VillagersellanimalsModTrades` | лишь Java/event patch | **нет override** |
| AE2 Villager Trade Fix | наш собственный мод (`com/daedr/ae2villagerfix`): по `VillagerTradesEvent` снимает десять продаваемых id и **переводит остаток профессии `ae2:fluix_researcher` на `numismatics:bevel`** (метод `repriceIntoBevels`) | Java-обработчик пакета | да, через этот обработчик, не через данные | **сделано на уровне кода**, не проверено в игре — строка `ИГРОВОЙ_ЧЕКЛИСТ.md` «AE2-исследователь» |
| Supplementaries + Moonlight | Red Merchant и JSON-сделки жителей/странника | Moonlight `villager_trades` JSON | да | **частично:** заменены `red_merchant/rope.json` и `cannon.json` |
| Millager | меняет/добавляет villager-trade путь (`VillagerTradeHelper`) | Java | лишь Java/event patch | **нет override** |
| Acolyte | Horn Merchant и дополнительные wandering-horn trades | Java `AdditionalWanderingHornTrades` | лишь Java/event patch | **нет override** |

**Не являются источником цены, но проверены:** MerchantMarkers лишь показывает маркеры/оверлеи; EMI Trades лишь отображает реальные сделки в EMI; Guard Villagers добавляет охрану, не витрину; Pillager Caravans создаёт караваны без классов trade/merchant. Для них переоценивать нечего. Это также закрывает риск принять отображение оффера за новый рынок.

Три существующие замены лежат в `kubejs/data/goblintraders/trades/vein_goblin_trader/uncommon.json`, `kubejs/data/supplementaries/moonlight/villager_trades/red_merchant/rope.json` и `kubejs/data/supplementaries/moonlight/villager_trades/red_merchant/cannon.json`. Следовательно, **gap** — все строки «нет override» выше, плюс не заменённые таблицы самих Goblin Traders и Supplementaries/Moonlight: их ассортимент ещё нельзя называть полным и согласованным. **Но не весь этот gap достижим игроком.** `config/incontrol/spawn.json` (байт-в-байт одинаков в MASTER, `server_mirror/`, CLIENT и SERVER) запрещает `finalize`-спавн `minecraft:wandering_trader`, `minecraft:trader_llama` и `supplementaries:red_merchant`. Из 29 таблиц 14 живут за этой стеной (11 red_merchant + 3 wandering_trader), 3 уже переопределены, и **реально достижимы и не переоценены 12**: восемь тиров гоблинов (их ничто не гасит: `traderSpawnChance = 25`, `includeChance = 1.0` у всех тиров кроме legendary) и четыре ванильные профессии Supplementaries. ⚠️ Оговорка из самого конфига: сущность, поставленная структурой из NBT, `finalizeSpawn` не проходит, так что запрет — это «нет естественного спавна», а не «не существует».

### Оптовые поставки / Wares

Это отдельный **тип торгового маршрута**, который должен быть виден рядом с декретами: игрок берёт крупный договор поставки, собирает или производит партию и сдаёт её за награду. Он не равен обычной витрине NPC и не должен автоматически становиться вторым рынком или независимой валютой. В прежней инвентаризации этот маршрут записан как `Wares` с договорами доставки и сделками Wandering Trader; Packager там указан работником, а не торговцем.

Первичная инвентаризация 25.08 нашла на обеих сторонах byte-identical `wares-1.20.1-1.2.10.jar` и
подтвердила реальную загрузку по логу. Его stock loot-tables уже создают contracts, в том числе
vanilla emerald cash-out; `WaresEvents.agreementCompleted`/`batchDelivered` существуют, но в нашем
KubeJS нет listener, а в `kubejs/data/wares/` нет override. Значит контур **живой, но не
экономически принят**: до authored таблиц/награды он не считается частью выпускаемой торговли.
Доказательство и точные пути: `agents/economy/2026_08_25_WARES_SOURCE_INVENTORY.md`.

## Что нужно от тебя

По Bountiful/Superior решения сейчас не нужно: Superior Shop уже отклонён. По Wares владелец уже
сказал «оставляем», и authored spec **уже написан** — `agents/economy/2026_08_26_WARES_AUTHORING_SPEC.md`,
статус `SPEC_READY` от 26.08. Следующий ход — не новый taste call и не спека, а один проверяемый
contract-spike по ней до любого широкого rollout.

Открыты пять вопросов, и все пять держат живого Beekeeper — `OPEN.md` §1.222a **G-1…G-5**:

| id | о чём |
|---|---|
| **G-1** | Ambrose против живого Beekeeper: НПС дороже, непересекающийся набор, или меняется сама доска |
| **G-2** | валюта и номинал: `spur`, spur на покупку и bevel на продажу, или смесь |
| **G-3** | сколько из 18 сделок выживает и в какую сторону |
| **G-4** | `bee_spawn_egg`, `bee_nest`, `dandelion` — оставить / убрать / оценить |
| **G-5** | чем гасить остальные ~475 листингов: только аддоном, или аддон плюс два конфиг-рычага |

Шестой вопрос той же строки, **G-6**, спрашивать больше нечего: его посылка «у клиента нет
`visiting-villagers-common.toml`» неверна — файл есть и байт-в-байт совпадает с сервером и
`server_mirror/`. Закрытие самой строки принадлежит регистру, не этой странице.

Сверх этих пяти есть один межглавный выбор: `OPEN.md` §1.220 просит пакетное решение, какую границу
quarantine снимать первой (спавн, структуры, один NPC, explosions), и называет Beekeeper тем самым
единственным NPC. Он не считается шестым торговым вопросом, но снимается вместе с ними.

⚠️ Отдельно, без вопроса, но с ценой: **шесть торговых конфигов существуют только ниже MASTER** —
`villagertradingplus-1.0-config.json5`, `wares-common.toml`, `visiting-villagers-common.toml`,
`goblintraders-entities.toml`, `acolyte-common.toml`, `millager.json`. Все шесть есть в CLIENT,
SERVER и `server_mirror/`, и ни одного нет в `bountiful_wizards_REMAKE/config/`. Два из них несут
экономически нагруженные числа (`trade_cost_scale`, `defaultVisitorTradeChance`), а сам
`config/incontrol/spawn.json` называет ровно этот сценарий режимом отказа. Перенос — sync-gated
действие при закрытой игре.

| проверить в игре | где подробно |
|---|---|
| Увидеть реальные офферы `visitor`, `horn_merchant` и караванов, подтвердить что их спавн/витрина соответствуют статическому разбору. ⚠️ Четвёртое имя в той строке чеклиста, `silkroute_trader`, принадлежит `arkane_domains` — мода нет ни на одной стороне (`OPEN.md` §1.218: удалить целиком), так что осматривать там нечего. | `ИГРОВОЙ_ЧЕКЛИСТ.md`, строка «**Торговцы.**» |
| Взять у Гидеона или Векса заказ на зачарованную книгу и сдать её — **только после синка копий**, пока 62 записи `lvl:N` живут в CLIENT и SERVER. | `ИГРОВОЙ_ЧЕКЛИСТ.md`, строка «**Заказы на зачарованные книги**»; строка «Заказы на зачарованные книги (NBT `lvl`)» выше |
| Компас за 16 изумрудов у `ae2:fluix_researcher` должен быть `ae2:meteorite_compass`, не `minecraft:compass`. | `ИГРОВОЙ_ЧЕКЛИСТ.md`, строка «**AE2-исследователь.**» |

Сохранённые merchant offers после смены таблиц в этот список **не входят**: правило миграции
`OPEN.md` §1.144 вынесено, но не реализовано, а непринятую миграцию нельзя проверить сессией.

## 🔴 Что сломано

| дефект | чем доказано | что ломается у игрока |
|---|---|---|
| Red Merchant: обмен четырёх emerald на четыре rope снова проходит PRT — **латентно, не на живом маршруте**. | `red_merchant/rope.json` берёт 4 emerald; живая Bountiful-оценка rope в `barlow_wants.json`, `marlow_wants.json` и `morgan_wants.json` равна 203 за штуку. Значит вход 460, а четыре rope несут 812. Но `config/incontrol/spawn.json` (байт-в-байт одинаков в MASTER, `server_mirror/`, CLIENT и SERVER) запрещает `supplementaries:red_merchant` на `finalize` с 06.08, а мод спавнит его через `WanderingTraderSpawner` с `MobSpawnType.EVENT` — то есть ровно через этот hook. | Сегодня игрок этого торговца естественным спавном не встречает, поэтому печатать некому. Дефект сработает в тот день, когда запрет снимут, — и до тех пор его нельзя считать ни закрытым, ни живым. ⚠️ Два утверждения в `__comment` самого `rope.json` устарели: он всё ещё считает rope по 100 (живые пулы — 203) и всё ещё пишет, что запрет InControl «его не касается». Правка живого `kubejs/data` — sync-gated действие, игра запущена, поэтому пока только на бумаге. |

Отсутствие override у остальных источников — **не названный дефект цены**, а незакрытая работа: до полного прохода нельзя честно утверждать ни «безопасно», ни «сломано».

## В работе

Принятой реализации ассортимента сейчас нет. Superior Shop уже снят. Wares источник установлен и
спека к нему готова (`agents/economy/2026_08_26_WARES_AUTHORING_SPEC.md`, `SPEC_READY`), но на HEAD
всё ещё нет ни `kubejs/data/wares/`, ни KubeJS completion-listener, ни игровой приёмки. Значит
следующий ограниченный экономический work item — **спайк по готовой спеке**, а не сама спека, и это
по-прежнему не разрешение оставлять stock emerald payout.

## На чём это стоит

- `OPEN.md` §1.217: Bountiful — компактное ядро; только несколько базовых сделок у выбранных живых NPC, Beekeeper — первый кандидат, сложные персонажи остаются досками.
- `OPEN.md` §1.186: 51 id законно остаётся на продаже; отсутствие мирового маршрута не является основанием снимать товар с полки.
- `OPEN.md` §1.215: канон популяции — строгий счёт **690** по живым пулам; это определение, а не число из устаревшего ночного отчёта.
- `OPEN.md` §1.144: уже выданные merchant offers мигрируются точным versioned old→new signature, без полного reroll и без потери случайных/несопоставленных офферов.
- `agents/codex/2026_08_18_MODDED_TRADER_ENUMERATION_REPORT.md`: Goblin и Moonlight — data-driven поверхности; AE2 fix — post-registration rewrite; статическая перепись не заменяет game check.
- `OPEN.md` §1.250: Superior Shop не принят; до завершения его безопасного удаления Bountiful остаётся единственным выбранным ядром торговли.

## Чем проверить

```powershell
# Целостность живых Bountiful pools: нормой является exit 0.
python bountiful_wizards_REMAKE/tools/validate_pools.py

# Живой состав декретов: команда, а не число в документе, печатает пары wants/gives.
Get-ChildItem bountiful_wizards_REMAKE/config/bountiful/bounty_pools -File -Filter *.json |
  ForEach-Object BaseName | Sort-Object

# Все действующие KubeJS-замены внешних торговых таблиц.
# ⚠️ Здесь стояла пара `rg`, а ripgrep на этой машине не установлен — ни в PATH Git Bash,
# ни в PATH PowerShell, и команда молча не работала. Это её PowerShell-родная замена.
Get-ChildItem bountiful_wizards_REMAKE/kubejs/data -Recurse -File |
  Where-Object FullName -match 'goblintraders.trades|supplementaries.moonlight.villager_trades'

# Перед сравнением с dedicated server получить его путь у машины, а не писать его руками.
python bountiful_wizards_REMAKE/tools/paths.py --server

# Runtime census Wares на обеих сторонах (сверить имя, версию и SHA-256).
$packPaths = python bountiful_wizards_REMAKE/tools/paths.py
$client = (($packPaths | Select-String '^CLIENT\s').Line -replace '^CLIENT\s+','')
$server = (($packPaths | Select-String '^SERVER\s').Line -replace '^SERVER\s+','')
Get-ChildItem "$client\mods", "$server\mods" -File | Where-Object Name -match '(?i)wares'
```

Нормой для внешнего торговца является: он найден в census, его authoring surface известен, цена/ассортимент имеют явный override либо помечены как ещё не пройденные, а его фактический оффер подтверждён в игре. Для Wares источник уже доказан; нормой теперь является authored override без stock cash-out и один проверенный delivery-loop.
