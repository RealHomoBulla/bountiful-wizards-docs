# Металлургия

> **Главный мод:** Create Metallurgy · **Готовность: не измерена**
> **Больше всего мешает:** не построена двухступенчатая лестница выхода металлов
> **Ждёт твоего слова:** 1 вопрос · **Проверить в игре:** 1 пункт

## Куда идём — каким это будет на 100 %

Create Metallurgy — стержень металлургии: игрок добывает и подготавливает руду привычными путями, получает базовые металлы ранними печами и машинами, а затем строит литейку Create Metallurgy как поздний путь к полному выходу и формам металла. В конце цепочки высокие сплавы требуют нагрева и самой литейки; литейка открывает не дешёвый металл, а осмысленный производственный уровень после IE и Nether.

Create: Vintage остаётся вспомогательным модом, а не вторым источником металлов: его формы, Helve Hammer и допустимое обогащение дополняют Create, не создавая параллельную рудную лестницу. Лимиты задают KubeJS-слой `20_Metallurgy_Strip.js`, пустые forge-теги для чужих металлов, точечные удаления Vintage и модель цен: они оставляют один канон каждого металла и не дают исходным рецептам обойти существующие IE/Create-маршруты.

Из оригинала Create Metallurgy вырезаны bronze, invar, osmium, lithium, tungsten и obdurium вместе с их рецептами и worldgen: в сборке для них нет принятого канонического металла, поэтому они были бы дубликатами или новой необоснованной рудной веткой. У Vintage отключены templateless netherite-upgrade, diamond→emerald и ведро из одной пластины; последний был денежным принтером. Незеритовая прокатка rod→2 wire также снята: в связке с плавкой Create Metallurgy она давала цикл ×2.

Скоуп не решён для унификации двух sulfur: `OPEN.md` §1.76. Формулировка про «ставить ли Vintage» в `OPEN.md` §1.95 устарела: сам Vintage уже оставлен решением §1.74; его нельзя выдавать за новый вопрос владельцу.

## Где мы сейчас

| подсистема | состояние | чем доказано |
|---|---|---|
| Архитектура: Create Metallurgy — стержень, Vintage — дополнение | 🔧 частично | `agents/metallurgy/METALLURGY.md` §§2–3; незакрытый scope — `OPEN.md` §1.76 |
| Удаление лишних металлов Create Metallurgy и их worldgen | ✅ готово | `kubejs/server_scripts/create_my/20_Metallurgy_Strip.js`; пустые forge-теги; сверено с jar: 141 id снято, 0 призраков, и **ни один живой рецепт CM не требует вырезанный металл** — `agents/metallurgy/2026_08_27_SYSTEMS_METALLURGY_READINESS_MEASURE.md` §2 |
| Защита Vintage: четыре исходных решения | ✅ готово | `defaultconfigs/vintage-server.toml`, `01_Removals.js`, `pack_model.SKIP_TYPES_BASE` |
| Пятый барьер: незеритовый цикл ×2 | ✅ готово | `OPEN.md` §1.187; удалён `vintage:rolling/netherite_rod` |
| Рецепт промышленного тигля | ✅ готово | `OPEN.md` §1.97; datapack override на steel + molten steel |
| Ценовая видимость литейки и исправление brass/electrum | 🔧 частично | `CREATE_METALLURGY_TYPE_LIFT` в `tools/pack_model.py`; сверка сплавов **закончена** — оверрайд понадобился только brass, у constantan/necromium/steel/netherite расхождения нет (`agents/kiro/2026_08_16_ALLOY_PARITY_REPORT.md` §3). Осталось: `FLUID_PER_MB` без расплавов и неразрешённый выход по тегу у `casting_in_basin/electrum/block` |
| Полная цена расплавленных металлов | ❌ нет | в модели нет полной цепочки цены molten-metal; это предшественник лестницы |
| Пост-IE цена самих машин литейки | ❌ нет | `agents/metallurgy/METALLURGY.md` §6: машины всё ещё слишком ранние |
| Двухступенчатая лестница выхода | ❌ нет | `OPEN.md` §4.5a решён как концепт, KubeJS-реализации нет |

**Готовность раздела: не измерена честно.** Наблюдаемый знаменатель — 9 подсистем: 4 готовы, 2 частичны, 3 отсутствуют. Прежние 45 % задавали полный набор весов, и он действительно сходится в 100 % — но число из него не выводится: доли готовности для архитектуры и ценовой видимости нигде не заданы, поэтому остаётся `0.10·f_арх + 0.15·f_цен = 0.15` — два неизвестных при одном условии. Сверх того формулировка «Vintage-защиты почти закрыты» противоречит двум ✅ строкам таблицы, а готовый рецепт промышленного тигля не имеет веса вообще: семь весов на девять строк, то есть знаменатель — не эта таблица. Сами девять вердиктов перепроверены по jar-ам, скриптам и конфигам и все верны; не воспроизводится именно число. Вернуть процент можно только после явной шкалы веса и критерия «частично» — так же, как в `ОРЕГЕН.md` и `ТЕХНОЛОГИИ.md`.

## Что нужно от тебя

| решение | где живёт вопрос |
|---|---|
| Как поступить с двумя разными sulfur: отдельный машинный маршрут, tag или оставить раздельными | `OPEN.md` §1.76 |

| проверить в игре | где живёт подробность |
|---|---|
| После загрузки Vintage убедиться, что рецепт **`vintage:rolling/netherite_rod`** больше не выдаёт `vintage:netherite_wire`, а **`vintage:coiling/netherite_rod`** по-прежнему даёт `vintage:netherite_spring` | `OPEN.md` §1.187. ⚠️ Пункт исправлен 2026-08-27: сюда стояла ссылка на «`ИГРОВОЙ_ЧЕКЛИСТ.md`, пункт N11» — такого пункта нет. Четыре grep-а по чеклисту (`N11`, `netherite`, `vintage`, `1.187`) дают exit 1 и ноль строк, и файл вообще не использует буквенно-числовые id: он размечен `##`-заголовками. Проверка целиком напечатана слева, её можно выполнить прямо отсюда; строка в чеклисте ещё не заведена. |

⚠️ Формулировка исправлена 2026-08-27: здесь стояли id **предметов** там, где нужны id **рецептов**. `vintage:netherite_wire` — запись в реестре предметов, ни один рецепт такого id не носит, и проверяющий по старой строке искал бы не то и мог отчитаться о ложном провале.

## 🔴 Что сломано

Подтверждённых живых поломок металлургии на сегодня нет.

**Важно: старый регресс промышленного тигля уже исправлен и не должен попадать сюда как живой.** Первичный dossier описывает верный для 17.08 дефект: jar-рецепт требовал `#forge:plates/obdurium` и 30 mB `createmetallurgy:molten_tungsten`, хотя обе семьи были вырезаны. Сегодня тот же recipe id перекрыт datapack-файлом `kubejs/data/createmetallurgy/recipes/sequenced_assembly/industrial_crucible.json`: он требует `#forge:plates/steel` и 30 mB `createmetallurgy:molten_steel`. Это закрытый `OPEN.md` §1.97; утверждение «тигель нельзя собрать» устарело.

## В работе

Подтверждённой активной правки металлургического конфига сейчас нет. Эта страница фиксирует живое состояние; задачи по машинному гейту, ценам расплавов и лестнице выхода остаются только в `TODO.md`, чтобы здесь не появился второй бэклог.

## На чём это стоит

- 🔴 **Жёсткий пин версии Create — это условие существования всей ветки.** `createmetallurgy` требует `create [6.0.8,6.1.0)`, `mandatory = true`, `side = "BOTH"` (прочитано из `META-INF/mods.toml` установленного джарника), а стоит у нас ровно **6.0.8** — нижняя граница. Бамп Create до 6.1.0 останавливает **обе стороны** на `MODS_LOADING`, и починок ровно два: откатить Create или снять мод вместе со 141 удалением и 21 оверрайдом, которые на нём стоят. Решение — `OPEN.md` §1.299.
- ⚠️ **У закрытого цикла незерита ×2 есть второе, дремлющее ребро.** `01_Removals.js:613-614` утверждает, что у `vintage:netherite_wire` единственный производитель — снятый `vintage:rolling/netherite_rod`. Перечисление всех рецептов установленных джарников даёт **два**: второй, `vintage:craft/netherite_wire`, никто не снимал. Он инертен только из-за собственного условия `forge:not{forge:mod_loaded createaddition}`, а `createaddition-1.20.1-1.3.3.jar` стоит на обеих сторонах (проверено 2026-08-27). В джарнике это **шейпед-крафт: 2 × `#forge:rods/netherite` → 4 `vintage:netherite_wire`** (`data/vintage/recipes/craft/netherite_wire.json` установленного `vintage-1.20.1-1.6.0.jar`), то есть соотношение то же самое — 1:2, — но искать в игре надо именно эту форму, а не «1 → 2». **Пара сделана взаимоисключающей самим модом:** у `vintage:rolling/netherite_rod` условие `forge:mod_loaded createaddition`, у `vintage:craft/netherite_wire` — его отрицание. Живой производитель всегда ровно один, и мод сам переключает, какой именно, по наличию `createaddition` — поэтому снятие этого мода не «рискует» открыть цикл, а открывает его гарантированно. **Если `createaddition` когда-нибудь снимут, цикл ×2 открывается заново через рецепт, который никто не удалял, и `OPEN.md` §1.187 тихо расклеивается.** Записано как условие его закрытия, рецепт не тронут.
- ⚠️ **У металлургии ноль квестового и конфигурационного контента.** Ни одна из **33 глав** `config/ftbquests/quests/chapters/` её не касается (знаменатель назван: 33 главы; прежние «43» — это все `.snbt` под `ftbquests/`, то есть 33 главы плюс 10 reward-таблиц). Единственное совпадение grep-а по всему дереву — `vintagedelight` в `food_and_farming.snbt`: это мод про еду, а не Create: Vintage и не Create Metallurgy. Своего конфига у ветки тоже нет — весь контроль живёт в KubeJS. Это не дефект, но объясняет, почему прогрессия её не гейтит.
- **Роль модов.** Create Metallurgy сохраняет литейку, формы и нагрев как основную металлургическую ветку; Vintage оставлен как добавка к ней. Шесть собственных CM-металлов вырезаны, потому что у них нет принятого канона в сборке; это правило проведено через `20_Metallurgy_Strip.js` и пустые forge-теги.
- **Пять применённых решений Vintage.** (1) `allowTemplatelessRecipes = false` отключает бесплатный templateless netherite-upgrade Helve Hammer; (2) снят `vintage:curving/diamond`; (3) снят `vintage:curving/iron_sheet`, который давал ведро из одной пластины и денежный принтер; (4) crushing-enrichment принят как штатный маршрут; (5) снят `vintage:rolling/netherite_rod`, закрывший цикл незерита ×2. Основания: `agents/metallurgy/METALLURGY.md` §3.2 и `OPEN.md` §1.187.
- **Ценовая половина.** `CREATE_METALLURGY_TYPE_LIFT` включён: модель видит обработку литейки; опасные отличия brass и electrum приведены к каноническим маршрутам, а небольшое позднее отличие necromium оставлено по правилу `ELIG` из `agents/RULINGS.md`. Но `FLUID_PER_MB` ещё не задаёт цены расплавленным металлам, поэтому цепочка «расплав → форма → сплав» не посчитана от начала до конца. Вода больше не блокер: `OPEN.md` §1.111 закрыт.
- **Лестница выхода.** Двухступенчатая схема из `OPEN.md` §4.5a решена, но не реализована: ранние печи/IE/миксер дают меньший выход, а литейка возвращает полный. Порядок жёсткий: сначала в модели должны появиться полные цены расплавов, затем меняется выход. Если сначала обложить ранний путь, модель увидит его как самый дешёвый и раздует базовые металлы примерно в `1 / 0.67`; это арифметическое следствие, не вкусовой выбор. Отдельно можно поднять рецепт самих машин литейки до post-IE: машина не расходуется на слиток, поэтому это не переписывает цены.
- **Ловушки, которые остаются актуальными.** При strip всегда проверять, не требует ли сохранённая машина вырезанный металл; искать мод по `vintage`, не по `vintageimprovements`; не «дополнять» пустой тег (так можно разбудить скрытую цепь); проверять не только ingot-теги, но и plates/rods/wires; а потребителей искать с раскрытием тегов, не только по конкретному id. Любой счётчик Vintage обязан называть свой знаменатель: старые 204/409, 198/402 и ошибочные 415 нельзя смешивать.

## Чем проверить

| команда | норма |
|---|---|
| `rg -n 'plates/(steel|obdurium)|molten_(steel|tungsten)' bountiful_wizards_REMAKE/kubejs/data/createmetallurgy/recipes/sequenced_assembly/industrial_crucible.json` | В активном override есть только steel и molten steel; obdurium/tungsten отсутствуют. |
| `node --check bountiful_wizards_REMAKE/kubejs/server_scripts/create_my/20_Metallurgy_Strip.js` | Выход 0: strip хотя бы синтаксически грузится. |
| `rg -n 'vintage:(rolling/netherite_rod|curving/diamond|curving/iron_sheet)' bountiful_wizards_REMAKE/kubejs/server_scripts/create_my/01_Removals.js` | Все три защитных удаления находятся в живом слое. |
| `rg -n 'allowTemplatelessRecipes = false' bountiful_wizards_REMAKE/defaultconfigs/vintage-server.toml` | Настройка Helve Hammer остаётся выключенной; затем подтвердить её в игре по чеклисту. |
| `python -c "import sys; sys.path.insert(0, 'tools'); from audit_jar_printers import FLUID_PER_MB; print([x for x in FLUID_PER_MB if 'molten_' in x])"` из `bountiful_wizards_REMAKE` | Пустой список означает, что цена расплавов всё ещё не реализована; непустой требует повторно оценить готовность и порядок лестницы. ✅ Перепрогнано 2026-08-27 в этом чекауте: **exit 0**, вывод `[]` — цены расплавов по-прежнему нет. `FreshnessError`, из-за которого строка когда-то не исполнялась, не воспроизводится. |
| `javap`/`unzip` по `META-INF/mods.toml` установленного `createmetallurgy` | Диапазон `create [6.0.8,6.1.0)` виден целиком. Если Create вышел за него — не обновлять до решения §1.299. |
