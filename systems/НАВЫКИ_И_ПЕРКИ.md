# Навыки и перки

> **Главный мод:** Puffish Skills (наше дерево, сейчас выключено) · рядом живёт ненастроенный Skill Proficiencies · **Готовность: 23 %**
> **Больше всего мешает:** §1.305/§1.306 закрыты при ParCool 4.x, а живой ParCool — снова **3.4.3.3**; пока базовая версия не сверена, разделение деревьев (`PARCOOL-PATHS-SEPARATION`) исполнять нельзя.
> **Ждёт твоего слова:** 6 вопросов (§1.182 + новые §1.398–1.402) · **Проверить в игре:** SP-1…SP-6 и профиль «перки выключены» в `ИГРОВОЙ_ЧЕКЛИСТ.md`

## Куда идём — каким это будет на 100 %

Главная система личного развития — одно дерево **Puffish Skills** в неймспейсе `arcane_engineer`. Игрок получает очки за понятные действия, выбирает профильные возможности и видит, почему следующий узел закрыт; FTB Quests остаётся картой исследования и фаз, а не вторым деревом способностей. Дерево не создаёт параллельной магической экономики: оно открывает способы играть, и каждую награду можно проверить на серверной либо клиентской границе.

Это именно одно **авторское** дерево: второй набор категорий означал бы второй бесплатный бюджет очков. Amblewood не возвращается (`OPEN.md` §1.157); Passive Skill Tree дважды отвергнут. По §1.306-B движение принадлежит ParCool с его собственным деревом, а `arcane_engineer:paths` — всему остальному без элементов ParCool; но этот рулинг принят при 4.x и требует сверки с живым 3.4.3.3 (`knowledge/skills_perks/CURRENT_STATE.md`).

⚠️ **Рядом уже работает вторая система, которую мы не настраивали:** `specialities` (Skill Proficiencies 1.6.3) стоит на обеих сторонах и ведёт **15 навыков до 100 уровня на дефолтах мода**, с бонусами вплоть до +50 % урона и −30 % задержки атаки поверх Apotheosis. Это не наш дизайн, а фон, который надо либо выверить, либо принять сознательно — §1.402.

Целевой объём нашего дерева не выбран: `OPEN.md` §1.182 (малая первая ветка или полная). До него страница не обещает список узлов или темп.

## Где мы сейчас

| подсистема | вес | доля | состояние | чем доказано |
|---|---:|---:|---|---|
| Движок Puffish и точка входа нашего дерева | 3 | 0,5 | ⏸ обратимо выключено — и это уже выложено | `puffish_skills-0.18.3` на обеих сторонах; `config.json` держит `"categories": []` во всех **трёх** копиях (SHA-сверка, `knowledge/skills_perks/INDEX.md` «Freshness») |
| Синхронизация данных дерева между копиями | 1 | 1,0 | ✅ готово | пять JSON дерева и оба скрипта перков хеш-идентичны master/клиент/сервер — выключенный профиль именно выложен, а не только написан |
| Проверочная сетка и два прикладных крючка | 2 | 0,5 | ⏸ временно выключены | 16 узлов сохранены, но не грузятся; оба крючка под `PACK_PUFFISH_PERKS_ENABLED = false`; GameStage-флаги игроков не удаляются. Обратимый тестовый режим, не постоянный дизайн |
| Релизный объём и связи настоящего дерева | 3 | 0 | 🅿️ ждёт решения | `OPEN.md` §1.182 открыт: `connections.json` пуст, все 16 узлов `root`, `starting_points: 20` покрывает сетку целиком |
| Получение очков действием и защита темпа | 2 | 0 | ❌ нет | `experience.json` отсутствует; в джарнике 14 встроенных источников и два anti-farm ограничителя, настроено ноль |
| Skill Proficiencies (`specialities`) как живой фон | 2 | 0 | ❌ работает на чужих дефолтах | 15 навыков до 100 уровня; 30 книг знаний (+25/+100 уровней) без единого маршрута в паке; шесть конфигов домена вне мастера. Чужой jar без нашей настройки = 0 по шкале. `knowledge/skills_perks/MOD_SURFACE.md`; решения §1.398, §1.401, §1.402 |
| Игровая приёмка игроком | 2 | 0 | ❌ нет | SP-1…SP-6 и профиль «перки выключены» в `ИГРОВОЙ_ЧЕКЛИСТ.md` не пройдены; старые пункты включённого дерева не могут пройти при пустом `categories` и требуют сверки чеклиста |
| Дерево самого ParCool | — | — | 🚫 чужой контент | живой ParCool — **3.4.3.3** на обеих сторонах (хеш-сверка). Утверждение о «трёх деревьях и 23 действиях» относилось к 4.x и текущую сборку не описывает |

**Готовность раздела: 23 %** — по единой шкале `agents/docs_audit/2026_08_29_SYSTEMS_READINESS_SINGLE_SCALE.md` (вес 3/2/1 · доля 1,0 / 0,5 / 0): **Σ(вес × доля) = 3,5 при Σ вес = 15 → 100 × 3,5 / 15 = 23,3 → 23 %.** Падение с прежних 27 % — не потеря работы, а честный знаменатель: добавилась строка `specialities` (вес 2, доля 0), которую мод-сюрфейс 2026-09-02 доказал живой и ненастроенной. 🚫 Дерево самого ParCool в знаменатель по-прежнему не входит (чужой контент, шкала §4).

## Что нужно от тебя

| решение | суть и рекомендация | где |
|---|---|---|
| Объём первой настоящей ветки | малая ветка / полное дерево / Project MMO — блиц открыт | `OPEN.md` §1.182 |
| 🔴 30 книг знаний `specialities` без маршрута | **A (рек.): оставить creative-only**; B — спрятать в вьювере; C — костированный маршрут (тогда книга +100 уровней ≈ 79 250 XP по V2.1) | `OPEN.md` §1.398 |
| 🟡 ParCool: мир противоречит сиду, у игрока залочен Roll | живой мир `limitation_imposed=false` против `true` в сиде; у UUID `6a98ac0a…` остался индивидуальный запрет Roll с теста 22.08, переживающий глобальный переключатель. **C (рек.): сначала мировой дефолт, потом индивидуальная запись** | `OPEN.md` §1.399 |
| 🟡 `amblewood_skills` — пять папок мёртвых данных | **B (рек.): оставить с README**; A — удалить; C — доделать (новый мод + баланс) | `OPEN.md` §1.400 |
| 🟢 Шесть конфигов домена вне мастера | **B (рек.): принять только геймплейные** (`skill-proficiencies.json` + пара ParCool) | `OPEN.md` §1.401 |
| 🟡 Баланс `specialities` против нашего боя | +50 % урона и −30 % задержки атаки поверх Apotheosis. **A (рек.): сначала игровая приёмка, потом решать** | `OPEN.md` §1.402 |

⚠️ Отдельная сверка, не новый вопрос: §1.305/§1.306-B закрыты при 4.x, живой ParCool — 3.4.3.3. До исполнения `PARCOOL-PATHS-SEPARATION` продюсер обязан подтвердить, что откат на 3.x — намеренная база (`knowledge/skills_perks/DECISIONS_AND_VERIFICATION.md`, «Gates»).

| проверить в игре | где |
|---|---|
| SP-1…SP-6 (`specialities`: рост уровней, эффект бонусов, книги) | `ИГРОВОЙ_ЧЕКЛИСТ.md` |
| Профиль «перки выключены»: меню Puffish не открывается, старые эффекты не действуют | `ИГРОВОЙ_ЧЕКЛИСТ.md` (строка ~57) |
| Старые пункты включённого дерева («Дерево „Пути“», «Кувырок и чутьё») | не могут пройти при пустом `categories` — сверить чеклист с намеренной версией ParCool до любых замеров |

## 🔴 Что сломано

**У одного игрока молча залочен кувырок.** UUID `6a98ac0a-25c6-3aca-955c-4de9678bcda8` несёт индивидуальный запрет Roll с теста 2026-08-22; по байткоду ParCool такая запись **переживает** глобальный `limitation_imposed=false`. Аккаунт не умеет кувырок, и никакой экран этого не объясняет. Снятие — одна команда или правка JSON на остановленном сервере; выбор порядка — `OPEN.md` §1.399.

**Живой серверный мир противоречит собственному сиду:** `world/serverconfig/parcool-server.toml` держит `limitation_imposed=false`, `defaultconfigs`-сид — `true`. Любой новый мир получит другое поведение, чем текущий. Тоже §1.399.

🪦 **Прежний главный дефект страницы снят как устаревший.** «Половина ParCool у перка „Кувырок“ отгружена и мертва» описывал ParCool 4.0.0.x: в живой сборке снова **3.4.3.3**, где семейство `parcool limitation`, действие `Roll` и ключ `stamina_consumption_of_Roll` существуют (живой `parcool-server.toml`: `permit_Roll = true`, `stamina_consumption_of_Roll = 100`). Авторские команды перка соответствуют установленному поколению — а сами крючки в любом случае выключены флагом. Разбор 4.x сохраняется как исторический (`agents/progression/2026_08_27_SKILLS_PAGE_FLAGGED_DEFECTS.md`) и станет актуален снова только при возврате 4.x.

## В работе

Новой реализации дерева не ведётся. `PARCOOL-PATHS-SEPARATION` стоит в `TODO.md`, но его 4.x-посылка не совпадает с установленным 3.x — исполнение заблокировано сверкой версии (см. выше). Список будущих работ живёт только в `TODO.md`.

## На чём это стоит

- Основой остаётся голый `puffish_skills` 0.18.3 и наш datapack `arcane_engineer`; Amblewood не восстанавливается — `OPEN.md` §1.157.
- Тестовая сетка остаётся частью приёмки, но не подменяет решение об объёме — `OPEN.md` §1.182 (подстроки 2026-08-22: сетка остаётся, сироты amblewood — в архив, к 27.08 не исполнено).
- **§1.305 (2026-08-30):** все элементы ParCool убрать из `arcane_engineer:paths`, перк не перенацеливать. **§1.306-B (2026-08-30):** оба дерева живут, ParCool владеет движением. ⚠️ Оба рулинга приняты при 4.x — исполнение только после сверки базовой версии.
- Мод-сюрфейс домена 2026-09-02 открыл живой `specialities` и породил §1.398–1.402 — `knowledge/skills_perks/MOD_SURFACE.md`.
- Встроенные источники опыта и anti-farm лимиты Puffish — по установленному джарнику 0.18.3, не по вики: `agents/progression/2026_08_20_ABILITY_PERK_SYSTEM_STUDY_VERIFIED.md` (Puffish-часть; ParCool-часть исторична).
- FTB Quests, GameStages и фазы принадлежат `ПРОГРЕССИЯ.md`; здесь — только личная способность игрока.

## Чем проверить

- `python bountiful_wizards_REMAKE/tools/paths.py` — корни трёх копий; пути руками не подставлять.
- **Первым делом — поколение ParCool:** `Get-ChildItem (Join-Path $root 'mods') -Filter 'ParCool*.jar'` на обеих сторонах. Норма сейчас: `3.4.3.3`, побайтно одинаковый. Любое утверждение о командах ParCool без этой проверки — слух.
- `Get-Content bountiful_wizards_REMAKE/kubejs/data/arcane_engineer/puffish_skills/config.json` — норма выключенного профиля: `"categories": []`.
- `Select-String bountiful_wizards_REMAKE/kubejs/server_scripts/33_Perk_Projection.js, bountiful_wizards_REMAKE/kubejs/client_scripts/98_Perk_Attuned_Sight.js -Pattern 'PACK_PUFFISH_PERKS_ENABLED'` — норма: `false` в обоих.
- `Test-Path bountiful_wizards_REMAKE/kubejs/data/arcane_engineer/puffish_skills/categories/paths/experience.json` — сейчас `False`; появление файла — отдельный предмет проверки.
- Хеш-сверка пяти JSON дерева и двух скриптов по трём копиям (полная команда — `knowledge/skills_perks/DECISIONS_AND_VERIFICATION.md` §«Short evidence commands») — норма: побайтное совпадение.
- Серверный лог: `rg -n "arcane_engineer|\[perks\]" "$server/logs/latest.log"` — при выключенном профиле строк загрузки категории и крючков быть не должно; строка `[perks]` доказывает запуск скрипта, **не** приём внешней команды.
