# Навыки и перки

> **Главный мод:** Puffish Skills · **Готовность: не измерена**
> **Больше всего мешает:** отгруженный перк «Кувырок» ничего не делает — его половина ParCool написана под API, которого в установленном моде больше нет.
> **Ждёт твоего слова:** 3 вопроса · **Проверить в игре:** 2 набора проверок

## Куда идём — каким это будет на 100 %

Главная система личного развития — одно дерево **Puffish Skills** в неймспейсе `arcane_engineer`. Игрок получает очки за понятные действия, выбирает профильные возможности и видит, почему следующий узел закрыт; FTB Quests остаётся картой исследования и фаз, а не вторым деревом способностей. Дерево не создаёт параллельной магической экономики: оно открывает способы играть — например, личное движение, доступ к информации или специальные действия — и каждую награду можно проверить на серверной либо клиентской границе.

Это именно одно дерево: второй набор категорий означал бы второй бесплатный бюджет очков. Amblewood не возвращается (`OPEN.md` §1.157): у него не было точки входа Puffish, а восстановление дало бы вторую, не выбранную систему. Passive Skill Tree также не входит в пак: его дважды отвергли как второе дерево, во второй раз владелец прямо сказал, что мод был только для проверки.

У Puffish уже есть собственные источники опыта действий и защита от фарма; значит маршрут «добываешь блоки — растёт горное развитие» можно собрать изменением нашего datapack, без нового мода. Но целевой объём и темп ещё не выбраны: `OPEN.md` §1.182 оставляет выбор между малой первой веткой и полной; до него эта страница не обещает окончательный список узлов или скорость их открытия.

## Где мы сейчас

| подсистема | состояние | чем доказано |
|---|---|---|
| Движок Puffish и точка входа нашего дерева | ✅ готово | `arcane_engineer` есть в master, серверной и клиентской копиях; серверный лог подтверждает загрузку datapack |
| Синхронизация данных дерева между копиями | ✅ готово | контрольные суммы всех файлов дерева совпали в трёх копиях |
| Проверочная сетка и два прикладных крючка | 🔧 сетка и крючок Create живые, крючок ParCool отгружен и мёртв | 16 узлов покрывают **все 5** типов наград, которые джарник реально регистрирует (`BuiltinRewards`: attribute · command · points · scoreboard · tag). Клиентский predicate Create зарегистрирован в рантайме — клиентский `logs/latest.log` `[perks] goggle predicate registered`. 🔴 А проекция ParCool целится в `parcool limitation … possibility Roll`, которого в установленном **ParCool 4.0.0.x нет вообще**: ни класса `Limitation`, ни действия `Roll` (есть `parcool:breakfall`); `runCommandSilent` глушит отказ, поэтому скрипт пишет успех. Решение — `OPEN.md` §1.305 |
| Релизный объём и связи настоящего дерева | 🅿️ ждёт решения | `OPEN.md` §1.182: решение оставить тестовую сетку принято, но размер первой содержательной ветки — нет. Сегодня связей нет вовсе: `connections.json` пуст, все 16 узлов `root`, ни одного поля `required_*`, а `starting_points: 20` покрывает всю сетку сразу |
| Получение очков действием и защита темпа | ❌ нет | в активной категории нет `experience.json`; в джарнике зарегистрировано **14** встроенных источников и **два** ограничителя anti-farm, настроено **ноль** |
| Игровая приёмка игроком | ❌ нет | два незакрытых пункта в `ИГРОВОЙ_ЧЕКЛИСТ.md`, раздел «Магия, перки и прогрессия — один персонаж»: «Дерево „Пути“» и «Кувырок и чутьё инженера» |
| Второе дерево способностей, пришедшее с джарником ParCool | 🔴 живое и нерешённое | в живом серверном `world/serverconfig/parcool-server.toml` стоит `[Game] enable_skill_tree = true`; ParCool 4.x везёт свой `SkillTreeScreen` и три дерева, 23 действия открываются за уровни опыта (`learn_cost`, у `breakfall` — 10). Это ровно тот второй бесплатный бюджет, который раздел «куда идём» запрещает. Решение — `OPEN.md` §1.306 |

**Готовность раздела: не измерена** — процент из этой таблицы не выводится: весов подсистем нет, а доля для «🔧 частично» и «🅿️ ждёт решения» не задана нигде (`OPEN.md` §1.298). Вместо числа — счёт: **две подсистемы готовы** (движок с точкой входа и синхронизация данных: джарник побайтово одинаков на обеих сторонах, пять файлов дерева совпали во всех трёх копиях, серверный лог подтверждает загрузку datapack), **одна расщеплена** (сетка и крючок Create живые и доказаны в рантайме, крючок ParCool отгружен и не работает), **одна ждёт твоего решения** (релизный объём — и связей в файлах сегодня нет никаких), **две отсутствуют** (опыт за действия, игровая приёмка) и **одна живёт вне нашего контроля** (дерево самого ParCool). Написанного-но-не-выложенного в этом контуре нет ни одного файла. Замер: `agents/progression/2026_08_27_SYSTEMS_SKILLS_READINESS_MEASURE.md`.

## Что нужно от тебя

| решение | варианты и цена | где |
|---|---|---|
| 🔴 Чем починить мёртвую половину перка «Кувырок»? | **A (рекомендация).** Перенацелить на команды ParCool 4.x (`parcool action unlock\|lock parcool:breakfall`): цена — две строки в `33_Perk_Projection.js`, две в `definitions.json` плюс правка видимого игроку описания, и один синк при закрытой игре. **B.** Оставить перк чистым флагом GameStages без действия ParCool: цена — перк перестаёт что-либо давать в движении, но перестаёт и врать. **C.** Заменить демо-перк другим действием ParCool: цена — новый дизайн узла, старое описание всё равно переписывать. ⚠️ Сам по себе A не делает перк воротами — `breakfall` доступен всем, см. следующую строку. | `OPEN.md` §1.305; разбор — `agents/progression/2026_08_27_SKILLS_PAGE_FLAGGED_DEFECTS.md` |
| 🔴 Что делать со вторым деревом способностей, которое привёз ParCool 4.x? | **A (рекомендация).** `enable_skill_tree = false`: ParCool остаётся чистым мувментом, единственный бюджет способностей — `arcane_engineer:paths`; цена — игрок теряет 23 покупаемых действия, которых мы ему и не обещали. **B.** Оставить оба и разделить по темам (мувмент — ParCool, остальное — наше), сняв дублирующий перк: цена — две прогрессии надо согласовывать вечно. **C.** Оставить внахлёст: цена — посылка «это именно одно дерево» из раздела «куда идём» становится ложной. ⚠️ Перед A/B нужен один игровой замер — строка ниже. | `OPEN.md` §1.306 |
| Какой первый настоящий путь развития нужен поверх проверочной сетки? | **A.** Оставить малую ветку Puffish и добавить опыт за выбранные действия: цена — настройка datapack и игровая калибровка, нового мода нет. **B.** Сразу заполнить полное дерево Puffish: цена — большой объём дизайна узлов, связей и темпа до проверки баланса. **C.** Взять Project MMO для уровней действий: цена — новый мод, его карантин и согласование двух прогрессий; Puffish тогда нельзя оставлять вторым бесплатным бюджетом. | `OPEN.md` §1.182; блиц открыт, решения здесь не принимаются |

| проверить в игре | где |
|---|---|
| Открывается ли дерево, применяются ли тестовые награды, работают ли «Кувырок», респек и «Чутьё инженера» без гогглов | `ИГРОВОЙ_ЧЕКЛИСТ.md`, раздел «Магия, перки и прогрессия — один персонаж» — пункты «Дерево „Пути“» и «Кувырок и чутьё инженера» |
| Стартует ли новый персонаж с уже открытым `parcool:breakfall` — это и есть замер, без которого нельзя выбрать A или B в `OPEN.md` §1.306: если да, перк «Кувырок» не может быть воротами ни в каком варианте | `ИГРОВОЙ_ЧЕКЛИСТ.md`, раздел «Магия, перки и прогрессия — один персонаж» — пункт «Своё дерево ParCool» |

## 🔴 Что сломано

**Половина ParCool у перка «Кувырок» отгружена и мертва.** `perk_shared_roll` шлёт `parcool limitation enable individual of @s` и `parcool limitation set individual of @s possibility Roll …` — а в установленном ParCool `4.0.0.x` нет ни семейства команд `limitation`, ни действия `Roll`: у джарника вообще нет класса с `Limitation` в имени и ни одного класса, чьи байты содержат строку `limitation` — проверено на обеих сторонах, джарник за джарником. Перекат теперь зовётся `parcool:breakfall`, а команда — `parcool action unlock|lock <id> <players>`. Оба отказа съедены молча: `puffish_skills` выполняет `unlock_command` с `withSuppressedOutput()`, а наш `33_Perk_Projection.js` — через `runCommandSilent`, который не бросает; поэтому в клиентском логе стоит `[perks] roll projection … permitted=false` как будто всё удалось, и `Unknown or incomplete command` там нет ни разу. **Что видит игрок:** покупает «Кувырок», прыгает с шести блоков — кувырка нет, урон не отменяется, и респек ничего не меняет. Это регрессия обновления ParCool `3.4.3.3 → 4.0.0.x`, а не ошибка автора: под 3.x перк был написан верно. Твой выбор — `OPEN.md` §1.305; разбор и варианты замены — `agents/progression/2026_08_27_SYSTEMS_SKILLS_READINESS_MEASURE.md` §4 и §9 (D1), воспроизведение по джарнику — `agents/progression/2026_08_27_SKILLS_PAGE_FLAGGED_DEFECTS.md` §2; правка лежит в паке (`definitions.json` и `33_Perk_Projection.js`), эта страница её не делает.

**Описание «Кувырка» врёт игроку в самом дереве.** `extra_description` обещает «Расход выносливости за перекат задан сервером (`stamina_consumption_of_Roll`)» — такого ключа в схеме 4.x нет (`grep` по живому `world/serverconfig/parcool-server.toml` — пусто); живой ключ `[Action.parcool.breakfall] cost_start = 50`. Текст правится вместе с командами перка.

⚠️ Даже после починки команд перк не станет воротами сам по себе: `breakfall` в живом серверном конфиге стоит `available = true`, и собственное дерево ParCool продаёт его за 10 уровней опыта — см. строку «Второе дерево способностей» выше, `OPEN.md` §1.306 и §9 (D2) замера.

Прежний отказ всего datapack из-за недействительного типа награды **исправлен** и живой поломкой не является — серверный лог подтверждает загрузку `arcane_engineer`. Незаполненный опыт действий и незакрытая игровая приёмка — это пробелы реализации и доказательства, а не поломки.

## В работе

Новой реализации дерева сейчас не ведётся: сначала нужен выбор из `OPEN.md` §1.182. Починка перка «Кувырок» тоже не начата — она ждёт `OPEN.md` §1.305, а её смысл зависит от `OPEN.md` §1.306; обе правки требуют закрытой игры и синка трёх копий. Эта страница фиксирует состояние; перечень и порядок будущих работ остаются только в `TODO.md`.

## На чём это стоит

- Основой остаётся голый `puffish_skills` и наш datapack `arcane_engineer`; Amblewood не восстанавливается — `OPEN.md` §1.157.
- Тестовая сетка остаётся частью приёмки, но не подменяет решение о содержательном объёме дерева — `OPEN.md` §1.182.
- Отвергавший весь datapack недействительный тип награды удалён; доказательство прежней причины и исправления — `OPEN.md` §1.207, успешная загрузка подтверждена текущим серверным логом.
- Встроенные источники опыта и лимиты anti-farm установлены по установленному `puffish_skills` 0.18.3, а не по вики — `agents/progression/2026_08_20_ABILITY_PERK_SYSTEM_STUDY_VERIFIED.md`. 🪦 **ParCool-половина того же исследования недействительна:** `Limitation` API, ключ `permit_Roll` и разбор `ServerLimitation.apply` описывали ParCool 3.x, которого в паке больше нет — перепроверено по джарнику 4.0.0.x, `agents/progression/2026_08_27_SYSTEMS_SKILLS_READINESS_MEASURE.md` §4b.
- FTB Quests, GameStages и фазы принадлежат странице `ПРОГРЕССИЯ.md`; здесь остаётся только личная способность игрока, чтобы не завести второй прогрессионный маршрут.

## Чем проверить

- `python bountiful_wizards_REMAKE/tools/paths.py` — сначала печатает актуальные корни master, сервера и клиента; перед сравнением не подставлять путь вручную.
- `rg --files bountiful_wizards_REMAKE/kubejs/data/arcane_engineer/puffish_skills` — должны быть видны точка входа и данные категории «Пути».
- ```powershell
  $roots = @((python bountiful_wizards_REMAKE/tools/paths.py --master).Trim(), (python bountiful_wizards_REMAKE/tools/paths.py --server).Trim(), (python bountiful_wizards_REMAKE/tools/paths.py --client).Trim())
  $rel = @('config.json','categories/paths/category.json','categories/paths/connections.json','categories/paths/definitions.json','categories/paths/skills.json')
  foreach ($root in $roots) { foreach ($file in $rel) { Get-FileHash (Join-Path $root "kubejs/data/arcane_engineer/puffish_skills/$file") -Algorithm SHA256 } }
  ```
  — для каждого одноимённого файла хэш должен совпасть во всех трёх копиях.
- ```powershell
  $server = (python bountiful_wizards_REMAKE/tools/paths.py --server).Trim()
  rg -n "\[puffish_skills\].*arcane_engineer.*loaded successfully" "$server/logs/latest.log"
  ```
  — нормой является успешная загрузка datapack, а не пустой результат поиска.
- `Test-Path bountiful_wizards_REMAKE/kubejs/data/arcane_engineer/puffish_skills/categories/paths/experience.json` — сейчас ожидается `False`; после выбора маршрута опыта файл и его anti-farm настройки должны стать отдельным предметом проверки.
- ```powershell
  $client = (python bountiful_wizards_REMAKE/tools/paths.py --client).Trim()
  $jar = (Get-ChildItem (Join-Path $client 'mods') -Filter 'puffish_skills*.jar').FullName
  Add-Type -AssemblyName System.IO.Compression.FileSystem; $zip = [IO.Compression.ZipFile]::OpenRead($jar)
  $zip.Entries | Where-Object FullName -match '/experience/source/builtin/.+ExperienceSource\.class$|AntiFarmingPer(Chunk|Entity)\.class$' | ForEach-Object FullName; $zip.Dispose()
  ```
  — выводит доступные в установленном джарнике источники действий и оба ограничителя фарма; это проверка API до проектирования опыта, не доказательство игрового баланса.
- ```powershell
  $client = (python bountiful_wizards_REMAKE/tools/paths.py --client).Trim()
  $jar = (Get-ChildItem (Join-Path $client 'mods') -Filter 'ParCool*.jar').FullName
  Add-Type -AssemblyName System.IO.Compression.FileSystem; $zip = [IO.Compression.ZipFile]::OpenRead($jar)
  $zip.Entries | Where-Object FullName -match 'Limitation|ParCoolActions\.class' | ForEach-Object FullName; $zip.Dispose()
  Select-String -Path bountiful_wizards_REMAKE/kubejs/data/arcane_engineer/puffish_skills/categories/paths/definitions.json, bountiful_wizards_REMAKE/kubejs/server_scripts/33_Perk_Projection.js -Pattern 'parcool (limitation|action)' | ForEach-Object { "$($_.Filename):$($_.LineNumber): $($_.Line.Trim())" }
  ```
  — единственная проверка, которая ловит поломку из раздела «Что сломано». Норма: класса с `Limitation` в джарнике **нет**, `ParCoolActions.class` **есть**, и каждая команда `parcool …` в наших двух файлах — это `parcool action unlock|lock <id> <players>` с id из `ParCoolActions`. Любое `parcool limitation …` в выводе означает, что перк снова целится в удалённый API.
- ```powershell
  $client = (python bountiful_wizards_REMAKE/tools/paths.py --client).Trim()
  $server = (python bountiful_wizards_REMAKE/tools/paths.py --server).Trim()
  foreach ($log in @("$client/logs/latest.log", "$server/logs/latest.log")) {
    $hits = @(Select-String -Path $log -Pattern '\[perks\]' -ErrorAction SilentlyContinue)
    Write-Output "$log -> $($hits.Count)"; $hits | ForEach-Object { "   $($_.LineNumber): $($_.Line.Trim())" }
  }
  ```
  — оба крючка перков сами пишут в лог, и это единственное доказательство, что они выполнились. ⚠️ Строка `[perks] roll projection … permitted=…` доказывает только запуск скрипта, **не** приём команды ParCool: `runCommandSilent` возвращается и при отказе. Пустой результат на серверной стороне сам по себе не дефект — сперва проверь, заходил ли вообще игрок (`Select-String -Pattern 'joined the game'`).
